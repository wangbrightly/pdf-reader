# v0.3.0 决策复核：智能排版（CV 版面识别）路线是否重新启用

复核日期：2026-09-04
复核起因：用户提出"用小白 PDF 阅读器同款智能排版技术"，与 2026-08-29 NOTES.md #55/#56 记录的**正式否决**结论存在分歧。本文档逐条重新审视否决理由，不默认推翻也不默认维持。

> 标注说明：沿用 SELECTION.md 的约定，关键论断标注来源与核实日期；无法联网核实的一律标注"训练记忆，未核实"。

---

## 0. 复核结论先行

三条否决理由里，**两条可以被具体方案部分化解，第三条（许可证）是硬约束，且化解它所需的替代方案恰好是准确率更弱的那一档**——这是本次复核最重要的发现，直接决定了推荐路线。

| 否决理由 | 今天是否仍成立 | 变化 |
|---|---|---|
| ① 模型体量差一个数量级 | **仍成立**，且无法在预算内消除 | 找到了 Apache-2.0 替代模型家族，但体量与顶配模型的差距依然是数量级级别 |
| ② 四层管线复杂度 | **可实质性降低** | 本项目的实际文档语料（真数字排版 PDF，非扫描件）让 OCR 层和页面分类层大概率是冗余的 |
| ③ AGPL-3.0 与闭源 App 冲突 | **仍成立**，且是本次复核唯一没有找到令人满意解法的一条 | 有 Apache-2.0 替代模型，但精度落在已实测"劣于/持平现有几何规则"那一档 |

---

## 1. 理由①：模型体量——仍成立

小白 PDF 用的 `doc_layout_x_260625.onnx`（YOLOv12x）236MB。可行的 Apache-2.0 替代是百度 PaddleOCR/PaddleX 生态的 **PP-DocLayout 系列**：

| 型号 | 底层架构 | ONNX 体积 | 许可证 |
|---|---|---|---|
| PP-DocLayout-S | PicoDet-S | 4.7 MB | Apache-2.0 |
| PP-DocLayout-M | PicoDet-M | 22.4 MB | Apache-2.0 |
| PP-DocLayout-L | RT-DETR-L | 123.4 MB | Apache-2.0 |

（体积数据来自 [PaddlePaddle/PP-DocLayout-M · Hugging Face](https://huggingface.co/PaddlePaddle/PP-DocLayout-M)、[oar-ocr/docs/models.md](https://github.com/GreatV/oar-ocr/blob/main/docs/models.md)，2026-09-04 核实；许可证来自上游 [PaddleOCR/LICENSE](https://github.com/PaddlePaddle/PaddleOCR/blob/main/LICENSE)，2026-09-04 核实）

M 档（22.4MB）与顶配 YOLOv12x（236MB）差距约 10.5 倍，量级差距确认仍然存在。L 档（123.4MB）把差距缩小到约 1.9 倍，但会撞上你定的 150MB APK 体积上限（见第 4 节）。

**8 月 POC 已经测过的正是 M 档**——四页真实问题页对照，2 张更差、1 张更好、1 张打平（NOTES.md，2026-08-29）。也就是说，"体量差距"和"精度差距"这两件事目前是绑定的：唯一没有许可证问题的档位，恰好是已经被证明精度不够的那个档位。L 档没测过，是否够用是未知数，但先撞体积预算。

## 2. 理由②：四层管线复杂度——可实质性降低

小白 PDF 的四层：页面分类 → CV 版面检测 → OCR（检测+识别）→ 重排合成引擎。

对照本项目实际情况：

- **本项目处理的文档全部是"真数字排版"PDF**（Chromium 打印、InDesign 导出、Ansys/HFSS 技术手册），不是扫描件——这从 v0.1.0 起就是产品前提，现有 65+ 条 NOTES.md 修复记录里没有一条是"文字层缺失需要 OCR"，全部是"文字层存在但坐标/分段逻辑判断错了"。
- 由此推论：**OCR 检测+识别两个模型大概率是冗余的**——本项目已经用 PdfBox-Android 抽取到逐字符坐标的真实文字层，不需要用 OCR 重新"认"一遍图片里的字。
- **"重排合成引擎"（`libtt_ugen_layout.so`）本项目已经有对应物**——现有 Kotlin `reflow()` 算法。CV 模型如果只是输出版面区域的 bbox（"这块是正文""这块是表格""这块是图片"），完全可以喂给现有的段落/图片归类逻辑，不需要移植一套新的合成引擎。
- **页面分类层**（判断整页属于什么类型）目前不确定是否必要——现有几何规则本身就是在做"这页要不要整页栅格化"的二元判断，如果 CV 版面检测本身能输出可靠的区域划分，页面分类层可能可以省略，但这点没有实测验证，标记为待验证而非确认结论。

**结论**：如果走 CV 路线，合理的范围是"只加一个版面检测模型，输出接入现有 Kotlin 抽取+重排代码"，而不是移植小白 PDF 的四层架构。这把复杂度降低到接近"新增一种局部检测信号"的量级，不是原否决理由描述的"整套四层管线"。这一条判断基于对本项目文档语料特征的分析，**没有做过针对性实测（比如故意找一份扫描件 PDF 验证 OCR 真的用不上）**，建议在选定路线后第一步就用现有语料验证这个假设，而不是默认成立。

## 3. 理由③：AGPL-3.0 冲突——仍成立，是本次复核的关键卡点

### 3.1 顶配模型链路的许可证状态（沿用 2026-08-29 实锤，未变化）

- `doc_layout_x_260625.onnx` = YOLOv12x（Ultralytics）：**AGPL-3.0**
- `pc_260629.onnx` = YOLO12s-cls（Ultralytics）：**AGPL-3.0**

Ultralytics 官方许可证页面（[ultralytics.com/license](https://www.ultralytics.com/license)，2026-09-04 核实）确认：使用 Ultralytics 的代码、模型架构或训练/微调后的模型权重，必须二选一——整个项目按 AGPL-3.0 开源，或购买 Enterprise 商业授权。这条约束**覆盖训练产出的权重本身**，不是"用了他们的训练代码才算"，所以不存在"我只用权重不用代码就没事"的空子。

### 3.2 商业授权成本

官方定价页面（[ultralytics.com/pricing](https://www.ultralytics.com/pricing)，2026-09-04 核实）不公开具体金额，需联系销售定制报价。GitHub Discussion 里有一条个案提到 **$5,000/年**（[github.com/orgs/ultralytics/discussions/7440](https://github.com/orgs/ultralytics/discussions/7440)），但这是特定案例的报价，**不代表标准费率**，不同规模/用途报价可能差异很大。对个人开发者的免费小众 App 而言，这个量级的年费大概率不划算，除非你计划走商业化路线。

### 3.3 替代方案排查结果

| 候选模型 | 架构 | 许可证 | 结论 |
|---|---|---|---|
| PP-DocLayout 系列（百度） | PicoDet / RT-DETR | **Apache-2.0** | 可用，见第 1 节 |
| DocLayout-YOLO（opendatalab） | 改进版 YOLOv10 | **AGPL-3.0** | 不可用——仓库 [LICENSE 文件](https://github.com/opendatalab/DocLayout-YOLO/blob/main/LICENSE) 明确写 AGPL-3.0（[github.com/opendatalab/DocLayout-YOLO/issues/110](https://github.com/opendatalab/DocLayout-YOLO/issues/110)，2026-09-04 核实，有社区成员质疑与 Hugging Face 页面标注的 Apache-2.0 不一致，维护者未回应，**不能采信 HF 页面的 Apache 标注**，以仓库自己的 LICENSE 文件为准） |

排查范围内没有找到"精度接近 YOLOv12x 顶配、且许可证是 Apache/MIT 等可商用友好协议"的选项。这不是排查不够仔细导致的，而是文档版面检测这个细分领域里，**目前公开可用的高精度模型大量基于 Ultralytics YOLO 系架构**，继承了 AGPL 传染性；真正的 Apache-2.0 阵营（百度系）体量普遍轻量化，是为了服务器端批量处理场景优化的，不是为了追顶配精度。

### 3.4 运行时依赖

`onnxruntime-android`：MIT 许可证，aar 体积约 21–26MB（[Maven Central](https://central.sonatype.com/artifact/com.microsoft.onnxruntime/onnxruntime-android)，2026-09-04 核实版本 1.19.2 为 26.5MB）。无许可证冲突。

## 4. APK 体积预算核算（上限 150MB）

当前 v0.2.0 debug APK 实测 **14.3MB**（`app/build/outputs/apk/debug/app-debug.apk`，2026-09-03 构建）。

| 方案 | 新增体积 | 预算内总计 | 是否超预算 |
|---|---|---|---|
| PP-DocLayout-S + onnxruntime | 4.7 + 26 ≈ 31MB | ≈ 45MB | 否，余量大 |
| PP-DocLayout-M + onnxruntime | 22.4 + 26 ≈ 48MB | ≈ 63MB | 否，余量大 |
| PP-DocLayout-L + onnxruntime | 123.4 + 26 ≈ 149MB | ≈ 164MB | **是，超出约 14MB** |

release APK 会比 debug 小（去混淆/资源压缩），上表按 debug 口径算是保守估计，实际 release 可能有一些余量，但 L 档本身单模型就吃掉预算的 82%，风险很高，不建议作为首选。

## 5. 目标机型核算

小米 23013RK75C（mondrian），高通 SM8475（骁龙 8+ Gen 1），Adreno GPU（`adb shell getprop` 实测，2026-09-04）。这个档位的 SoC 跑 S/M 档 ONNX 模型（几十 MB 级、单张页面推理）在 CPU 或 NNAPI 上做同步推理，量级上不构成瓶颈（**训练记忆推断，未做真机推理耗时实测**）——建议选定路线后第一步做的验证之一就是真机测单页推理耗时，确认不会让翻页产生明显卡顿。

---

## 6. 候选路线（互斥，供选择）

### 路线 A：增量改进现有几何规则引擎（不引入任何 CV 模型）

- **解决的局限**：可以逐条针对性攻克——比如给"纯空白对齐表格"加行列间距统计检测、给"表格与正文混排"做局部裁剪而非整页栅格化判断
- **许可证**：零风险，无新依赖
- **新增体积**：0
- **工作量**：中低，延续现有 TDD 节奏，每条局限是独立小增量
- **失败风险**：低——方法论已经用 60+ 条历史修复验证过，团队（就是你和我）对这套代码路径最熟悉
- **天花板**：几何规则处理不了需要"理解版式语义"的场景（例如两个并排独立表格 vs 一个大表格，几何特征可能相同），这个天花板短期内摸不到，但长期存在
- **回退**：本身就是现状，不需要回退

### 路线 B：引入 PP-DocLayout-M（Apache-2.0）做局部区域裁剪，与现有规则共存

- **解决的局限**：直接命中当前最大的已知局限——"表格与正文混排时正文也变图"。CV 模型输出的是多个独立 bbox（"这块是表格""这块是正文段落"），理论上可以只把模型标出的表格/图片区域局部栅格化，其余正文继续走现有重排逻辑，这是现有纯几何规则做不到的能力（几何规则目前只能做"整页是/否"的二元判断）
- **许可证**：Apache-2.0，第 3.3 节确认无冲突
- **新增体积**：约 48MB，预算内有大量余量
- **工作量**：中高——这是本项目第一次引入 ONNX/native 推理管线，是全新技术面（不是延续现有 Kotlin 代码风格的增量）；还需要把模型输出的 bbox 接入现有段落/图片归类逻辑，不能简单粗暴替换
- **失败风险**：中——8 月 POC 的"2 差 1 好 1 平"验证的是"整页判断准不准"，这次要验证的是"局部区域裁剪准不准"，是不同的成功标准，旧数据只能部分参考，需要针对新的成功标准重新测
- **回退**：模型置信度低于阈值时可以原样退回现有几何规则，两套是叠加关系不是替换关系，回退成本低
- **范围边界**：只做版面区域检测，不引入 OCR（本项目文档语料已有真实文字层，见第 2 节），不移植"重排合成引擎"（现有 `reflow()` 已经承担这个角色）

### 路线 C：维持 2026-08-29 的正式否决结论不变

- **解决的局限**：不解决，止损
- **许可证/体积/工作量/风险**：均为 0（现状已完成）
- **适用场景**：如果你判断三张已知局限页的实际阅读体验影响不大，不值得为此引入新技术面

---

## 7. 推荐

**推荐路线 A 作为下一步，路线 B 作为独立的、范围收紧过的探索性尝试，而非小白 PDF 那套完整架构的移植。**

理由：
1. 路线 A 风险最低、回报明确，且不排斥未来叠加路线 B——两者不互斥于时间线，可以先做 A 再评估要不要做 B。
2. 路线 B 如果做，必须显式收窄到"只加版面检测模型，复用现有抽取+重排代码"，绝不是移植小白 PDF 的四层管线——原否决理由里的"管线复杂度"和"模型体量"，只有在收窄范围之后才真正被化解到可接受的程度。
3. 路线 B 的许可证结论明确：**只能用 Apache-2.0 的 PP-DocLayout 家族，不能用任何 Ultralytics YOLO 系或 DocLayout-YOLO（AGPL-3.0）**——这条是硬约束，不因为"想要更好效果"而松动，除非你决定购买 Ultralytics 商业授权（成本量级需要你直接联系对方报价，本文档给出的 $5,000/年只是个案参考）。
4. 不建议直接选 L 档模型追求更接近顶配的精度——它会吃掉 150MB 预算的绝大部分，且精度是否真的够用完全没有验证过。

**不建议的选项**：完整移植小白 PDF 四层架构（含 OCR + 独立页面分类 + 新重排引擎）——于本项目语料而言大概率是过度设计，且当前唯一可用的许可证合规版面检测模型精度还没有被证明能超过现有几何规则。

---

## 待你决定

1. 选路线 A / B / C（或 A+B 组合，先做 A）
2. 若含路线 B：是否接受"这是一次范围收紧过的探索，可能验证下来仍然不如现有规则"这个前提，而不是预期它一定能达到小白 PDF 的效果（模型档位差距摆在那里）

---

## 8. 路线 B 可行性探针结果（2026-09-04，已完成，用户已选定路线 B）

用 `sample-with-table.pdf`（既有 fixture，"文字段落→3列4行表格→文字段落"，正好对应
路线 B 要解决的核心局限）在真机（小米 23013RK75C / 骁龙 8+ Gen 1）跑了一次最小可行性
探针，代码见 `app/src/androidTest/java/app/pdfreader/extract/DocLayoutOnnxProbeTest.kt`。

**耗时**：
- session 创建（一次性，应用/文档加载时）：约 100ms
- 单页推理稳态耗时：约 305ms

**精度（官方 `inference.yml` 推荐阈值 `draw_threshold=0.5`）**：

| 类别 | 置信度 | bbox（原图像素坐标） | 判读 |
|---|---|---|---|
| table | **94.3%** | [191,351,1626,678] | 精确框住表格，这正是路线 B 相对现有几何规则的核心优势——不用整页二选一判断，能把表格单独裁出来 |
| text（页首段落） | 50.9% | [343,212,974,293] | 压线过关 |
| text（页尾段落） | 38.0% | [298,730,929,815] | **未过官方阈值**，是一次漏检 |

其余所有类别（chart/image/footer 等）全部低于 15%，没有出现高置信度误判。

**结论**：单页推理耗时和表格识别精度都支持继续往下投入正式实现；页尾段落漏检提示实际接入时不能只依赖"过阈值就采纳"的简单策略，需要设计降级路径（比如"该区域没有任何类别过阈值时，交给现有几何规则兜底判断"，而不是"模型没给出结果就当作无内容"）。**耗时这一项还没有触达真正的风险点**——303ms 是探针脚本每次都新建 `OrtSession` 的极限单次测量，真实应用里 session 会常驻复用，但 4232 页的大文档如果对每一页都做一次推理，即使摊薄了 session 创建开销，累计仍然是分钟级——正式实现阶段第一件事应该是设计"什么时候触发 CV 检测"的策略（比如只在现有几何规则本身犹豫不决的页面上触发，不是无条件跑全书），而不是假设"单页 305ms 挺快的，全跑一遍就行"。

**下一步**：正式进入 TDD，把这里验证过的预处理/后处理逻辑（640×640 直接缩放、ImageNet 归一化、`draw_threshold=0.5` 过滤）抽成正式的 `LayoutDetector` 类，设计触发策略与降级路径，接入 `extractContent`。
