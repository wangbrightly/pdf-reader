# 踩坑记录（供以后回来读这个项目时先看）

## 1. Gradle 测试要用 JDK 21，不是文档里"验证过"的 JDK 17

`~/Desktop/核实与改写档案/小米PDF阅读器App-提示词-v1.md` 里写的"已验证工具链"是 JDK 17.0.20（复用自 weekly-surprise 项目），但那只覆盖了**空壳 App 编译**。这个项目接了 Robolectric 4.16.1（跑 `PdfTextExtractorTest` 需要，见下），Robolectric 要支持 compileSdk 36 就要求跑测试的 JVM ≥ 21，JDK 17 会直接报错：

```
Failed to create a Robolectric sandbox: Android SDK 36 requires Java 21 (have Java 17)
```

**用法**：跑 `gradle test` 一律用 JDK 21：

```bash
export JAVA_HOME=/usr/local/opt/openjdk@21
gradle test
```

（本机 `openjdk@21` 已经装好，keg-only，不在 PATH 里，必须显式 `JAVA_HOME`。）

`compileSdk`/`minSdk` 等 Android 侧配置维持 36/26 不变，这条只影响"用哪个 JVM 跑 Gradle"。

## 2. 千万别用机器默认的 `openjdk`（当前是 JDK 26）跑这个项目

机器上 `/usr/local/opt/openjdk` 这个符号链接指向的是 JDK 26.0.2（很新的版本），不是文档里验证过的 17。**JDK 26 在这台机器上会导致 Gradle 下载依赖时反复"TLS 握手失败"**（`Remote host terminated the handshake`），无论走代理还是走阿里云镜像都一样失败——换成 JDK 17 或 21 后同样的下载秒级完成。没深挖 JDK 26 具体是哪个 TLS 参数不兼容，只确认了"换 JDK 就好"，如果以后其他项目也遇到类似的下载失败，先检查 `JAVA_HOME` 有没有不小心指到 26。

## 3. `settings.gradle.kts` 里加了阿里云镜像

Robolectric 的 `nativeruntime-dist-compat` 依赖包约 159MB，走代理连 Maven Central 只有约 10KB/s（要 4 小时+），改成不走代理直连阿里云镜像后约 9MB/s（17 秒）。`~/.gradle/gradle.properties` 里已经把 `*.aliyun.com` 排除在代理之外，所以只要仓库列表里有阿里云镜像，Gradle 会自动挑更快的那个，不需要额外配置。

## 4. PdfBox-Android 有个已知的中文提取 bug，已经修了

**现象**：某些常见汉字（十、一、二、人、大、文、工、日……还有"长门马车页"这几个）会被提取成形状几乎一样但编码不同的"部首"字符（比如"十"被提出来变成"⼗"），肉眼一晃很难发现，但逐字比对/搜索/复制粘贴会出问题。

**用真实中文 PDF 实测确认过**（不是猜的）：在 `~/Desktop/核实与改写档案/中县干部-十全十美清单.pdf` 上复现，query 过 PDFBox 社区的已知 issue（[PDFBOX-55](https://issues.apache.org/jira/browse/PDFBOX-55)、[sourceforge #72](https://sourceforge.net/p/pdfbox/bugs/72/)）——是"字体的 CID→Unicode 映射有歧义时，PDFBox 会挑中部首而不是汉字本字"这个已知问题类别，poppler/pdftotext 在同一份文件上没这个问题。

**修法**：`PdfTextExtractor.fixRadicalVariants()` 逐字符检查——落在康熙部首区间（U+2F00–U+2FDF）的用 `Normalizer.normalize(_, NFKC)` 修（214 个康熙部首里 Unicode 官方就有兼容分解映射，直接能用）；另外 19 个"CJK 部首补充"区（U+2E80–U+2EFF）里本身是常用独立汉字的（车/长/门/见/贝/韦/页/风/飞/马/鱼/鸟/卤/麦/黄/齐/齿/龙/龟），Unicode 没给兼容映射，手写了一张表。纯部首用字（钅饣纟讠辶等，从不单独成字）没收——这类字形不会在正常文本里单独出现，收了也用不上。

**踩过的坑**：一开始图省事对整句话做 NFKC，顺手把中文全角标点（，？！（）：）降级成了英文半角标点——NFKC 的兼容折叠范围比想象中大，Fullwidth Forms 区块也在里面。改成只对部首区间的单字符做 NFKC，标点原样保留。

## 5. 测试用的中文 PDF fixture 别用 macOS 自带工具生成

最早用 `textutil`/`cupsfilter` 生成小测试 PDF，两者背后都是同一套 macOS 系统打印管线，会给某些汉字（恰好是上面第 4 条里出问题的那批字）逐字单独起一个文字对象，PdfBox 的分段启发式应付不了，断行断得很怪，且用 `pdftotext -raw` 交叉核对也是乱的——是 fixture 生成方式本身的问题，不是抽取层的锅。

改用本机已装的 Puppeteer（`~/.claude-tools/webshot`）把一段 HTML 打印成 PDF，文字对象按正常语句连续排布，`pdftotext -raw` 核对正常。现在 `app/src/test/resources/sample-chinese.pdf` 就是这样生成的。这也顺带更贴近真实场景——"网页另存为 PDF"是用户真会遇到的常见 PDF 来源。

## 6. Chromium 打印表格边框，画的是"细长填充矩形"，不是描边直线

做表格检测（2026-08-18 增量）时反编译了 `sample-with-table.pdf` 的 content stream 才发现：Chromium 打印 `<table border>` 的单元格边框，用的是 `re`（画矩形）+ `f`（填充）——每条边框是一个宽或高只有 1pt 的细长矩形，不是常见印象里"用 `m l S` 描一条线"。如果表格检测只处理 `strokePath`（对应 `S`），会完全漏检 Chromium 打印出来的表格。`TableGridDetector`/`PdfTextExtractor.TableGridStreamEngine` 因此同时处理 `appendRectangle`（矩形四条边）和 `fillPath`（不只是 `strokePath`），并且用"线段长度"过滤掉矩形的"厚度"那条短边，只留长边参与网格判断。以后如果还要基于矢量图形做检测，先反编译几个真实来源（浏览器打印/Office 导出/LaTeX）的 content stream 看它们实际画法，不要凭 PDF 规范的"教科书画法"猜。

## 7. PdfBox-Android 的 `PDFGraphicsStreamEngine` 和 `PDFRenderer` 在 Robolectric 下能正常跑，不需要额外配置

`com.tom_roush.pdfbox.contentstream.PDFGraphicsStreamEngine`（自定义图形流引擎，用来拿矢量线段坐标）和 `com.tom_roush.pdfbox.rendering.PDFRenderer`（整页渲染成 `Bitmap`）在现有 Robolectric 4.16.1 环境下直接能跑，用真实含表格的 fixture 验证过（A4 页面 @150 DPI 渲染出 1241×1753 px 的 `Bitmap`，和"210×297mm×150dpi"的理论值吻合），没有触发"Robolectric 默认不支持真实图形栅格化"这类担心——不需要额外开启 `NativeGraphicsMode`/`LegacyGraphicsMode` 之类的配置，跟已有 `PDImageXObject.getImage()`（[4] 节图片抽取用到的 API）同样开箱即用。以后如果要在这个项目里做更多"依赖 PDFBox 图形渲染"的功能，可以直接假设 Robolectric 环境支持，不用先怀疑环境限制。

## 8. `gradle assembleDebug` 成功不等于 App 能跑起来——真机装上直接闪退过一次

2026-08-18 装真机（小米，代号 mondrian）第一次打开就闪退。原因：`MainActivity` 继承
`AppCompatActivity`，AppCompat 的 `setContentView` 硬性要求应用主题是
`Theme.AppCompat`（或子类），但项目从建骨架起就没声明过任何主题，`AndroidManifest.xml`
里也没写 `android:theme`，用的是系统默认主题——`AppCompatDelegateImpl.createSubDecor`
直接抛 `IllegalStateException`。

**为什么一路 TDD 都没抓到**：写过的测试全是纯逻辑（抽取/重排/设置/进度这些），没有一个
测试真正跑过 `MainActivity.onCreate()` 里 `setContentView` 这条路径——`assembleDebug`
只验证代码能编译打包，不验证运行时不崩。UI 增量的边界写的是"UI 代码不强求自动化测试
覆盖，跑一次真实编译验证即可"，这个尺度对纯逻辑够用，但对"Activity 起不起得来"这类
运行时才暴露的问题不够，**光编译成功不能当作"App 能用"的证据，得真机/模拟器实际跑一次
才算数**。

修法：加 `res/values/themes.xml` 定义 `Theme.PdfReader`（继承
`Theme.AppCompat.DayNight.NoActionBar`），`AndroidManifest.xml` 的 `<application>`
标签加 `android:theme="@style/Theme.PdfReader"`。

## 9. 用 adb 装包到小米手机，"USB 安装"这个开发者选项开关容易漏开

第一次 `adb install` 报 `INSTALL_FAILED_USER_RESTRICTED: Install canceled by user`——
不是安装包或代码问题，是 MIUI/HyperOS 在"开发者选项"里单独放了一个"USB 安装"开关
（跟"USB 调试"是两个不同开关），没开会拦下所有非应用商店来源的 adb 安装。开发者选项里
手动打开后重新 `adb install -r` 就通过了。

## 10. 待办（暂缓）：扫描版 PDF 的文字重排——需要 OCR，用户 2026-08-18 决定先不做

扫描 PDF 本质是"页面照片"，没有真正的文字层，`PdfTextExtractor` 抽不出任何文字，
所以现在这类页面没法重排/调字号——这是预期行为，不是 bug。要支持的话需要接入 OCR
（光学字符识别），技术路线已经调研过：

- **可行方案**：Google ML Kit 文字识别（`com.google.mlkit:text-recognition` +
  `text-recognition-chinese`），离线运行、免费、不用联网，CJK 识别效果不错。
- **前提已确认**：这台测试机上其实**有** Google Play 服务（`adb shell pm list
  packages | grep google` 能看到 `com.google.android.gms`/`com.google.android.gsf`，
  包名里还有个"谷歌安装器"，像是手动装上的）——ML Kit 依赖 GMS，这台机器上能跑。
  但不能想当然认为"小米手机都有 GMS"，换一台机器要重新核实这个前提。
- **代价**：APK 体积会明显变大（ML Kit 识别包十几 MB 级别）；扫描页要先转图片再逐页
  识别，比现在的纯文字抽取慢不少；识别质量有上限，不保证 100% 准确，需要保留"认不出
  就回退成看图片"的降级路径，不能因为识别失败就崩溃或整份文档失败。

用户 2026-08-18 决定"留着以后做"，不是否决，是排期问题——以后要捡起来做的话，先重新
确认目标机型有没有 GMS，再决定还是走 ML Kit 还是换成不依赖 GMS 的 Tesseract 方案
（体积更小依赖更少，但识别准确率通常更弱，尤其 CJK）。

## 11. 内嵌图片朝向修正：纸面推导的符号推错了，"独立验证"的测试其实也不独立

**现象**：真机反馈一本扫描书全书图片上下+左右都反了。第一次修复（018b673）时用纸面
推导 CTM 数学，假定"不翻转"的基准 CTM 是 `a>0、d<0`；装机验证后依然是反的。

**关键教训：测试里的"独立参照模型"和被测代码用了同一个错误假设，所以互相"验证通过"，
却验证的是同一个错误**。`PdfTextExtractorImageOrientationTest.referenceLayout()` 当
时直接把 Bitmap 左上角映射成图片空间坐标 `(0,0)`——这其实是把"Bitmap 顶部=图片空间
原点"当成了默认前提，跟生产代码里那次推错的"基准"假设是同一个坑，不是两套独立逻辑。
两边"数学推导一致"这种验证方式，遇到"两边共享同一个错误假设"时是失效的，不会自己
报错，只会在真机上暴露。

**这次怎么验证的**：不再靠推导，改用真正独立于 PDFBox/本项目代码的第三方渲染器
（poppler `pdftoppm`）：Python 手写最小 PDF（4×4 像素红/绿/蓝/黄四象限测试图 + 明确
指定的 CTM 矩阵），poppler 渲染出 PNG，PIL 逐像素采样具体颜色，跟代码算出来的结果
直接比对坐标和颜色，不比对"两套数学推导是否一致"：

- CTM `a>0,d>0` → ground truth 证明**不需要修正**（旧代码判断成"需要垂直翻转"，错）。
- CTM `a>0,d<0` → ground truth 证明**需要垂直翻转**（跟旧假设的方向正好相反）。
- CTM `a=0,d=0`（90°旋转分支）→ ground truth 证明 `m01` 应为 `-sign(c)`，旧代码写
  成 `sign(c)`，符号反了；`m10=-sign(b)` 是对的，没改。

**修法**：`orientationMatrixOrNull()` 改了两处符号（`m11`、`m01`），测试文件的
`referenceLayout` 图片空间坐标映射也一并改成跟 PDF 规范一致（Bitmap 顶部对应图片
空间 `y=1` 而不是 `y=0`），`ctmCases` 里被搞反的标签也改了。装机验证：用户确认改完
后全书方向问题基本解决，库里另有一本书仍有方向问题，但用户判断是那本书本身排版有
问题，不是这次要覆盖的场景，先不追。

**往后再改这类朝向/坐标问题，别再纯靠纸面推导，也别造一个"独立模型"就假设它真的独立
——用第三方成熟工具（不同代码库、不同作者）产出的结果当地面真相，才是真的独立验证。**

## 12. 已修：配置变化（转屏等）会导致 Activity 重建，当前打开的文档整个丢失

**现象**（code-review 2026-08-19 发现，不是真机反馈）：`MainActivity` 没有
`android:configChanges`，也没有 `onSaveInstanceState`/`ViewModel` 之类的状态保存
机制——只要发生配置变化（最常见是转屏，也可能是系统字体大小变化等），Activity 会
被完整销毁重建，`currentContent`（已经抽取好的文字/图片/大纲）、当前滚动位置、
当前打开的文件全部丢失，回到"还没打开任何文档"的初始画面。

**修法**（commit `6026899`）：`onSaveInstanceState` 里只存一个 `currentUri`
（`Uri` 是 `Parcelable`，体积小，不受 `Bundle` 大小限制），`onCreate` 检测到
`savedInstanceState` 非空时用这个 `Uri` 重新走一遍 `loadPdf()`——相当于自动帮
用户重新打开刚才那份文件，会比真正的状态保留慢（要重新抽取），但实现简单；滚动
位置不用额外处理，`onPause` 在销毁前已经存过一次阅读进度，重新 `loadPdf` 走的是
和"手动重新打开"完全相同的路径，自动读回那次存的位置。

**真机验证**：adb 关闭自动旋转、强制转横屏，截图确认文档没有消失、还停留在转屏前
的大致位置；logcat 确认 `extractContent` 确实被重新调用了一次（不是系统侥幸保留
了视图状态），转回竖屏正常。这处改动涉及 Activity 生命周期，Robolectric 测不了，
用这种方式验证，没有另外写自动化测试。

## 13. 已修（部分）：大图加载慢——反编译 PdfBox-Android 找到根因，JPEG 走安卓原生解码

**现象**：一份 258 页、图片较多的文档打开要 25 秒，真机日志确认几乎全部耗时在图片
解码——`PDImage.getImage()` 默认按源图片原始分辨率整张解码，源图片是几千像素见方
的扫描页/照片时，解码成本跟屏幕实际显示尺寸完全不成比例。

**前两次尝试都失败了**（教训见下）：
1. 用 `PDImage.getImage(Rect, Int)` 降采样重载：真机实测 `subsampling>1` 时稳定
   抛 `y + height must be <= bitmap.height()`，全部图片解码失败、静默丢图，日志
   显示的"快了 16 倍"其实是图片全没了。
2. 解码完整图后 `Bitmap.createScaledBitmap` 事后缩小：图片都在了，但真机实测总
   耗时反而变成 36.3 秒，比原来还慢——真正的开销大头（全分辨率解码本身）没有省
   掉，事后缩小是纯粹叠加的额外成本。

**根因**（反编译 PdfBox-Android 2.0.27.0 字节码找到的，不是猜的）：
`getImage()`/`getImage(Rect,Int)` 最终都调用同一个内部方法
`SampledImageReader.getRGBImage(...)`，只是传的 `subsampling` 参数不同——尝试 1
踩到的异常是这个内部方法本身处理 `subsampling>1` 的 bug，不管怎么调用外层 API 都
绕不开。

**第三次尝试成功**：完全绕开 PdfBox-Android 的图片解码，只对 JPEG（`getSuffix()
=="jpg"`，反编译确认内容流最后一层过滤器是 `DCTDecode` 时才返回这个值）生效——用
`PDImage.createInputStream(listOf("DCTDecode","DCT"))` 拿到停在 DCT 解码之前的
原始 JPEG 字节（反编译确认这个重载的语义就是"遇到列表里的过滤器名字就停手"），交给
安卓自带的 `BitmapFactory.decodeByteArray` + `Options.inSampleSize` 解码——平台
自带、久经考验，跟 `SampledImageReader` 不共享任何代码，不会被同一个 bug 影响。
任何一步失败都回退到 `pdImage.image`，不会丢图。

**真机验证**（同一份 258 页文档）：25.4s → 16.2s（提速约 36%），0 张图片解码失败，
截图确认多张扫描地图/人物照片正常清晰显示。

**"部分"修好的意思**：只覆盖 JPEG 编码的图片，PNG/TIFF/JBIG2 等其它格式仍然走原来
较慢但可靠的 `pdImage.image` 路径——JPEG 是真实场景里最常见的大图来源（扫描页/
照片），但不是唯一来源，遇到非 JPEG 大图较多的文档，这次的提速覆盖不到。

**这次投入的调查方法值得记录**：前两次都是"看起来解决了"就装机验证，第三次先反编译
库的字节码找到真正的根因和正确的 API 用法，再动手实现——同一个问题反复失败之后，
停下来往底层查一层，往往比继续在上层试各种参数组合更快找到真正能用的方案。

## 14. 已修（2026-08-25，见 #37）：紧凑列表（比如目录/大纲列表）会被段落合并逻辑粘成一大段，丢失换行

**现象**（表格误判修复后真机暴露，不是这次改动引入的新问题）：`linesToParagraphs`
按"相邻行的 y 间距是不是明显大于同文档典型行距"判断"是不是同一段"——这个启发式
分不清"一段话自然换行"和"一份紧凑的列表，每一项各自成行"，因为两者的行间距量级
往往差不多。真机复现：一篇文章开头的目录列表（11 个条目，每项本该单独一行）被
合并成了一整段连续文字，读起来顺序很乱。

**为什么现在才暴露**：这块内容之前被表格检测误判成图片整块显示，图片保留了原始
排版（看不出这个问题）；表格误判修好之后走正常文字抽取路径，这个一直存在的分段
局限才第一次被真机看见。

**以后要修的话**：思路方向——如果能拿到每一行文字的 x 坐标（起始位置），"这一行
和上一行的起始 x 明显不同"或者"这一行本身很短、不到页面宽度的一半就换行了"都是
"这可能是列表项而不是自然换行"的信号，比单纯看 y 间距更可靠；`Line` 目前只记录
了 `y`，没有记录 `x`，需要先补上这个字段。这次没有作为紧急问题处理，用户当时的
优先级是先做加载速度架构调整。

**2026-08-19 补记：按上面思路做过一次尝试，是错的，别直接照抄**——加了 `x`/
`endX` 字段和"x 差异大/行宽明显短于中位数就切段"的判断，跑单元测试直接炸了 9 个：
`This is page 1 body` 这种正常段落被错误切成 `This is page` + `1 body` 两段，
`This is page` 又因为在多页上逐字重复，被 `RunningFooterFilter` 当成页眉页脚水印
整段删掉，最终只剩 `1 body`。问题不在"要不要看 x 坐标"这个方向，而是阈值/触发
条件太粗糙——固定 `8pt` 的 x 容差、"行宽 < 中位数一半"这两个条件对正常段落（尤其
是段落最后一行天然更短）命中率太高。这次没有把这段代码修好提交，是在另一个任务
（按需加载架构）验证阶段跑测试才发现被这段没做完的代码拖累而顺手撤回的。**真要
接这个坑，先补一批"临表格末行更短""段落最后一行明显更短"的负样本 fixture，把
阈值往保守调，而不是直接调宽容差数字。**

## 15. 已修：图片/表格"按需加载"架构落地，真机发现并修了一个"假装分批、实际整批"的 bug

**背景**：用户反馈打开速度明显慢于 WPS，真机日志确认耗时大头集中在图片解码/
表格区域渲染，文字抽取本身一直很快（[PdfTextExtractor.Session] 类注释有完整背景）。
方案：文字部分照旧一次性快速抽完立刻显示，图片/表格区域改成先摆占位符、后台按
页逐个解码，解码完一个就把对应占位符换成真图片，不用等全部处理完才能开始阅读。

**踩的坑**：第一版 `loadPendingMediaInBackground` 是按 `PdfTextExtractor.Session
.pendingMediaPageByAfterIndex` 的 key（"插入位置"）分组处理——一个 key 对应的
`List<页码>` 内部用 `flatMap` 一次性加载完才更新一次 UI。这个设计隐含一个没
验证过的假设："同一个插入位置只会对应少数几页"。真机测试一本 258 页、以图版为主
的书（敦煌星图/古典地图）时打脸：这本书完全没有可提取的文字层（`Session` 诊断
日志打出"原始段落数=0"），258 张图因此全部落在同一个插入位置（"所有段落之前"），
`flatMap` 把这 258 页当一个整体处理，变成"等 258 页图片全部解码完（真机实测
20.4 秒）才第一次更新画面"——跟设计目标"文字秒开、图片逐步加载"完全对不上，用户
反馈"这一版感觉上并没有提速"正是这个 bug，而不是"按需加载"这个架构方向本身有问题。

**修法**：`loadPendingMediaInBackground` 改成直接展开成"页码"这个更细的粒度顺序
处理，不管这一页的插入位置是否跟别的页共享同一个 `afterParagraphIndex`，加载完
这一页就立刻更新一次 UI。`resolvedMedia`/`placeholderViewsByPage`/
`DisplayBlock.Placeholder` 都同步从"按插入位置索引"改成"按页码索引"（页码在一份
文档内天然唯一，改起来很直接）。修完真机复测同一本书：每页约 50-70ms 加载完就
立刻替换占位符，258 页总耗时不变（约 15.6 秒），但从"一次性阻塞"变成"渐进式"，
用户反馈"加载很快，体验很好"。

**教训**：`pendingMediaPageByAfterIndex` 这个数据结构名字和类型（`Map<Int,
List<Int>>`）已经明明白白写着"一个位置可能对应多页"，第一版实现却在性能敏感的
加载循环里把这个 `List` 当成一个不可再分的整体处理——数据结构暴露的可能性和代码
实际处理的粒度不一致，是这次 bug 的根源，不是什么隐蔽的边界情况。

## 16. 已修：无边框表格/表单文字顺序错乱——根因不是"没检测到表格"，是 `PDFTextStripper` 没按视觉位置排序

**现象**：真机测试两份真实文档（一份体检报告、一份技术规格表）都发现文字顺序
错乱——体检报告"年龄: 43岁"被拆成"43 岁年龄:"，标签和值位置颠倒；技术规格表
同一行的好几个数值被打乱顺序拼在一起（"−24 −18.5 −20 dB"应该分行分列展示）。
表面上看跟"表格没检测到"（[TableGridDetector] 只认矢量网格线，两份文档的这几
处表格/表单都没有画线）很像，一开始也确实往这个方向去分析，甚至跟用户讨论过要
不要专门造一套"基于文字间距识别无边框表格"的新检测逻辑（工作量评估：中等偏大，
还有误判正常留白排版的风险）。

**真正的根因**：`PDFTextStripper` 默认按 PDF content stream 里的绘制顺序输出
文字，不是按肉眼看到的从上到下、从左到右的视觉顺序。单栏正文通常两者一致（大部分
PDF 生成器本来就按视觉顺序画），但"表单/表格模板"这类常见生成方式——先整体画
一批标签、再单独一批填值，或者分栏绘制——会导致行内乃至跨行的文字顺序错乱，跟
"有没有画表格线"完全是两回事，纯粹是文字提取顺序的问题。

**修法**：`LineCollectingStripper`（[PdfTextExtractor] 内部同时供 `extractContent`
和 `Session` 复用的文字提取器）的 `init` 里加一行 `sortByPosition = true`——这是
PDFBox 官方就为这类问题准备的标准选项，不是自己发明的算法。真机复测两份文档，
错乱全部消失，恢复正常的"标签在前、值在后"顺序。

**教训（本条最值得记的部分）**：真机看到的表面现象（"表格样式的内容顺序不对"）
和"表格没被检测到"这两件事看起来高度相关，实际是两条完全独立的因果链——一个是
"内容该不该整页/整块降级成图片"（[TableGridDetector] 的职责），一个是"文字提取
出来的顺序对不对"（`PDFTextStripper` 排序方式的职责）。差点因为表面相关就去投入
一套复杂度高得多、还有新误判风险的检测逻辑，而真正的修法是一行配置。**看到"像
表格"的错乱现象时，先确认问题出在"检测"还是"提取顺序"，别急着往复杂的方向想。**

**已知的权衡（如实记录，还没有验证具体影响多大）**：PDFBox 官方文档提示
`sortByPosition` 有性能代价。真机测试一份 19 页、含矢量图表的文档时
`Session.init` 抽取文字阶段耗时 2185ms，看起来不低，但同一份文档"扫描页面
（不解码图片）"阶段也要 2731ms（这一步跟 `sortByPosition` 无关，是另一个还没
深入调查的疑点，见类注释"待办"标记），没有排除法拿到"开这个开关之前 vs 之后"的
直接对比数据，不确定这 2185ms 里有多少是 `sortByPosition` 新增的成本。目前判断
"正确性优先于这点耗时"，两份实测文档主观体感依然是"很快"，暂不单独立项调查，
如果以后有文档因为这个开关明显变慢，再回来看这条。

## 17. 已撤回：表格检测的"行列间距均匀性检查"（#16 修完顺带真机测出的新漏检）

**发现过程**：#16 的 `sortByPosition` 修复上线后，用户反馈"表格变成文本了"——
真机诊断（给 `tableRegionOrNull` 加临时的 `diagnose` 输出）确认：一份技术规格
文档的表格页，横线聚类位置是 `[246, 296, 588, 650, 664, 679]`——246/296 是一对
跟表格无关的线（大概率是标题下的分隔线），588 往后才是真表格的行线，两拨线
夹着一大段空白，整页算出来的"间距是否均匀"自然不均匀，"行列间距均匀性检查"
这条规则（当天早些时候刚加的，见本文件更早的表格相关条目）把真表格也一起挡掉了。

**尝试过的修法（没采用）**：先把线段按位置分组、只在最密集的那一组里检查均匀性，
不要被页面上其它不相关的线拖累。写单元测试验证时发现：这个修法在数学上没法把
"一小撮紧挨着的真表格行线 + 一条不相关的远处线"和"一小撮紧挨着的装饰线 + 一条
不相关的远处线"分开——两者的坐标分布形状完全一样，`TableGridDetectorTest.kt`
里已有一条对应的真机验证过的测试用例，跟这次真实文档的坐标形状是同构的。要真的
分清楚，得引入新的信息（线段长度、离页面边缘的距离……），工作量比预想的大，
而且有真实的回归风险。

**最终决定**：跟用户确认后，直接撤回"行列间距均匀性检查"这条规则（`hasUniform
Spacing`/`MAX_GAP_TO_MEDIAN_RATIO` 整个删掉），回到"只看横竖线数量够不够、
空间是否重叠"这个更早的判断标准。代价是明确的：之前专门为这条规则写的真机
验证用例（页边距装饰边框+零散分隔线）现在会被重新误判成表格，用户明确选择
接受这个回归，换回更多真表格能被正确识别。

真机复测：技术规格文档的"REVISION HISTORY"表格（Version/Date/Notes 三列）
之前是打散的文字，现在正确渲染成带网格线的图片。

**教训**：同一类几何信号（线段位置、间距）在"防表格误判"和"防表格漏检"这两个
方向上是零和的——收紧会漏检真表格，放松会误判装饰线，本类历史上已经在这两个
方向来回调过三次（空间重叠检查→行列间距均匀性检查→撤回）。这类"宁可错杀 vs
宁可漏检"的取舍是产品判断，不是技术判断，遇到新的误判/漏检报告时，先如实汇报
"这次修复方向本身能不能跟历史上加过的某条规则共存"，不要预设"修复一定是安全的
增量"就直接动手——这次差点因为最初的"回归风险比较小"这个错误估计，多做一轮
返工。

## 18. 已修：一页正常图片被显示成好几张变形的窄条——某些扫描 PDF 把整页图切成等宽竖条嵌入

**现象**：用户反馈"一页正常竖版图片被横屏显示成 2-3 张图片"。加诊断日志（打印
每页图片数量+每张的宽高）后，真机实测一份 90 页地图书，很多页并不是一张完整
内嵌图片，而是 6-7 张——比如 `125x1078 ×6 + 19x1078`：前几张宽度完全相同、
高度也完全相同，最后一张明显更窄（宽度上的"整除余数"）。

**根因**：某些扫描/压缩工具会把一整页扫描图切成若干等宽竖条分别嵌入 PDF（可能
是为了绕开单张图片大小/内存限制），PDF 里仍然是"挨个画在正确位置组成一整页"，
肉眼看是完整的一页图。[PageContentStreamEngine] 原来遇到几张图片就存几张，
[MainActivity.createImageView] 对每一张图独立"撑满可用宽度"显示——一条只占
原图一小部分宽度的窄竖条被单独拉伸到全屏宽，效果就是好几张被拉得很扭曲的图片
堆在一起，用户描述成"变成横版了"。

**修法**：新增 [ImageStripStitcher]（纯逻辑，7 个单元测试），识别"至少 3 张、
高度完全相同、除最后一张外宽度也完全相同"这个平铺切片指纹，按原始抽取顺序
横向拼接成一整张图；不满足这个模式的图片列表原样返回，不影响其它文档。接入
[PdfTextExtractor.scanPages]（供 `extractContent` 用）和 [PdfTextExtractor.Session
.loadPageMedia] 的内嵌图片分支（两条路径分别独立解码图片，各自接一遍）。

**已知的未验证假设**：拼接顺序沿用 PDFBox 抽取图片的原始顺序（`Do` 操作符
出现的顺序），假设这个顺序就是从左到右的视觉顺序——没有读取每张图片的 CTM
平移分量来精确定位（[PageContentStreamEngine] 目前只用 CTM 做朝向修正，不
记录位置），如果以后遇到画图顺序跟视觉顺序不一致的反例，需要改成读 CTM 精确
定位，工作量更大，这次没做。

真机复测：90 页地图书的地图页从"好几条拉伸变形的窄图"变成完整的一整张地图，
用户确认"完整了"。拼完之后地图本身仍然是横向长条（地图内容天然如此，不是
bug）——用户反馈"挺好，不耽误看"，就此收尾。

## 19. 已修：某些图片解码出来高度被砍掉约 1/6，画面对角线花屏——根因是位深 4，不是 PNG 本身

**背景**：修完 #18 的图片拼接问题后，用户反馈同一本地图书翻到后面还有花屏（对角线
彩色噪点）。一开始误判成"CMYK JPEG 走原生解码路径花屏"（见 #18 相邻的
`decodeJpegWithNativeSubsampling` KDoc"已修"一节，那次修复本身是对的、有真实
CMYK 场景、有单元测试验证，只是不是这次花屏的原因），加诊断日志在 `drawImage`
里打印每张图片的 `suffix`/原始尺寸/是否走快速路径/解码结果尺寸后才发现真相：

```
解码诊断 suffix=png 原图=983x1441 走快速路径=false 组件数=null 结果尺寸=983x240
```

`suffix=png` 说明这些图片根本不是 JPEG，`走快速路径=false` 说明从头到尾都在用
`pdImage.image`（PdfBox-Android 自己的解码路径），没碰过 `decodeJpegWithNative
Subsampling` 那段代码。真正的问题：原图 983×1441（正常竖版），解码出来变成
983×240——高度被砍到约 1/6（983×1441 这组数据里 1441/240≈6.00，983×1391→
983×232 这组是 1391/232≈6.00，比例非常一致，不是随机的解码失败），画面因此被
压扁+对角线错位，就是用户看到的花屏。

**现状**：根因定位到"PdfBox-Android 解码这份 PNG 时高度被砍到约 1/6"这一步，
还没往下查为什么——候选方向：这份 PNG 会不会是隔行扫描（Adam7 interlaced，
标准是 7 遍，不是 6，但值得先排除）、位深/颜色类型是不是 PdfBox-Android 的 PNG
解码器不常见的组合、或者是这份 PNG 文件本身编码有点不标准（某些 PDF 生成工具
产出的 PNG 不完全遵守规范）。**这次没有继续深入**——用户明确决定先收尾当前
这轮工作（图片拼接、JPEG CMYK、文字排序、加载速度、表格检测都已修完提交），
这个 PNG 解码 bug 留到下一轮专门查。

**后续排查（本轮完成）**：没有反编译字节码，改成直接从 PdfBox-Android 官方
GitHub 仓库拉 `SampledImageReader.java` 源码（`v2.0.27.0` tag，路径
`library/src/main/java/com/tom_roush/pdfbox/pdmodel/graphics/image/
SampledImageReader.java`）——比反编译 `.class` 字节码可靠、直接得多，能看到
完整逻辑和注释，这次踩过的坑（`raw.githubusercontent.com` 的 tag 要带 `v`
前缀、仓库里 Java 源码目录是 `library/` 不是常见的 `pdfbox/`）值得记一笔，
下次查这个库的其它方法可以直接照这个路径规律去找。

源码里 `getRGBImage` 这段是真正的根因：

```java
else if (bitsPerComponent == 8 && colorKey == null && ...) {
    return from8bit(...);   // 正常路径：每个颜色分量正好 1 字节
}
Log.e("PdfBox-Android", "Trying to create other-bit image not supported");
return from8bit(...);       // 兜底：位深不是 8（这次真机数据是 4）也硬当 8 位读
```

`bitsPerComponent` 只有严格等于 1 或 8 时才有专门解码路径，其余位深会打一行
"not supported"日志后，依然照样调用只认"每个颜色分量正好 1 字节"的
`from8bit`——加诊断日志装机复现后拿到的真实数据：`suffix=png bpc=4 分量数=3
原图=983x1441 结果=983x240 宽度比=1.0 高度比=6.0041666`，确认命中的是 4 位
（不是最初怀疑的 16 位或者 Adam7 隔行扫描，两个候选方向都排除了，没有花时间
误入歧途——虽然一开始按 4/8/组件数的比例关系手工反推"为什么恰好是 6 倍"没能
完全对上（PdfBox 内部具体怎么从 `createInputStream` 拿字节流没有继续深挖），
但"4 位数据被 8 位假设读错"这个方向的机制本身已经通过官方源码直接证实，不是
猜的）。

**修法**：这是第三方库本身的缺陷，没法直接改库代码——遇到库明确不支持的位深
（不是 1 也不是 8）就跳过这张图不解码、不显示，不展示错误内容比展示花屏更
诚实，跟本类"宁可漏检"的一贯降级原则一致（见 `PageContentStreamEngine
.drawImage`）。真机复现过、装机验证过（用户后续决定结束这轮排查，未做逐
像素二次确认，如实记录）。

## 20. 已修：图片/表格放大后，往一个方向能拖出空白，往另一个方向拖不到边

**现象**：用户反馈缩放后左右拖动不对称——往左拖显示不全（拖不到图片真正的右
边缘），往右拖却会拖出图片以外的空白。

**根因**：`enablePinchZoom`（[MainActivity]，双指缩放+拖动手势）里，缩放矩阵
用 `postScale(scale, scale)`（不带锚点参数）——锚点默认是 (0,0)，也就是图片
自己像素坐标系里的左上角，不是 View 的屏幕坐标原点。意味着"没有额外拖动"
（`translateX=0`）时，图片左上角死死钉在 View 左上角，放大多出来的宽高全部
朝右下方溢出。但同一个 View 里的 `clampTranslation`（算拖动允许范围的函数）
假设的是另一套模型：`translateX=0` 时图片应该左右对称溢出、整体居中摆放
（`±(scaledWidth-width)/2`）。两处对"没有额外拖动是什么状态"的理解不一致：
往右拖（正 `translateX`）一超过 0 就会露出图片左边缘以外的空白，往左拖（负
`translateX`）却在真正拖到图片右边缘之前就被提前拦住。

**踩的坑**：第一版修法想用 `postScale` 自带的锚点参数 `postScale(scale, scale,
width/2f, height/2f)`，把锚点定在"View 宽高的一半"——这是错的，忘了这个锚点
参数是在**输入坐标系**（这里是图片自己的原始像素坐标）里取值，不是 View 的
屏幕像素坐标。`width`/`height`（View 尺寸，屏幕上也就几百 px）和
`bitmapWidth`/`bitmapHeight`（原图尺寸，扫描图常有几千 px）完全不是同一个
量级，锚点用错坐标系，缩放效果整个错位——真机反馈"放大后完全没法往右/往下
拖动"，比修之前更糟。

**实际修法**：不依赖 `postScale` 的锚点参数，自己算好"让缩放后的图片居中摆放"
所需的基准偏移量（`(width - scaledWidth) / 2f`，和 `clampTranslation` 用的是
同一个公式），再叠加用户实际拖动的偏移量，两步都在 View 屏幕像素这同一套
坐标系里算，不给锚点坐标系混淆留空间。

**教训**：矩阵/坐标变换类的 bug，"看起来像是对的"和"真的是对的"之间的差距
就在于有没有把每一步用的是哪一套坐标系想清楚——`postScale` 的锚点参数字面意思
很直观（"以这个点为中心缩放"），但没意识到它是在输入坐标系里取值，凭直觉套用
"View 的中心" 直接导致了比原 bug 更严重的回归，真机测出来才发现。这类改动
以后要么先手算几个具体坐标点验证一遍，要么用更"笨"但坐标系单一、不易出错的
写法（这次最终选的办法）。

## 21. 已修（见 #22 的架构落地）：4232 页超大文档打开会 OutOfMemoryError，是"文字按需加载"要解决的真实场景

**背景**：用户反馈某份文档打开后一直转圈（真机反馈"12 分钟还没好"）。一开始
诊断方向错了——真机日志显示画面在持续做阻塞式 GC，误判成"矢量线段无限增长"
（跟之前"敦煌星图"那本矢量密集文档扫描慢是同一个机制），加了单页线段数量
上限（[PageContentStreamEngine] 里 `segmentCollectionCapped`，20000 条/页）
先兜底，装机复测问题依旧。

**真正根因**：给 `Session` 构造入口加了立刻打印页数的诊断日志才看清——**这份
文档有 4232 页**。逐页耗时日志显示 `scanPages`（这一步只扫矢量线段+记有没有
图片，不解码图片本身）每页普遍 10-40ms，4232 页累加起来就要 1-2 分钟；这一步
跑完之后进入 `PDFTextStripper` 抽取全文字（当前架构是一次性抽完整份文档），
真机复现两次都在这一步之后不久（`scanPages` 完成后几分钟内）稳定复现
`OutOfMemoryError: ... growth limit 268435456`（256MB 堆上限）——不是单页
内容有多复杂，是"一次性把 4232 页全部处理完才能显示任何内容"这个架构，遇到
页数级别在几千页的文档时，内存和时间都撑不住。

**真机验证过 App 没有崩溃闪退**：两次复现 OOM 后进程都还活着（`ps` 能看到），
说明 `MainActivity.loadPdf` 里 `runCatching { Session.open(...) }` 确实接住了
`OutOfMemoryError`（`Throwable` 兜底，`Error` 子类也在内），`PdfLoadReducer`
把它转成了 `PdfLoadState.Error`——只是错误提示 Toast 是一过性的，没截图截到
那个瞬间，看起来像是"什么都没发生"，不是真的没兜住异常。

**已经加的安全阀（有价值，但没解决这次的问题，继续保留）**：
`segmentCollectionCapped`（[PageContentStreamEngine]，单页线段数上限）——
对"某一页矢量图形极端密集"这种真实存在过的场景仍然有效，只是不是这次 4232
页文档变慢/OOM 的主因，两个问题不冲突，都值得防。

**下一步**：真正的修法是让 `Session` 的"即时可用阶段"不再要求一次性扫完/
抽完全部页面——跟图片那次"按需加载"同一个方向，但这次轮到文字/段落数据结构
（`paragraphs`/`paragraphPages`/`paragraphTopY`）也要跟着改成按页/按需构建，
牵动目录跳转、页脚过滤、阅读进度这几处依赖"全部段落已知"的计算，是这个项目
目前为止最大的一次架构改动，还没开始设计，见对话记录里跟用户的讨论。

## 22. 已修：#21 的架构改动落地（RecyclerView 窗口式加载）+ 上线后一批真机问题

**架构改动本身**：按 `/Users/mac/.claude/plans/fizzy-snuggling-cloud.md` 分四步
落地——`ReadingProgressKey`/`ReadingProgressStore` 从"哈希全部段落/存滚动比例"
改成"哈希文件字节/存页码"；`PdfTextExtractor.Session` 从"打开时一次性抽完全部
段落"改成 `loadPage(pageNo)` 按页加载（页脚水印检测改成"用前 150 页学一次噪音
模式，之后按页应用"，不再需要全文档已知）；`MainActivity` 的 `ScrollView`+
`LinearLayout` 换成 `RecyclerView`+`PdfPageAdapter`（条目粒度=页，`getItemCount
()=session.pageCount` 打开时立刻可知，翻远的页通过一个有界 `LinkedHashMap`
LRU 缓存自动淘汰，不再无限常驻内存）；目录跳转/阅读进度接回新模型。4232 页
那份文档真机复测确认不再 OOM。

**上线后真机陆续暴露的问题（均已修）**：

1. **崩溃**：`PdfPageAdapter` 缓存淘汰时主动调 `Bitmap.recycle()`，但淘汰的
   只是缓存引用，RecyclerView 自己的 ViewHolder 回收池可能还在用同一张图
   渲染，抢先销毁原生内存导致 hwui 渲染线程 `SIGSEGV`，真机复现为"点目录
   跳转后崩溃白屏"（崩溃线程栈顶就是 `PdfPageAdapter$cache$1
   .removeEldestEntry` 调 `Bitmap.recycle`）。改成只丢弃缓存引用，不主动
   `recycle`，交给 GC 自动回收 Bitmap 的原生内存。
2. **目录跳转体验差**：`smoothScrollToPosition` 会带动画连续滚过起点到
   终点之间的每一页，滚过的每一页还会触发按需加载和缓存淘汰，密集触发
   正是上面那次崩溃的诱因。改用 `scrollToPosition` 无动画直接跳转。
3. **段落/图片间视觉留白丢失**：RecyclerView 重写时漏带了旧版写死的
   12dp 间距逻辑。补回来，并且改成设置面板里可拖动的"段距"滑杆（用户
   要求），页内块间距和页间距共用同一个值。
4. **中文标点显得拥挤**：给段落 `TextView` 加固定 0.05em `letterSpacing`，
   对每个字符（含标点）一视同仁地加空隙。
5. **段落错误拆分**：中英文混排的视觉行会被 PDFBox 因字体切换拆成多次
   `writeString` 回调（零间距片段）。改造前"整份文档一次性调用"时这类
   零间距片段占比很小，不影响"正常换行间距"的中位数估计；改成按页调用
   后单页行数骤减，零间距片段占比一高，中位数很容易直接算成 0，导致该页
   所有正常换行全部被误判成"另起一段"（真机复现"可以翻译"|"为，一切
   都……"被拆开）。加一步"同视觉行片段先合并"（`mergeSameLineRuns`），
   避免零间距污染统计基准，并补充了照抄真机日志数据形状的回归测试。

**顺带加的新功能**：标题加粗——字号明显大于本页中位数、或字体本身标了
加粗，两个信号任一命中即判定为标题（用户明确选择"任一命中"而不是"同时
满足"），从抽取层（`Session.loadPage`/`classifyHeadings`）判断，UI 层只
负责按结果切换 `TextView` 字重。

**方法论小结**：这一整条链路（架构改动 + 5 个上线后问题）里，除了第 2
条（体验判断，不需要现场数据）之外，全部靠"加临时诊断日志、装机复现、
看真实数字"定位，没有一次是靠读代码猜出根因的——尤其是第 5 条，一开始
凭直觉怀疑"页边界"，加了日志才发现是"中位数被零间距拖成 0"这种纯统计
学意义上的边界情况，猜不出来。

## 23. 已修（部分）：打开 PDF 太慢——页脚水印学习/大纲抽取改成后台异步，真正的第三个瓶颈（PDDocument.load 本身）留待下次

**背景**：用户要求"一秒之内打开 PDF，后台加载数据"。真机诊断日志逐层拆开
`Session.open` 的耗时，一共揪出三个独立的瓶颈，前两个已修：

1. **页脚水印学习**（已修）：`footerLearnedTitles` 原来是 `Session` 构造函数
   里的同步字段初始化——要在最多 150 页的样本上完整跑一遍文字抽取才能返回，
   真机复现过单这一步 81 秒。改成 `@Volatile var` + 后台线程学习，`open()`
   不再等它，学完前 `loadPage` 用空集合退化成"暂不过滤标题类水印"，学完后
   自动恢复正常。
2. **大纲抽取**（已修）：同样的问题，`outline` 原来也是构造函数里的同步字段，
   真机测过单独占 2.95 秒。同样改成后台异步，`Session.open(context, file,
   onOutlineReady)` 加了一个抽完之后的回调，`MainActivity` 用它在抽完后重新
   算一次目录按钮是否可点（已知代价：文档刚打开、大纲没抽完这段时间目录按钮
   会先短暂显示禁用，抽完立刻恢复）。
3. **`PDDocument.load(file)` 本身**（未修，试过一个思路但没成功）：修完前
   两个之后，大多数文档 `Session.open` 已经降到 100ms-1.5s 量级；但一份
   126MB 的大文档单 `PDDocument.load` 这一步要 6-10 秒。查官方源码确认：
   不带 `MemoryUsageSetting` 参数的 `load(File)` 默认调用
   `MemoryUsageSetting.setupMainMemoryOnly()`——PDF 流内容全部缓冲进 Java
   堆的字节数组，猜测大文件在这台设备 256MB 堆增长上限下压力不小，试了
   `setupTempFileOnly()`（缓冲到临时文件，不计入堆）。**装机实测这个思路
   没有用**：同一份文档改完从 6.6 秒变成 7.9 秒，反而更慢（多了磁盘 I/O，
   没换来预期的堆压力下降收益），已经改回默认设置。如实记录：这个方向已经
   验证过是错的，下次不要再重复尝试同一个思路；`PDDocument.load` 本身的
   耗时目前没找到有效优化手段，可能真正命中的是这份文件结构本身的解析
   开销（候选方向：反编译或查 `COSParser` 相关源码，看是不是命中了某个
   "修复损坏 xref 表"之类的慢速兜底路径），留给以后有需要再查。

**方法论**：跟 #22 一样，全靠加诊断日志分层拆解（先分 拷贝文件/Session.open，
再拆 PDDocument.load/Session构造），每次都是先有真实数字才动手改，没有一步
是凭感觉猜的——包括第 3 点这次"验证了一个思路但没成功"，也是靠装机实测的
真实数字发现的，没有靠猜就直接把改动定下来。

## 24. 已修：MIUI 锁屏清理杀进程后，正在读的文档无声消失——加了"冷启动自动恢复"

**背景**：用户反馈"这本书还有问题"，一开始以为是图片渲染错，实际排查发现文档
整个不见了——App 回到最初的空白首页（只有"打开 PDF"按钮，内容区空白）。

**根因**：`logcat -b events` 抓到确凿证据：
```
am_kill: [0,pid,app.pdfreader,905,stop app.pdfreader due to LockScreenClean,...]
```
小米 MIUI 锁屏后会主动杀掉后台 App 进程，且**连 Recents 任务卡片一起清掉**。
项目里已有的"配置变化（转屏等）重建后恢复文档"机制（`MainActivity` 靠
`onSaveInstanceState` 的 `Bundle`）只在"同一个任务被重新调度到前台"时生效——
任务卡片被清掉之后点桌面图标是彻底的冷启动，`savedInstanceState` 必为
`null`，那条恢复路径根本不会触发。这不是 MIUI 独有的边界情况，是"进程随时
可能被系统回收"这个安卓平台级别的常态，只是 MIUI 更激进、更容易撞见。

**修法**：新增 `LastOpenedFileStore`（`app.pdfreader.progress` 包，单元测试见
`LastOpenedFileStoreTest`）记住"最近一次通过系统文件选择器主动挑的文件"
（只记这条路径——`ACTION_VIEW` 分享过来的 Uri 权限是临时的，对它调用
`takePersistableUriPermission` 会直接抛 `SecurityException`，语义上也不适合
冷启动时不问自取地重新弹出来）。真正冷启动（没有配置变化重建的 Uri，也没有
分享过来的 PDF）时读出来自动重新 `loadPdf` 一次，配合已有的 `ReadingProgressStore`
自动跳回上次页码——效果上跟用户没被打断过一样（重新打开现在只要 0.1-1.5 秒，
见 #23）。

同时处理了两个容易漏掉的细节：
- **持久授权数量上限**：安卓限制每个 App 最多约 512 条持久 Uri 授权，超过会
  抛 `SecurityException`。每次选新文件时主动释放上一次记住的那一条，长期
  最多只占用 1 条。
- **避免"打不开的文件"无限重试**：冷启动自动恢复失败（文件被删/移动、权限被
  收回……）时，只有"这次失败的正好是当前记住的那个文件"才清掉记录，不会
  因为用户手动挑了一份坏文件就误把之前一份好端端的记录冲掉。

**真机验证方式**：`adb shell am force-stop app.pdfreader`（模拟 MIUI 锁屏清理
的效果——杀进程 + 清任务卡片，比等真实锁屏触发更快复现）后 `am start` 冷
启动，日志确认新 PID 下没有任何用户点击就自动触发了 `loadPdf` 并成功恢复到
原来的页码范围，装机验证通过。

**分岔路（如实记录，别下次重复踩）**：验证这个修复时，一度怀疑发现了第二个
bug——`uiautomator dump` 看到 `RecyclerView` 里图片条目位置很奇怪（大片
空白），screencap 截图内容区整个空白。加诊断日志直接采样 Bitmap 像素
（网格采样统计非白色占比，走 logcat 文本输出，不受大文件传输问题影响）确认
图片解码本身完全正常（非白占比 10%-12%，就是正常扫描文字页该有的墨迹密度）。
最后定位：**这次会话 adb 传输大文件（截图、`adb pull`）非常不稳定，经常从几
百 KB 到几 MB 之间随机截断**，`sips` 对截断的 PNG 很宽容（照样解出图像开头
部分、其余留白，不报错），造成"内容区空白"的假象；`uiautomator dump` 那次
"位置奇怪"大概率是同一次会话里 RecyclerView 冷启动首帧 + 立刻截图的时序巧合，
不是稳定复现的布局 bug（后续同样操作的 dump 恢复正常）。教训：**这台设备/这
次会话传大文件前先跟已知稳定信号（文本类：logcat、uiautomator xml）交叉验证
一下，别急着信一张可能被截断的截图**——已经在 `feedback_verify_on_real_path`
这条记忆里记过类似教训（模拟环境自测比不测更有欺骗性），这次是同一个精神的
另一种表现形式（"看起来传完整了的文件，其实是截断的"）。

## 25. 已修（部分）：JBIG2 图片"有的干脆不出现"——真正解码走不通，改成显示占位图

**背景**：#24 修完之后用户追加反馈"图片加载不全"，追问确认是"有的图片干脆不
出现"（不是显示不完整/加载慢）。加诊断日志直接在 `drawImage` 里记录每一次
图片被跳过的原因，真机复现：

```
图片被跳过[标准解码失败] bpc=1 suffix=jb2 宽高=2112x3012
```

**根因**：`suffix=jb2` 说明是 JBIG2 编码——扫描书常用的黑白（1 位）压缩格式，
PdfBox-Android 这个 fork 完全没实现 JBIG2 解码器，`pdImage.image` 对这种图片
必定失败，原来的兜底逻辑因此把每一张 JBIG2 图片都静默跳过，不报错也不提示。

**试过、装机验证过走不通的路**：接入 Apache 官方的 `jbig2-imageio`
（`org.apache.pdfbox:jbig2-imageio:3.0.5`，原 levigo/jbig2-imageio 捐赠给
Apache PDFBox 项目，纯 Java、Apache 2.0 许可证、无 native 代码、经查 Maven
Central 确认无运行时第三方依赖——供应链信任度够，事先跟用户确认过要不要
接这条路）。`gradle assembleDebug`/`dexBuilderDebug` 都能成功打包（编译期
`android.jar` 有桩类能过编译），**但装机真机测试直接炸**：

```
decodeJbig2 抛异常: java.lang.NoClassDefFoundError: Failed resolution of: Ljavax/imageio/ImageReader;
```

这个库是标准 `javax.imageio` ImageReader 插件，依赖 `java.awt.image
.BufferedImage`/`javax.imageio.*` 这些桌面 Java API——**Android 运行时从设计
上就不包含整个 AWT/Swing/ImageIO 桌面 GUI API 体系**，编译能过、装机就抛
`NoClassDefFoundError`，没有版本能绕开这一点。已撤回这次依赖改动
（`build.gradle.kts`）。

**已修的部分**：JBIG2 图片改成显示一块保留原图长宽比、缩到固定小尺寸
（长边 400px，见 `PdfTextExtractor.createUnsupportedImagePlaceholder`）的
占位图（浅灰底+边框+"图片格式不支持（JBIG2）"提示文字），不再静默消失——
真机装机验证过占位图确实画出来了（背景色像素采样精确匹配 `#EEEEEE`）。有
单元测试覆盖（`PdfTextExtractorImageTest`"JBIG2图片显示占位图，不再静默
消失"）。

**未修的部分（如实记录，别下次重复评估同一条路）**：真正解出 JBIG2 内容
目前没有可行路径——唯一还没试过的是 JNI 包一个 C 语言解码器（比如
`jbig2dec`），但跟本项目"无 native 库"的一贯选型原则直接冲突（见
`CLAUDE.md` 拒绝 MuPDF 的理由，供应链信任度/编译风险是同一类考量）；或者
从 JBIG2 规范（ITU-T T.88）自己实现一个纯 Kotlin 解码器——工作量是"整个
格式的算术编码+通用区域解码+符号字典+文字区域"，规模上不是这个项目其它
"库有 bug、自己按规范补一段"类型的修复能比的，这次没有做。

**方法论**：跟 #19/#23/#24 一样，先加诊断日志拿到真实数据（`suffix=jb2`）
再动手，选新依赖前用 WebSearch/WebFetch 核实许可证和依赖树（不是凭训练
记忆猜），关键的"Android 运行时兼容性"这一条搜索结果给不出确定答案时，
没有直接采信"应该没问题"，而是真的装机跑一遍拿到确定性的
`NoClassDefFoundError`——事先跟用户说清楚"这条待验证"，出问题后如实
汇报、撤回，不是硬撑着说"能用"。

## 26. 已修：这本书部分页面显示反了——页面级 `/Rotate` 没被读取过

**背景**：#25 修完之后用户反馈"这本书部分页面显示反了"。先排查了两个候选
方向：

1. 查这份文档缓存文件的原始字节，`strings | grep /Rotate` 发现全书页面
   `/Rotate 0`，一度以为可以排除页面级旋转这个假设。
2. 加诊断日志确认翻到的几页图片 CTM 都是最简单的"不旋转不镜像"，没有触发
   `orientationMatrixOrNull` 的已知局限（非轴对齐 CTM 不修正）。

两个方向都排除后用户反馈"现在就能看到反的"，重新加日志才在**另一份缓存
文件**里看到 `pageRotation=180` 和 `pageRotation=0` 相邻交替出现——第 1 点
的排查排查错了具体文件（用户当时打开的不是那份文档，缓存目录里有几十份
`opened-*.pdf`，`ls -t` 挑的"最近修改"不一定是当前正在读的那份）。

**根因**：确认后是页面字典的 `/Rotate`——扫描书很常见：大部分页面正常拍摄，
个别页面扫描时物理方向不同，靠这个字段告诉阅读器"显示时把整页转一下"。查
PdfBox-Android 官方源码确认 `PDPage.getMatrix()`（`drawImage` 拿到的 CTM
最终基于这个"初始矩阵"累积）**固定返回单位矩阵，完全不看 `/Rotate`**——这
是 PdfBox-Android 内容流处理管线本身的缺口，不是本项目代码引入的 bug；页面
级整页渲染的 `PDFRenderer`（表格检测用）是独立代码路径，会正确处理，不受
影响。

**修法**：新增 `applyPageRotation`（`PdfTextExtractor.kt`，纯 `Matrix
.postRotate` + `Bitmap.createBitmap` 重采样，跟 `applyCtmOrientation` 是
同样的"矩阵计算/像素重采样"两段式）+ `PageContentStreamEngine.pageRotation`
字段（读 `page.rotation`）+ `orientImage` 把两层修正叠一起（先 CTM 朝向、
再页面级旋转）。真机日志确认这本书确实存在 `pageRotation=180` 的页面，修
复后这些图片会被正确转正。单元测试见 `PdfTextExtractorImageOrientationTest`
两条新用例（0 度快速路径 + 设了 `/Rotate 90` 的整页集成测试——跟已有的 CTM
朝向测试一样，Robolectric 环境下 `Bitmap.createBitmap` 带 `Matrix` 参数的
影子实现不会真的重采样，测试只能验证"链路接上了、没抛异常"，不能验证
真实像素，真机验证才是最终判据）。

**教训**：诊断时"用哪份缓存文件代表当前文档"这件事本身容易搞错——这台
设备缓存目录常年堆着几十份 `opened-*.pdf`（每次打开都新建一份，没有清理
机制，见 `MainActivity.copyToCacheFile`），`ls -t` 挑最近修改的不一定是
用户当前正在看的那份（比如同一份文件被打开关闭多次，或者诊断过程中穿插
打开过别的文件）。更可靠的确认方式：从 `LastOpenedFileStore`/日志里的
`loadPdf` 时间戳反推，或者干脆让用户直接告诉你翻到第几页——这次是后者
（用户反馈"现在就能看到反的"）帮着避开了这个坑，不是靠猜文件名解决的。

## 27. 已确认：JBIG2 真正解码在 Android 上没有可行路径——两次装机验证过

**背景**：用户明确要求"解出 JBIG2 图片内容，找找好的方案"——#25 只做了
占位图，这次专门花一轮找真正的解码路径。

**尝试 1（`org.apache.pdfbox:jbig2-imageio` 标准入口）**：走库自带的
`JBIG2ImageReader.read()`——真机报
`NoClassDefFoundError: Ljavax/imageio/ImageReader;`。这是 #25 已经踩过、
记录过的坑，这次重新确认了一遍前提没变。

**尝试 2（绕开 ImageReader，直接访问库内部）**：反过来查了这个库自己的
源码结构（不是猜的，用 WebFetch 逐个文件核实）：真正的解码结果装在库自己
的 `Bitmap` 类里——这个类整个是 `public`，完全不 import 任何
`javax.imageio`/`java.awt.image` 相关的包，数据存成 `byte[]`
（`getByteArray()` 能直接拿整个数组）。真正碰 AWT/ImageIO 的只有最外层的
`JBIG2ImageReader` 封装，`JBIG2Document`/`JBIG2Page`（内部编排逻辑）本身
很干净，只是构造函数/`getBitmap()` 是 `protected`。写了一个新文件
（`app/src/main/java/org/apache/pdfbox/jbig2/AndroidBridge.kt`）故意声明
成跟它们同一个包，靠 Java/Kotlin"同包可访问 protected 成员"这条语言规则
拿到访问权限（不是反射）。

第一轮真机测试：`NoClassDefFoundError: MemoryCacheImageInputStream`——
`JBIG2Document` 构造函数还需要一个 `javax.imageio.stream.ImageInputStream`，
库自带的这个具体实现类不可用。**关键的第二个问题**：具体实现类不可用，
不代表接口本身也不可用——这是两件独立的事，值得单独验证。于是手写了一个
不 extends 任何 `javax.imageio.stream.*` 抽象基类、只依赖 `ImageInputStream`
接口本身的最小实现（`ByteArrayImageInputStream.kt`，~45 个接口方法全部
手写，数据直接从内存里的 `ByteArray` 读）。

第二轮真机测试：`NoClassDefFoundError: javax/imageio/stream/ImageInputStream;`
——**连接口本身都不可用**。这把"绕开具体类、只用接口"这条路也彻底堵死了：
`javax.imageio.stream` 这整个子包在 Android 运行时都不存在，不是某几个
具体类的问题。

**结论（已验证，别再重复评估同一条路）**：`jbig2-imageio` 这个库任何一层
入口最终都会触碰 `javax.imageio`/`java.awt.image` 家族的类型，Android 平台
从设计上就不包含整个 AWT/Swing/ImageIO 桌面 GUI API 体系，没有绕开的办法。
两次尝试都已撤回全部代码改动（新增文件删除、`build.gradle.kts` 依赖撤回），
仓库恢复成 #25 那版"占位图"状态。真正解出 JBIG2 内容仍然只剩两条没试过
的路（跟 #25 记录的一样）：JNI 包 `jbig2dec` 这类 C 语言解码器（违反本项目
"无 native 库"的选型原则）、或者从 JBIG2 规范自己写一个纯 Kotlin 解码器
（工作量是另一个数量级）。

**方法论**：核实一个库能不能在 Android 上用，不能只测"最外层的标准入口"
就下结论——这次如果止步于尝试 1，会错误地把"这个库不能用"和"这个库的
某一层封装不能用"混为一谈；深入到"绕开标准入口、直接用内部类"这条路之后
才确认问题出在比 `ImageReader` 更底层的 `javax.imageio.stream` 这整个
子包上，这才是"这个库彻底不能用"的确凿证据，不是猜测。两轮都是先读官方
源码（WebFetch，不是凭记忆）搞清楚类结构和访问修饰符，再写代码验证，再
装机拿到确定性的错误信息，跟 #25 的方法论一致。

## 28. 已修：JBIG2 占位图的提示文字被页面旋转误伤，转得上下颠倒、左右镜像

**背景**：#26 修完页面级 `/Rotate` 之后用户反馈"部分页面显示反了，没有修好"。
加诊断日志（临时给 `drawImage` 加了页码/suffix/位深/`pageRotation` 记录）
装机复现，同时确认了 #27 那个问题（"JBIG2有些页面可以显示，为什么"）：
这本书页面之间混用 JBIG2 和 JPEG 两种编码（`suffix=jb2` 的页面显示占位图，
`suffix=jpg` 的页面走原有解码路径正常显示），不是"JBIG2 有时候能解出来"。

**"反了"的根因**：用户反馈"翻到了"之后截图确认——真正显示反的不是照片
内容，是**我们自己画的 JBIG2 占位图提示文字**"图片格式不支持（JBIG2）"，
在 `/Rotate 180` 的页面上被转成上下颠倒、左右镜像，读不出来。#26 的
`orientImage`（CTM 朝向修正 + 页面级 `/Rotate` 修正）这两层修正的意义都是
"把原始画面转回正确方向"——占位图是运行时现画的提示文字，从头到尾没有
"原始扫描方向"这个概念，套用这两层修正对它来说是纯粹的误伤：本来朝上可读
的文字被硬转成跟原始扫描图一样的方向。

**修法**：JBIG2 占位图分支不再调用 `orientImage`，`createUnsupportedImage
Placeholder` 的结果直接加入 `images`，跳过 CTM 朝向修正和页面级旋转两层。
单元测试（`PdfTextExtractorImageTest`"页面设了 Rotate 90 时，JBIG2占位图
的长宽比不受页面旋转影响"）故意用 90°（不是 180°）验证——90° 旋转会交换
宽高，如果占位图被误套页面旋转，长宽比断言会直接失败，比用 180°（宽高
不变，容易被"看起来没问题"掩盖过去）更容易在测试里揪出这类回归。

**过程中的一次失误（如实记录）**：真机排查时一次自动化滑动手势因为 adb
连接抖动坐标发飘，意外点开了系统文件选择器——立刻按返回退出，没有选择
任何文件，查缓存目录确认原来那本书没有被换掉。但排查过程中一度因为这次
意外跳转，两批诊断日志的数据对不上（同一页在前后两次记录里报的图片尺寸
不一样），一度怀疑是新的独立 bug，后来定位到是这次误触导致日志混乱，
不是真实的双重问题。教训：涉及真机手势自动化时，adb 连接不稳定的时段
（这次会话反复出现的已知问题）要更谨慎，宁可少做几次自动滑动、多向用户
确认，也不要在连接状态不确定时连续发手势指令。

**已知局限（如实记录，可能还没覆盖全）**：这次只处理了 JBIG2 占位图这一
种"合成内容不该被套用图片朝向修正"的场景。`decodeRawImageByBitDepth`
手动位深解码失败时目前直接跳过不显示（不生成占位图），如果以后也给这条
路径加占位图，需要记得同样跳过 `orientImage`，不要重复踩这次的坑。

## 29. 已修：CMYK JPEG 解码结果是纯黑，不是花屏——推翻了 #18 那次"回退路径没问题"的结论

**背景**：另一本书用户反馈"这本书图片显示颜色不对"，追问确认是"背景变为
黑色"。加诊断日志（网格采样统计纯黑像素占比，同 #24/#25 用过的手法）装机
复现：这本书里几乎每一张 JPEG 图片，网格采样纯黑占比都接近或等于 100%
（比如 3675/3675、10488/10488），不是背景局部变黑，是整张图基本全黑。

**根因**：这些 JPEG 全部是 4 分量（真实 CMYK/YCCK，用 `JpegComponentCount
.of` 读 JPEG 自身 SOF 标记确认，不受 `pdImage.colorSpace` 那个"CMYK 永远
误报成 DeviceRGB"的已知缺陷影响，见 #18）。`decodeJpegWithNativeSubsampling`
里 CMYK 检测本身工作正常（正确挡住了带降采样参数的快速路径，避免了 #18
记录的对角线花屏），但挡住之后落到的回退路径——`pdImage.image`
（PdfBox-Android 自己的解码）——对这批 CMYK JPEG 解码结果是纯黑，不是
花屏也不是颜色正确。#18 当时写的"`pdImage.image` 内部会调
`PDColorSpace.toRGBImage`，能正确处理 CMYK，只是慢"这句话**只验证了"能
消除花屏"，没有反过来验证"回退路径解码结果真的颜色正确"**，是那次判断
留下的缺口，这次真机数据推翻了这个假设，已经在 #18 对应的 KDoc 位置补记
不要误导以后的读者。

**双重验证，排除"这是 PdfBox-Android 一个库的 bug"这个可能性**：绕开
PdfBox-Android，直接用安卓原生 `BitmapFactory.decodeByteArray` 解码同一份
原始 JPEG 字节（不带任何降采样参数，排除降采样相关的另一个变量）——结果
同样纯黑。两条完全独立的解码实现（一个纯 Java 的 PdfBox 实现，一个安卓
自带的 Skia/libjpeg-turbo 原生实现）给出一致的失败结果，说明这不是某一个
第三方库的 bug，是这台设备的安卓系统层 JPEG 解码器本身就不认识这批 JPEG
用的这种 CMYK/YCCK 编码变体（推测是 Adobe 反色 CMYK 之类的非标准约定，
具体是哪一种没有继续深挖字节级细节，性价比不高——两条路都已经证明走不通，
不需要再确认"具体是哪种不标准"这个更深的问题）。

**修法**：新增 `decodeCmykJpegPlaceholderOrNull`——检测到 JPEG 是 4 分量
就直接跳过所有解码尝试（快速路径、`pdImage.image` 都不试），复用 #25/#28
给 JBIG2 建好的"诚实占位图"机制（同样不套用 `orientImage`，原因跟 #28 一致
——占位图是合成内容，没有"原始扫描方向"这回事）。检测本身很轻量，只读
JPEG 头部 SOF 标记算分量数，不需要真的解码一遍再看结果是不是纯黑。

`PdfTextExtractorJpegSubsamplingTest`"超过阈值的大 CMYK JPEG..."那条测试
断言的是旧结论（回退到 `pdImage.image`、保留原始尺寸），已经跟着这次修复
改写——新断言验证的是"显示占位图"（长边远小于原图、长宽比跟原图一致）。

**方法论**：跟 #25/#28 一样，先用真机数据把"猜测"变成"确认"（网格采样
统计黑色占比，不是看一眼截图就下结论），关键假设（"CMYK 图能正确解码，
只是走的路径不同/比较慢"）没有单独验证过就写进代码注释，这次踩了一次
"记录下来的结论其实没有被完整验证过"的坑——教训是：修复某个具体症状
（花屏）之后，如果连带宣称"这样就正确了"，这个"正确"本身也需要独立验证，
不能因为原来的症状（花屏）消失了就假设问题已经彻底解决。

## 30. 已修：双指放大后分辨率明显低于 WPS——两处解码分辨率上限调高

**背景**：用户反馈"这个文件放大以后可以发现，分辨率远小于用其他pdf软件
（wps)打开的分辨率"。

**根因**：本项目的双指缩放（`enablePinchZoom`）是纯粹的"把已经解码好的
`Bitmap` 用 `Matrix` 放大显示"，不是"缩放时重新按更高精度解码"——这意味着
放大能看到的细节上限，从解码那一刻起就被锁死了，解码分辨率越低，放大后
越糊。两处解码分辨率上限定得偏保守：

1. `MAX_IMAGE_DIMENSION_PX`（内嵌图片解码降采样阈值）：2000px，是照着
   "这台设备物理宽度约 1440px"定的（见 NOTES.md #13），只留了不到 1.4 倍
   的放大空间——超过这个倍数，图片早在解码阶段就被降采样丢了细节，放大
   只是把马赛克放大给你看。
2. `TABLE_PAGE_RENDER_DPI`（表格区域整页渲染 DPI）：150，同样偏低。

**修法**：`MAX_IMAGE_DIMENSION_PX` 2000→3000（常见 300dpi 扫描件，A4 约
2481×3508px，基本不会再触发降采样），`TABLE_PAGE_RENDER_DPI` 150→220。
两个常量的详细权衡写在各自的 KDoc 里（`PdfTextExtractor.kt`），不重复抄
一遍。

**如实记录的代价**：`MAX_IMAGE_DIMENSION_PX` 提高之后，原来会被降采样
（省内存/省时间）的一批图片（长边落在 2000-3000px 区间）现在会解码到
更高分辨率，加载更慢、内存占用更高。用 #13 记录的比例线性估算，如果一份
文档的图片大多落在这个区间，加载时间可能重新逼近甚至超过 2026-08-19 那次
优化之前的 25 秒量级——这次没有选更保守的中间值（比如 2500），是因为用户
的诉求是"效果匹配 WPS"这个相对明确的目标，权衡后优先满足这个诉求，代价
（大图文档加载变慢）目前还没有实际反馈遇到，如果以后用户反馈"打开变慢
了"，直接回来看这条记录，不用重新排查是哪次改动引入的。

**测试改动**：`SubsamplingFactorTest` 全部断言跟着新阈值改写；
`PdfTextExtractorJpegSubsamplingTest` 的"大 JPEG"fixture 从 3000×2000
重新生成成 4500×3000（Python Pillow，跟原来同一个四象限测试图案），保持
"长边超过阈值、触发一次减半降采样"这个测试意图不变，`cmyk-quadrant.jpg`
（走占位图分支，不依赖尺寸阈值）不用动；`PdfTextExtractorTableTest` 的
表格裁剪高度上限按 DPI 提高的比例（150→220，1.4667 倍）等比放宽。

**未做的部分（如实记录，以后有需要再评估）**：真正匹配 WPS 那种"缩放时
按当前缩放级别重新渲染"的体验（理论上限是"不管怎么放大，只要没超过源图
原始像素密度，都清晰"），需要把 `enablePinchZoom` 从"缩放已解码 Bitmap"
改造成"缩放触发重新解码/重新渲染"，这次没有做——这是本项目图片显示模块
最大的一次架构改动候选，工作量和风险都明显高于"调高两个分辨率常量"，
这次先用成本低得多的方式尽量贴近诉求，如果用户反馈"提高上限之后还是
不够"，再评估要不要做这个更大的架构改动。

## 31. 已修：JBIG2 真正解码——通用区域+符号词典两种编码都自己写了完整解码器

**背景**：用户明确要求"解决 JBIG2 图片显示的问题"——#27 已经确认第三方库
两条路都走不通（Android 运行时不存在 `javax.imageio.stream` 整个子包），
这次不再尝试接库，改成自己实现。

**摸清范围再动手**：加了一个轻量诊断（只扫 JBIG2 段头的类型字段，不解码
任何像素），确认真机反馈的那本书全部 JBIG2 图片段类型只有 `[48, 38]`——
页信息 + 通用区域，没有符号词典/文字区域这些更复杂的类型。这说明这批数据
是最简单的"整页当一张位图算术编码"，只实现通用区域解码器就够覆盖这个
场景，不需要啃下符号词典+文字区域这块工作量高一个数量级的部分。

**实现**：新增 `Jbig2GenericRegionDecoder.kt`——照着 ITU-T T.88/ISO IEC
14492:2001 标准描述的算法，对照 Apache PDFBox 项目的 `jbig2-imageio`
开源实现（只当参考，不依赖它）用纯 Kotlin 重写了 MQ 算术解码器 + 4 种
通用区域模板的逐行解码逻辑。滑动窗口式的位运算写法照抄参考实现，没有
改写成更符合直觉但需要自己重新推导正确性的版本——算术编码这类状态机
对 context 数值精确性极其敏感，错一位不会报错崩溃，只会解出一张跑偏的
错误图片，没有其它信号能提示出错，抄的时候不引入新错误比自己重新推导后
相信它是对的更可靠。

**正确性验证方法（这是这次修复里最关键的一步）**：不是"看着顺眼就信了"，
是找了一个可信的独立标准来比对。`testImplementation` 引入
`org.apache.pdfbox:jbig2-imageio`（只在纯 JVM 单元测试范围用——这个库在
Android 运行时用不了是 #27 确认过的事，但 Robolectric 跑在桌面 JVM 上，
不受这个限制），构造 7 组不同参数（4 种模板×开关 TPGDON×非 8 倍数宽度×
多个随机种子）的最小合法 JBIG2 段，同一份字节喂给自己写的解码器和这个
成熟的参考实现，逐像素比对——**全部完全一致**。这是比"对一份 fixture
人工核对"更强的证据：验证的是算法本身在多种参数组合下都等价，不是某一份
数据的巧合。

**装机验证发现了范围边界（如实记录）**：装到真机后，用户反馈"这本书还是
有图片不能正常显示"——排查发现是**另一本书**（不是最初摸清范围那本），
JBIG2 图片用的是符号词典编码，不是通用区域。这不是这次实现有 bug，是
"某一份真机数据只用了简单编码"这个结论不能泛化到所有书——JBIG2 标准
允许"通用区域"和"符号词典+文字区域"两种完全不同的编码策略，扫描软件
选哪种没有统一标准。诊断方法：直接把这本书 JBIG2 图片的原始字节前 64
字节转十六进制打进日志，手工按标准的段头格式逐字段解析，确认页信息段
（类型 48）之后紧跟的是类型 0（符号词典），不是类型 38——不是靠猜，是
直接读字节验证。完整背景/已知局限见 `Jbig2GenericRegionDecoder.kt` 类
KDoc（已经在这次改动后更新，记录了这个边界）。

**现状**：通用区域编码的 JBIG2 图片现在能正常解码显示了；符号词典编码
的解码器 2026-08-23 同一天也已实现并验证（`Jbig2SymbolTextDecoder.kt`，
632 行，见下面更新），两种编码策略现在都覆盖。遇到更冷门的变体（Huffman
编码的符号词典、精细化聚合、词典间导入）仍返回 null 走占位图。

**2026-08-23 更新：符号词典解码器从"未验证"变成"交叉验证过"**。实现当天
用户配合装机，一次性诊断代码从 logcat 导出了这本书最小的一张符号词典图片
（主数据流 5073 字节 + 共享 Globals 流 4624 字节，存成测试 fixture
`jbig2-symbol-*.jb2`）。验证方法有个小巧妙处：jbig2-imageio 的
`JBIG2Globals` 只是空容器、没有从字节解析的入口，但观察段编号发现 Globals
流的段是 0-12、主数据流第一个段（页信息）编号 13——两半本来就是一个连续
编号序列，直接拼接 `globals + main` 就是一份合法的内嵌组织形式流，参考实现
单流构造函数直接吃。自己写的解码器和参考实现对这份真实数据逐像素比对完全
一致。装机肉眼确认：这本书之前显示不出的图片现在内容正常。诊断代码用完
即删（当时注释就写明"诊断完会删掉"）。

## 32. 已修：CMYK JPEG 自己手写解码器真正解出内容（排查过程保留如下）

**背景**：用户反馈"这本书的cmyk jpeg格式的图片无法显示"（#29 已经把这类
图片从"纯黑块"改成了诚实的占位图，这次是要求真正解出内容，不是要求"占位图
本身有问题"）。

**先查性价比更高的路再决定要不要写解码器**：加诊断读了这批图片的 JPEG 头部，
确认带 Adobe APP14 标记、`transform=0`——这是 Photoshop/印刷软件产出 CMYK
JPEG 的标准做法，通常意味着数据是"反色"存储的（真实值取反后才是最终颜色）。
如果失真的根因真的只是"安卓的解码器没处理这个反色约定，把反色数据当正常
数据处理"，理论上把已经解出来的（已知纯黑的）结果直接做一次 RGB 反相，
应该能大致救回可辨认的内容——这条路成本几乎为零（不需要写任何解码逻辑，
只是对已有输出做一次简单变换），值得在动手写解码器之前先试。

**验证结果：这条低成本路径不成立，别再重复尝试**。真机测了十几张图，反相
后结果压倒性地是均匀纯白，不是"大部分白背景+小部分黑字"这种能辨认出扫描
页的分布——比如 `2317/2337` 纯黑像素反相后 `2320` 接近白、`0` 接近黑、仅
`17` 落在中间调。这种分布不是"颜色算错了但内容还在"，是解码这一步本身
几乎没有保留任何图像细节（近乎均匀单色输出）。推测这台设备的 JPEG 解码器
对这类 4 分量数据在比"色彩空间转换"更早的阶段就出了问题（比如色度通道
处理或者饱和截断），不是一个能靠事后颜色变换修正的"选错了转换公式"问题。

**结论**：真正解出这批 CMYK JPEG 的内容，没有找到比"自己实现 JPEG 解码
（熵解码+反量化+IDCT+色度上采样+正确的 CMYK/YCCK→RGB 转换，含 Adobe 反色
约定处理）"更便宜的路。这个工作量和 #31 的 JBIG2 符号词典是同一个数量级
（甚至可能更大——JPEG 标准本身的解码步骤比 JBIG2 通用区域更多）。用户表态
投入后已完成，见下方更新。

**2026-08-23 更新：解码器已实现并接进取代占位图**。`JpegDecoder.kt`
（550 行，手写完整 JPEG 解码：标记解析+Huffman 熵解码+反量化+8×8 IDCT+
Adobe 反色约定下的 CMYK→RGB 转换）。方法论沿用 #31：范围严格限定在真机
字节级确认过的数据形状——4 分量、Adobe APP14 transform=0、1×1 采样、
baseline/extended sequential 单扫描；范围外（渐进式、色度子采样、YCCK
transform=2、无 Adobe 标记）一律返回 `null` 退回诚实占位图，不猜不硬解。
这块限定顺带避开了 JPEG 里最复杂的两块（色度上采样滤波器选择、渐进式多次
扫描），不是偷懒，是"没见过的数据形状不给可能错误的结果"。

**交叉验证的参考实现换过一次，如实记录**：最初想学 #31 用第三方 Java 库
（TwelveMonkeys imageio-jpeg）在测试里当场解码比对——喂一张已知纯黑
（CMYK 全拉满、K=0）的最小测试图，这个库解出 (44,48,49)，偏差量级不是
有损压缩误差，是它自己处理这类极端组合有问题，不能拿自己都不准的实现当
标准答案。改用 Python Pillow（底层 libjpeg-turbo，工业界最广泛使用的
JPEG 库）预先解码存成无损 PNG 当参考：7 组数据（6 组合成：四象限纯色/
非 8 倍数尺寸渐变/含黑墨灰度/重启间隔/高频噪声 + 1 组真机取回的原始数据
3000×2000）逐像素比对全部一致。真机那组是"合成数据上验证过的算法"和
"真机真实数据"之间的桥——装机后用户看到的画面对不对，最终由它背书。

**已知边界**（范围外数据维持占位图现状）：渐进式 JPEG、色度子采样、
YCCK（transform=2）、无 Adobe 标记的 4 分量数据——真机目前没遇到过这些
变体，遇到了再说。**待办**：真机装新 APK 打开那本 CMYK 书，确认真的显示
内容（测试只证明了解码算法和接线逻辑，装机效果要人眼看）。

## 33. 已修：`Session` 后台线程跟 `loadPage` 并发读同一个 `PDDocument`，同一份文件解析结果不确定——不是真机反馈，是主动借语料库撞出来的

**背景**：用户问"这个项目怎么系统性发现边缘情况"，讨论后决定试一条新路子：
不等真机用户撞见，主动借用 `mozilla/pdf.js` 项目 `test/pdfs/` 测试语料库里
多年积累的真实 bug 复现文件（CMYK JPEG、JBIG2、页面 `/Rotate` 各挑几个）
接进现有解码链路探测。这批文件大多数结果都在已知范围内（该走占位图的走
占位图），但 `JBIG2Globals.pdf`（这份文件本身来自一个 pdfminer.six 的
GitHub issue，xref 表缺了 2 号对象定义，结构本身有缺陷）验证时发现一个
新东西：独立全新 JVM 之间反复跑，同一份文件解析出的结果不一致——有时候
"2 张图片、0 段文字"，有时候"0 张图片、300/42/3/1 段文字"。

**定位过程**：先怀疑是文件本身的畸形结构触发了某种哈希序相关的解析分支
（xref 缺对象常见的修复路径），但推演发现顺序扫描式修复不该受哈希序影响，
这个方向站不住。改成直接读 `Session.loadPage` 源码，发现 `Session.open()`
起了两个后台线程（`footerLearningThread` 学页脚水印、`outlineThread` 抽
目录，见 #23），都会 touch 同一个 `document`（`PDDocument`）——这份 fixture
只有 1 页，页脚学习的取样范围（最多 150 页）正好把这唯一一页也读一遍，
跟调用方紧接着 `open()` 之后同步调的 `loadPage(1)`（"打开文档立刻看第一
页"是正常使用路径）形成真实的并发访问。PDFBox-Android 没有声明 `PDDocument`
线程安全。

**受控实验实锤**（不是靠猜）：同一个 JVM 里对同一份文件反复调用 `Session
.open()` + `loadPage(1)`，对比"立刻读页"（有并发风险）vs"先调 `awaitFooter
LearningForTest()`/`awaitOutlineForTest()`（两个本来就为测试留的等待接口）
等两个后台线程跑完再读页"（无并发）：不等的一组 8 次里 2 次结果异常，等
了的一组 8 次全部一致；加到 40 次重复同样的结论。定位到根因之后再看，
这个 bug 理论上不挑文件——任何文档的前 150 页都有被撞上的可能，只是大多数
文档内容复杂、后台线程跑得慢，两边撞上同一页的窗口期短，真机上不容易被
注意到；这份 fixture 小、解析快，把窗口期放大到了肉眼可见的概率。

**修法**：加一个 `documentLock`，把三处会碰 `document`/`PDPage` 的代码都
用它串行化：
1. `loadPage` 整体包一层锁（拆成 `loadPage`→加锁→`loadPageLocked` 两层，
   不动原来的函数体）——函数体内部有好几处独立 touch 文档的调用，逐处判断
   "中间这段纯内存计算安不安全被打断"出错代价高，`loadPage` 本身就是
   "几十毫秒量级"的操作，整体加锁足够快也足够安全。
2. `footerLearningThread` 改成**逐页**加锁/放锁，不是一次性锁住整个 150
   页取样区间——后者会让 `loadPage` 最坏情况等这一整批后台学习跑完才能
   拿到锁，等于把 #23 特意做的"后台学习不阻塞打开"这个优化又变相锁死了；
   按页拆开后 `loadPage` 最多等一页的学习耗时。
3. `outlineThread` 整体包一层锁——目录树通常小，一次锁住够快。

**验证**：受控对照实验从 8 次扩到 40 次，加锁前会错、加锁后 40/40 全部
一致；全量单元测试（含这次新增的两个诊断测试文件）212 条零失败零错误。
没有做真机验证——这次改动是并发时序层面的，真机上的表现取决于这台设备
CPU 调度/后台线程实际跑多快，跟 Robolectric 环境下的时序不是同一回事，
装机验证意义有限，靠受控实验里"加锁前后行为差异"这个更可靠的证据链。

**新增的诊断工具**：`PdfjsEdgeCaseFixturesTest.kt` +
`app/src/test/resources/pdfjs-edge-cases/` 下 11 个从 `mozilla/pdf.js`
语料库借来的真实边缘案例文件（CMYK JPEG 2 个、JBIG2 通用区域/符号词典/
Huffman 5 个、页面 `/Rotate` 4 个）——这不是这个项目自己积累的真实用户
反馈，是主动借用另一个成熟 PDF 阅读器项目多年 bug 报告攒下来的边缘案例
语料库，留在仓库里备将来继续用。这次顺带验证了 fork 报告不能直接采信：
第一次派 fork 去跑这批探测，返回了一个 0 次工具调用、看起来完整实际什么
都没做的假报告，靠 `git status` 核实才发现；第二次真正跑出来的报告里，
"疑似占位图"的判断也只是个粗糙的尺寸阈值，靠对照占位图函数的精确缩放
公式（长边应该落在 395-400 附近）+ 左上角像素颜色核对才发现有一处误判。

**方法论小结**：这次是这个项目 33 条记录里第一条不是从真机用户反馈来的
bug，而是主动用外部语料库撞出来的——验证了"参考其他成熟项目的边缘案例
测试语料库"这条路径确实能在真机用户遇到之前找到真实问题，但也验证了
"信任但要核实"这条原则同样适用于自己派出去的 fork：fork 的执行状态
（工具调用次数、`git status` 前后对比）跟 fork 自己汇报的文字内容要
分开核实，不能因为文字汇报读起来完整就直接采信。

## 34. 已修：真机装机验证 #32 的 CMYK 解码器时连续挖出三个新问题——OOM、
读写锁误伤并发性能、公平性饿死，一条比一条隐蔽

**背景**：用户要求真机验证 #32/#33 的修复效果。用之前批量扫描手机缓存文件
的方法（见 #33）精确定位到真正命中"真书不反色 CMYK"场景的文件——
《义务教育教科书·数学·七年级下册》（915 张 CMYK 图片）。装机确认这本书
本身没问题（诊断日志显示全部图片 `解码成功=true`，一致投票 `invert=false`，
跟提交 c5920c6 的结论吻合）。但排查过程中顺带牵出另外两个跟这本书无关、
更严重的真问题。

### 问题一：封面大图（4469×3871，约 1730 万像素）真机 OOM

排查这本数学书封面显示异常时，`logcat` 直接抓到
`Throwing OutOfMemoryError "Failed to allocate a 95744012 byte allocation..."`。
根因：`JpegDecoder.decodeInternal` 给 CMYK JPEG 的 4 个分量各分配一份跟原图
等分辨率的 `IntArray`（[planes]，4 字节/像素）+ 最终 `argb` 又是一份等分辨率
`IntArray`——峰值同时存活内存约等于 20 字节/像素 × 像素数，这张封面图算下来
超过 200MB，直接把这台设备 256MB 堆占爆。因为异常被 `decode()` 的
`runCatching` 悄悄接住，最终显示成误导性的"格式不支持"占位图文案，而不是
真实原因"内存不够"。

**修法**：加 `JpegDecoder.MAX_CMYK_JPEG_PIXELS`（400 万像素，约 2000×2000）
硬上限，解析出 SOF 尺寸后立刻判断，超过直接返回 `null`，不进入任何大额分配。
阈值取法：同一次真机 session 里两个实测数据点——1795×1181（约 212 万像素）
反复解码多次正常，4469×3871（约 1730 万像素）稳定 OOM，400 万取在两者之间
偏保守一侧，按 20 字节/像素估算峰值约 80MB，256MB 堆里留了足够余量。
`decodeCmykJpegOrNull` 额外重复判断一次同一个阈值，让"太大"和"格式不认识"
显示不同的占位图文案（`图片过大，暂不支持解码` vs `图片格式不支持`），不
因为省一次判断而牺牲用户看到的诚实度。

**代价（如实记录）**：这个上限比 #30/#32 测试用的合成 fixture
（`cmyk-quadrant.jpg` 3000×2000、`large-quadrant.jpg` 4500×3000）都小，两个
交叉验证测试从"逐像素比对参考答案"改成"确认明确拒绝"——算法正确性改由
范围内的 6 组合成 fixture + 一份真机取回的 1725×955 fixture
（`cmyk-book-noinv.jpg`）覆盖，没有因为收紧上限就丢失验证覆盖面。真正的
根治办法是把 `planes` 换成占内存更小的存储、或者实现按需降采样解码（工作量
比这次高一个级别，这次没做）。

### 问题二：#33 的并发锁误伤了页面加载的原有并行度

修完 OOM 后用户反馈另一份完全不相关的文件——一份英文年报
（iFASTCorp-AR2023.pdf，用户自己打开的，排查过程中意外触发了系统文件
选择器才换的文档）"加载慢、图片显示不正常"。图片部分确认是已知边界（那张
CMYK 图片色度子采样 + transform=2，从 #29/#32 起就没打算支持，占位图是
设计内行为，不是新 bug）。但"加载慢"是真的：`慢页拆分` 诊断显示矢量扫描/
文字抽取都只要几百毫秒，可整页加载（`PdfPageAdapter.loadPage`）却要
15-18 秒，且好几个页面几乎同一时刻一起卡完——像是排队等同一把锁。

一查确实是 #33 当天加的 `documentLock`：`PdfPageAdapter.loadExecutor` 本来
是固定 3 线程池，设计成最多 3 个页面并发解码（见该类 KDoc），#33 用普通
互斥锁把 `loadPage` 整个函数体串成互斥访问，把这 3 路并发直接压成了串行。

**修法**：`documentLock` 从 `Any()`+`synchronized` 换成
`ReentrantReadWriteLock`——`loadPage` 只拿**读锁**（多个 `loadPage` 之间
恢复原有并发度），后台的 `footerLearningThread`/`outlineThread` 拿**写锁**
（独占，排斥所有 `loadPage`）。#33 发现的那个具体场景（后台线程 vs
`loadPage`）继续被挡住，`loadPage` 之间的并发不再被误伤。

### 问题三：读写锁本身还有公平性饿死问题，装机才测出来

换成读写锁后装机复测那份年报——**没有变快**，好几个页面依然卡 18 秒左右，
且卡住页面自己的渲染诊断只有两三百毫秒，说明真正的等待发生在"拿到读锁
之前"。根因：`ReentrantReadWriteLock()` 默认非公平模式，`footerLearning
Thread` 那个"逐页加锁/放锁"的循环（跑满 150 页样本）虽然每次只短暂持锁，
但循环足够紧凑，非公平模式下可以连续抢到写锁、把等在旁边的读锁请求晾很
久——"锁的粒度细、每次只短暂持有"不等于"另一方不会被饿死"，还需要显式
要公平性。

**修法**：`ReentrantReadWriteLock()` → `ReentrantReadWriteLock(true)`（公平
模式），等待中的读锁请求按到达顺序服务，不会被写锁的连续重新加锁一直
插队饿死。

**真机验证数据**（同一份年报，前后对比）：
- #33 刚发现问题时（普通互斥锁）：好几个页面卡 15-18 秒。
- 换成非公平读写锁：**没有改善**，依然卡 18 秒左右——证伪了"换读写锁就够"
  这个第一版假设，没有装机复测的话这个假设会被误判成"已解决"。
- 换成公平读写锁：降到 4.2-4.6 秒——比 18 秒快 4 倍以上，其余页面本来就快
  的（几百毫秒）没受影响。4.2-4.6 秒仍然比"完全没有等待"慢，是公平排队
  本身的合理代价（读者确实要等写者当前这一轮跑完），不是新 bug，没有继续
  往下压缩。

**回归测试**：`PdfjsEdgeCaseFixturesTest.kt` 新增一条不依赖真机/复杂 PDF 的
测试，直接在独立的 `ReentrantReadWriteLock` 上复现同款"写者紧凑循环 150 次、
每次短暂持有"模式，公平锁 vs 非公平锁对照——公平锁读者等待 6ms，非公平锁
读者等待 836ms，数字上直接证明"公平"这个属性是修复生效的原因，不是巧合。

**方法论小结**：这次三个问题环环相扣，且每一步的"看起来解决了"都被下一次
真机复测推翻过——(1) 光看代码逻辑觉得"改成读写锁应该能恢复并发"，装机测
发现完全没用；(2) 定位到公平性问题、改完再测，才真的见效。跟 NOTES 里
反复出现的教训（#11、#22、#23 等）同一个精神：并发/性能类改动，"逻辑上
说得通"和"装机测过是不是真的更快"是两回事，尤其是这次这种"锁粒度已经
拆得很细了"的情况，直觉最容易在这里失灵——细粒度只保证"不会长时间独占"，
不保证"公平轮到"，两者是完全独立的属性，都要单独验证。

## 35. 已修：JpegDecoder 补上 YCCK（transform=2）+ 色度子采样支持，顺带
发现 CMYK→RGB 转换公式一直算错

**背景**：用户反馈"这本书的图片大部分都不正常"（那份 iFASTCorp-AR2023.pdf
年报）。字节级核实（不是猜）：全书 144 张图片里 97 张是 CMYK，**这 97 张
全部**是 Adobe APP14 `transform=2`（YCCK），不是 [JpegDecoder] 原来唯一
支持的 `transform=0`——67% 的图片因此全走占位图。YCCK 其实是 Adobe 系
工具更常见的 CMYK JPEG 编码方式（比 transform=0 直接存 CMYK 更常见），
之前的范围限定覆盖的反而是相对小众的一种。

**第一步：YCCK 颜色转换公式，两次真机数据反推**。先按教科书 libjpeg 源码
的公式写（`C=255-R`、K 不变），真机 fixture 测出来色彩完全不对（跟参考
解码平均像素差 131/255，接近随机）。放弃凭教科书公式猜，改成用 Pillow
的 CMYK 模式直接读真值（不经过 RGB 反推，避免截断歧义），3 个真机采样点
反推出精确公式——教科书公式的两处符号都反了：C/M/Y 直接等于 YCbCr→RGB
公式算出的 r/g/b（不需要再 `255-` 一次），K 反而需要 `255-K`。3 个点整数级
精确匹配，不是"接近"。

**第二步：改完公式还有 86% 像素差超过 10，牵出一个更大的、影响全部 CMYK
（不只 YCCK）的既有 bug**。加诊断打印最大误差点和误差分布，发现是最后
"CMYK→RGB"这一步的公式本身就有问题——[JpegDecoder] 从 #29/#32 起一直用的
是加法公式 `R=255-min(255,C+K)`，KDoc 里还写着"这条公式是行业惯例"。
实测验证：Pillow（libjpeg-turbo）用的其实是**乘法公式**
`R=(255-C)×(255-K)/255`，两条公式在 K 值低时接近（之前所有测试 fixture
凑巧都是低 K 值，10 容差盖住了差异），K 值高时差异明显（真机 YCCK 数据
K=107~220，加法公式单通道最大差过 60）。"行业惯例"这句话原来没有真正
验证过，这次真机数据（高 K 值）第一次把差异测出来。换成乘法公式后：
YCCK 最大误差从 65 降到 3（浮点精度级别噪声），所有既有 transform=0
交叉验证 fixture 依然通过（K 值本来就低，两条公式该有的差异这次真的
测出来是"没差异"，不是"没测到差异"）。

**装机验证**：那份年报第 3 页的 iFAST 品牌 logo（红色主色 + 青色 "FSM"
色块）正常显示，颜色符合品牌视觉识别，不是花屏或反色。

**当天第一版"部分"修好**：YCCK 支持一开始只覆盖色度采样比例全部是 1×1
（不做子采样）这一种情况——这份年报 97 张 CMYK 图片里只有 4 张是这种最
简单的形状，另外 93 张（96%）带色度子采样，仍然走占位图。用户当天追问
"做剩下那 93 张带子采样的"，同一天继续把这部分也补上了，见下面"色度
子采样支持"一节。

### 色度子采样支持（同一天补上，覆盖剩下 93 张）

**结构改动**：原来的解码循环假设 4 个分量采样比例都是 1×1，一个块=一个
MCU，4 个分量共用同一个 plane 尺寸。真机数据（`[(0,2,2),(1,1,1),(2,1,1),
(3,2,2)]`——Y/K 全分辨率、Cb/Cr 4:2:0 子采样，标准 JPEG 照片最常见的子
采样比例）需要真正的 MCU 结构：MCU 覆盖 `8×Hmax × 8×Vmax` 像素，每个
分量按自己的采样比例贡献 `Hi×Vi` 个块，每个分量单独开一份跟自己采样比例
匹配的 plane（不再强行统一尺寸）。改造后 1×1 场景（`hMax=vMax=1`）是这套
通用逻辑的退化特例，不是两条分支代码——原有的 4 张 fixture 测试全部
不改代码就直接通过，验证了改造没有破坏已经工作的部分。

**色度上采样用最近邻，不是双线性**：子采样分量在最终图像坐标系取样时
用整数除法 `x*Hi/Hmax` 直接取最近的采样点，不做插值——JPEG 解码器"快速"
色度上采样的常见简化，跟本类一贯"够用就行"的尺度一致。真机 fixture
（`cmyk-ycck-subsampled.jpg`，263×256，特意挑颜色变化明显的，纯色图测不出
上采样错位）交叉验证：平均像素差 1.44（对比 libjpeg-turbo 的精细上采样），
只有 0.15% 的像素（色彩剧烈变化的交界处）diff 超过 15，最大到 28——加诊断
打印过这些误差点的坐标，散落在图片内容中间，不是集中在边缘或某个规律
位置，排除了"取样位置算错"这类系统性 bug，是上采样滤波器精细度的正常
差异。测试容差因此对这条 fixture 单独放宽到 35，同时新增一条独立的
"平均差必须 ≤5"断言防止容差放宽变成掩盖真 bug 的漏洞。

**装机验证**：那份年报的一张高管合影照片（白西装、深色背景配青绿色光线
装饰）正常显示，肤色和光影没有错位或变色，不是花屏。

**内存**：子采样分量的 plane 天然比全分辨率小（4:2:0 下 Cb/Cr 各只有
1/4 像素数），总内存比"先都按满分辨率分配再降采样"更省，不需要因为
支持了子采样就调高 [JpegDecoder.MAX_CMYK_JPEG_PIXELS]（见 #34）。

**回归测试**：新增两份真机 YCCK fixture——`cmyk-ycck-book.jpg`（767×2159，
1×1 采样）+ `cmyk-ycck-subsampled.jpg`（263×256，4:2:0 子采样），各自配
Pillow 参考逐像素交叉验证；原来测"YCCK 应该被拒绝走占位图"的回归测试
改成用未定义的 transform 值（99）触发，因为 2 不再是范围外。全量单元
测试 216 条零失败。

**方法论小结**：这次教训是"凭教科书公式/凭已写进 KDoc 的断言"都不能替代
"拿真机数据核对"——libjpeg 源码公式看起来是权威来源，实际用到这份真机数据
上是错的（这份文件的编码器没有严格遵循教科书约定，类似 #32 CMYK 反色/
不反色两种约定并存的教训）；"这条公式是行业惯例"这句话写进代码注释三次
提交都没被推翻，直到这次高 K 值真机数据才第一次真正测出差异——低区分度
的测试数据会让错误的假设长期"看起来是对的"，这次是运气好（YCCK 这个新
场景恰好用了高 K 值真机数据）才把这个潜伏的老 bug 一起挖出来。

## 36. 已修：#34 留的"loadPage 之间没验证过并发安全"这句话，补测试直接
实锤出真 bug——读写锁的"多读者"模型本身用错了

**背景**：#34 引入读写锁时明确留了一句"loadPage 之间会不会互相踩到彼此，
是这次改动之前就存在的、没有实证过的另一个问题，不在这次修复范围内"——
不是真机反馈，是主动去补这个已知缺口。写了一个专门的受控实验
（[SessionConcurrentLoadPageTest]）：6 页文档，每页嵌一张不同的真实
CMYK/YCCK 图片（覆盖 transform=0 反色/不反色、YCCK 1×1、YCCK 子采样几种
真正触发 [JpegDecoder] 不同代码路径的组合），3 个线程（跟真实的
`PdfPageAdapter.LOAD_POOL_SIZE=3` 一致）各自反复对不同页调 `loadPage`，
比对每次解出来的图片数量/尺寸是否跟单线程基准一致。

**结果**：180 次调用（3 线程×60 次）里复现了 1 次真实的数据损坏——第 3 页
本该正常解码出 767×2159 的 YCCK 图片，在并发压力下 `JpegDecoder.decode`
返回了 `null`，退化成了 142×400 的占位图。这跟 #33 那次 JBIG2Globals
"图片=2 文字=0"变成"图片=0 文字=300"是同一类信号：并发访问同一个
`PDDocument`/`COSStream` 时字节被读错/读乱了。

**根因**：#34 引入的 `ReentrantReadWriteLock` 假设"多个 `loadPage` 并发
读是安全的，只有后台写线程需要互斥"——这个假设从来没有验证过，这次补测试
直接证伪了。`PDDocument` 不是"多读者安全、只有写者需要互斥"的资源，它是
PDFBox 内部一整套带懒加载缓存的对象图，读取操作本身可能触发内部状态的
写入（懒加载缓存首次访问时初始化），"读锁允许并发"这个语义对它根本不
成立——读写锁的"读/写"划分本来就是照着"业务语义上的读操作/写操作"配的，
不是照着"这个对象内部会不会被并发访问搞坏"配的，这次是把前者当成了后者。

**修法**：`documentLock` 从 `ReentrantReadWriteLock` 换成
`ReentrantLock(true)`（公平模式的普通互斥锁），`loadPage`、
`footerLearningThread`、`outlineThread` 三处全部改成互斥访问，不再区分
读写。代价是 `loadExecutor` 的 3 个线程不再能真正并发解码 3 个不同页面——
但这份"3 路并发"本来就从没验证过对 `PDDocument` 安全，不能因为"看起来
更快"继续冒数据损坏的风险。

**真机验证（同一份年报，前后耗时对比）**：
- #34 公平读写锁（真并发，事后证明不安全）：4.2-4.6 秒。
- #36 公平互斥锁（无并发，安全）：5.7-6.1 秒。

多花了约 1.3-1.5 秒，换来的是数据不再损坏——这是必要的代价，不是可以
优化掉的回归。没有回退到 #34 之前互斥锁误伤并发那次的 18 秒量级，公平
性修复的收益还在。

**回归测试**：`SessionConcurrentLoadPageTest` 保留在仓库里（不是一次性
诊断脚本），连续跑 5 轮（900 次调用）零异常零不一致，验证竞争真的消失
不是运气。

**方法论小结**：这是这个项目第二次"写下'这个问题没验证过'这句话之后，
真的回来补验证就测出真 bug"（第一次是 #33 本身，用 pdf.js 语料库主动
撞出并发竞争）。#34 那次留下的伏笔不是走个形式的免责声明——180 次里只
复现 1 次，样本量不够大根本测不出来，如果当时没有专门写受控实验、只是
装机翻几页"看起来正常"，这个 bug 会一直潜伏到某个用户在某个特定时刻遇上
它，且因为发生频率低，出问题时几乎不可能靠"再打开一次试试"复现，会显得
无法定位。"没有实证过"这句话写进代码/文档里，应该被当成一个待办事项，
不是可以无限期搁置的免责条款。

## 37. 已修：#14 紧凑列表被段落合并粘成一大段——第三次尝试才对，前两次
（2026-08-19 那次已撤回的尝试）的教训这次真的用上了

**背景**：清点项目待办时挑中 #14——这是当时记录里唯一一条"确认存在、有
过一次失败修复尝试"的真 bug。#14 自己的教训写得很清楚："问题不在要不要
看 x 坐标，而是阈值/触发条件太粗糙"，还建议"先补一批'临表格末行更短'
'段落最后一行明显更短'的负样本 fixture，把阈值往保守调"——这次照着这个
建议做。

**先补 x 坐标数据**：`Line` 加了 `startX`/`endX`/`pageWidth` 三个字段
（取自 `TextPosition.xDirAdj`/`widthDirAdj`/`pageWidth`，行内取最小/最大
而不是只看首尾两个 `TextPosition`），`mergeSameLineRuns` 合并同一视觉行
的片段时同步取并集范围。默认值 0f，旧测试直接构造 `Line` 不传这几个
参数时，新规则里 `isShortLine` 恒为 `false`，行为完全不变——这是保证
这次改动不会意外影响没有提供这个信息的调用方的关键设计。

**用 Puppeteer 生成真机同款 fixture，测出真实数据再定阈值，不凭空猜**：
`sample-compact-list.pdf`（跟 `sample-chinese.pdf` 同一套生成方式，见
NOTES #5，不用 macOS 自带的 textutil/cupsfilter）——7 行紧凑列表
（`margin:0`，行间距≈自然段落内部行距）+ 2 段自然语言（默认 `<p>` 外
边距，行间距明显更大）。加临时诊断打印每一对相邻行的 `endX/pageWidth`
比例，真实数据：7 个列表项两两相邻的比例落在 0.10-0.25，两段自然语言里
真正的整行（非最后一行）落在 0.90-0.94，最后一行落在 0.17-0.33——中间
有一大段空区（0.25 到 0.90），阈值定在正中间的 0.5，两边都留了一倍以上
余量，不是贴着某一侧的边界值猜的。

**规则**：相邻两行的 `endX/pageWidth` **都**低于阈值才强制切段落（哪怕
y 间距没超过原有的 `paragraphThreshold`）。只看单独一行"短"不够——这正是
#14 记录的 2026-08-19 失败尝试踩过的坑（自然段落的最后一行天然更短，
单独判断会把它错误切开）；要求"连续两行都短"，因为自然段落里非最后一行
几乎总是接近整页宽度（排版算法本来就是"能塞多少词就塞多少"），这个组合
在正常段落里基本不会出现，只在"每一项都刻意独立成行"的列表里出现。

**验证**：`CompactListParagraphSplitTest` 两条测试——7 个列表项各自独立
成段（不跟相邻列表项合并）+ 紧接着的 2 段自然语言依然正常合并成整段（不
被拆碎）——都是先写红测试，跑起来确认真的失败（合并成 1 段，不是 9 段），
再实现，再转绿，标准 TDD 节奏。

**主动排查这次改动对既有 fixture 的影响，不是等下一个 bug 报告**：跑全量
测试，2 个既有测试真的中招——`PdfTextExtractorFooterNoiseTest` 和
`PdfTextExtractorSessionTest` 各有一份"每行一个短单词"的合成 fixture
（`This`/`is`/`page`/`$pageNo`/`body` 各自单独一行，或者 `page1 line0`
这种），全部远短于半页宽，新规则会把它们当成列表逐行拆开——**这正是 #14
NOTES 明确警告过的那类回归**（`This is page 1 body` 被拆成 `This is
page`+`1 body`）。这次没有去放宽算法阈值迁就这两个 fixture（那样会重新
削弱列表检测能力），而是确认这两份 fixture 本身的"每行一个词"排版是当年
图省事的写法、不是这两条测试真正要验证的东西（页脚水印过滤、per-page
中位数样本量），把 fixture 内容改成更接近真实段落的"非最后一行接近页宽"
结构，两条测试原本的断言意图完全不变，只是排版更真实。

**装机验证**：这是纯文字布局的几何判断，不涉及图片解码/内存分配，崩溃
风险的量级跟这次专项排查的 CMYK/并发问题不是一类——没有找到真机上现成
的目录页文档来肉眼核对，改用"用真实生成的 PDF 测量真实坐标数据、明确
写清楚判断条件成立的原因"这条路径验证（PDFBox 的文字位置计算在
Robolectric 和真机上是同一份纯 Java 逻辑，不像 `Bitmap` 栅格化那样受
Robolectric 影子实现精度限制，见 NOTES #20/#26 提到的先例）。真机上装了
最新版本、打开真实文档确认没有崩溃、翻页正常。全量单元测试 219 条零
失败。

## 38. 已修：CMYK 尺寸上限定低了，19 张真实图片被误伤成占位图；已修：
页脚学习白付出排序成本；已发现未修：封面页被误判成表格，是真正的
"加载慢"元凶

**背景**：用户反馈"还是那个文件（那份年报），加载慢，图片显示不正常，
你并没有解决"——#34/#35/#36 那轮修复之后的第二次复查，逼着往更深处查。

### 图片显示不正常：CMYK 尺寸上限（#34 加的 400 万）定低了

装机时这次改用像素采样直接在 log 里报告"这张图非白占比"（不传大文件，
adb 截图传输这次会话反复不稳定，见下面"方法论"一节），发现那份年报里
19 张约 460 万像素的人物合影图片全部命中"图片过大，暂不支持解码（CMYK
JPEG）"占位图——#34 定的 400 万像素上限，是照着当时仅有的两个数据点
（212 万安全、1730 万 OOM）估的，缺"真实文档常见图片能有多大"这个数据点，
这次真机数据补上了：460 万像素按 20 字节/像素估算约 92MB，离"确认 OOM"
的 1730 万像素（约 346MB）差近 4 倍，上限调到 600 万（约 120MB 估算峰值，
留约 30% 余量），装机复测这批图片全部正常解码、无 OOM。

`cmyk-quadrant.jpg`（3000×2000=600 万像素，真机数据、逐像素验证过跟
Pillow 完全一致）当年因为超过 400 万被改成"确认拒绝"的断言，这次落回
600 万这个新上限的边界内（`6_000_000 > 6_000_000` 为假，不拒绝），恢复
成原本的逐像素交叉验证测试。

### 加载慢：先修了一个真问题（页脚学习白付出排序成本），又发现了一个更大的、没修的问题（封面页误判成表格）

加锁等待诊断（临时代码，验证完已删）直接测出：某次冷启动 page=113/115/116
各自"等锁耗时"4.4-4.9 秒——不是处理慢，是排队等页脚学习线程释放锁。根因：
`learnFooterTitlesInBackground` 里的 `LineCollectingStripper` 无条件带着
`sortByPosition=true`（跟正式阅读文字提取共用同一个类），但
[RunningFooterFilter.learnTitleLikeNoiseTexts] 只按 `text`/`page` 分组
统计跨页重复率，从不看视觉顺序——`sortByPosition` 这个已知有性能代价
（NOTES #16 记过、没测出具体数字）的开关对页脚学习纯粹是白花钱。给
`LineCollectingStripper` 加 `needsPosition` 构造参数（默认 `true`，不影响
其它两处正式提取的调用点），页脚学习传 `false`。装机复测：这类几秒级
等锁大幅减少（后续几轮复测都在 1-2 秒量级）。

继续追查页面 1（封面）单独还要 14-15 秒的问题，加了更细的分段诊断
（等锁/处理/图片解码块各自计时），一路查下来发现一个跟"加载慢"完全
不同维度的真相：**这份年报的封面页被 [TableGridDetector.tableRegionOrNull]
误判成了表格**！直接打印 `hasImages`/`scanHasFullPageImage`/`tableRegion`
三个真值确认：封面页 `tableRegion=true`——封面是设计感很强的一页（色块、
装饰线条、几何图形），6508 条矢量线段里凑巧有 ≥3 条水平 + ≥3 条竖直、
边界框还重叠，命中了"≥3 横线+≥3 竖线"这条本来是为真表格设计的判定条件。
误判之后代码走进"表格"分支：`PDFRenderer(document).renderImageWithDPI
(pageNo-1, TABLE_PAGE_RENDER_DPI)`——把整个封面按 220 DPI 重新栅格化
成一张完整位图再裁剪，对一页有 6508 条矢量线段的复杂设计页，这一步本身
就很贵，这才是"额外 6.2 秒"真正的去向（不是我最初怀疑的图片解码，
封面页压根没走到 CMYK 图片解码那条分支——这个误判方向本身也是一次
"先靠数据反驳自己的假设，再重新定位"，见下面"方法论"）。

**这次没有动手修**：[TableGridDetector] 的判定条件是"宁可漏检、不可
误判"和"宁可误判、不可漏检"这两个方向来回调过至少三次的敏感区域（NOTES
#17 完整记录过"行列间距均匀性检查"加了又撤的故事，用户当时明确表态
"这类取舍是产品判断，不是技术判断"）。这次撞到的是同一类矛盾的另一个
触发场景（装饰性设计页，不是 NOTES #17 那次的"页边距装饰边框"），贸然
收紧判定条件有重蹈 #17 覆辙的真实风险——上次"看起来影响小"的估计错了，
返工过一次。如实记录，留给用户决定要不要投入、往哪个方向调。

**已知代价（这次误判顺带的次生问题，同样没有动手）**：表格分支渲染
失败或裁剪结果不理想时，用户看到的可能是被裁剪过的封面局部而不是设计
本身的完整画面——这有可能是"图片显示不正常"里被误认成"加载慢"的
另一部分体验问题，但这次没有单独截图核实裁剪结果具体长什么样（截图
传输这次会话太不稳定，见下面"方法论"），如实记录为待核实，不是确认。

**方法论**：
1. **adb 截图这条通道这次会话大范围失灵，不止是大文件传输**——之前
   NOTES #24 记录的是"大文件传输会截断"，这次连普通截图（exec-out 管道
   和先存机身再 pull 两种方式都试过）反复返回结构合法但内容错误（全白）
   的 PNG，`file`/`sips` 这类只检查文件结构完整性的工具测不出这类损坏。
   改用 `uiautomator dump`（小体积文本）交叉验证 + 直接在代码里加像素
   采样/耗时诊断打到 logcat（同样是小体积文本），两条路径这次都比图片
   传输可靠得多，是 NOTES #24 那条教训的延伸——遇到"看起来内容不对"的
   截图，先怀疑传输链路，用文本类信号交叉验证，而不是直接把画面当真。
2. **先觉得是"图片解码慢"，数据摆出来发现是"封面被误判表格、走了完全
   不同的代码分支"**——如果没有直接打印 `hasImages`/`tableRegion` 这几
   个真值，会一直在"CMYK 图片解码路径"这个错误方向里打转（那条分支
   压根没被这一页触发）。逻辑推导（"hasFullPageImage=true 理论上必然
   意味着 hasImages=true"）在这次调查里是对的，但推导对不代表推导出的
   "该走的分支"是真走的分支——**同一页可能命中多个互斥分支里的另一个**，
   这是推导本身覆盖不到的盲区，只有直接打印关键变量的真值才能发现。
3. 这次深挖之前的两个真实修复（CMYK 上限、页脚学习排序）已经装机验证
   生效；封面误判表格这个新发现明确记录、没有动手，避免在没有跟用户
   确认产品取舍之前就仓促调整一个历史上来回调过好几次的敏感判定逻辑。
   全量单元测试 219 条零失败。

## 39. 已修：#38 记录的封面页误判成表格——用户明确授权后动手，只在调用点加了一道"整页图片优先"的判断，没碰 TableGridDetector 本身；顺带发现"加载慢"这半个问题其实没有变快，是另一个更深的原因

**背景**：#38 发现问题后如实记录、没有动手，用户这轮明确说"解决上述问题呗"，
正式授权修复。

### 修法：调用点加一道优先级判断，不碰 TableGridDetector

`loadPageLocked` 里原来是"扫描到线段就无条件跑 `TableGridDetector
.tableRegionOrNull`"，改成`scanHasFullPageImage=true` 时直接跳过表格检测
（`tableRegion = null`），不再调用 `TableGridDetector` 的判定逻辑。理由：
`hasFullPageImage`（图片渲染宽高跟页面宽高的比例）和"矢量线段凑巧组成网格"
是完全不同来源、强弱不同的两个信号——前者是"这一页的主体内容明确就是一张
图"这种具体判断，后者是"一批线段的空间分布巧合符合网格特征"这种间接启发式。
两者本来就该是互斥的页面类型判断，不该在同一页同时命中却选中弱的那个。
这跟 2026-08-21 已经验证过的"图片占满全页时不显示旁边文字"是同一条产品
规则的延伸，不是新的判断，风险因此可控——**没有改动 `TableGridDetector`
内部任何判定阈值**，那是 #17 记录过的、来回调过三轮（其中一轮直接回退）
的敏感区域，这次刻意绕开，只在调用点加优先级。

TDD：先照抄 `图片占满全页时不显示旁边的文字` 那条测试的构造方式，同时叠加
一组凑巧组成 3 横 3 竖网格的填充细矩形（Chromium 风格 `re`+`f`，照抄
`TableGridDetectorTest`"3 列 4 行的规整网格线判定为像表格"那条用例的坐标
构造思路），红灯确认复现误判（图片被裁成两块而不是一整张），加判断后绿灯。
用图片解码结果的原始像素尺寸（120×80，`tiny.png` 真实尺寸）区分"直接抽取
原图"和"整页栅格化再裁剪"这两条分支——后者的尺寸取决于页面尺寸和渲染 DPI，
不可能刚好是 120×80，这个尺寸差异就是两条分支的可观测区别。全量单元测试
220 条（新增这一条）零失败。

### 装机验证：确认走对分支了，但意外发现"加载慢"这半个问题没有真的变快

真机复测这份年报封面页，日志确认表格检测已经被跳过（`表格检测=18-19ms`，
本来就快，跳不跳过时间差异很小）、走进了"整页图片"分支（`decodeImages=
true` 直接抽取内嵌图片，不再是 `PDFRenderer.renderImageWithDPI` 整页栅格化
再裁剪）。像素采样确认两张抽出来的图（1551×2320，约 360 万像素/张）"非白
占比"都是 100%——不是全白/全黑占位图，说明这次是真的解码出了图片内容，
不是空壳。**这部分（图片显示不正常）应该算修好了**，虽然这次没有留一份
"表格分支裁出来的图具体长什么样"的对照样本（#38 当时截图链路不稳定、没能
留证），"应该算"是根据代码路径推断，不是逐像素比对过新旧两版结果。

但装机测出一个意外：`PdfPageAdapter.loadPage(page=1)` 总耗时反而变成了
**19.3 秒**，比 #38 记录的（矢量扫描+表格检测+文字抽取三段共约 7.9 秒，
加上没测过的整页栅格化时间）看起来还要慢，不是"加载慢"问题的第二部分
也顺带修好了。加临时诊断拆开量过（验证完已删）：

- 矢量扫描 4478ms + 表格检测 19ms + 文字抽取 3664ms——这三段耗时量级
  跟 #38 记录的几乎一样（4290/36/3592），确认这是这一页内容流本身的
  固有开销（6508 条矢量段、45 万字节解压后内容），**走哪条分支都躲不掉**，
  不是表格误判本身导致的成本，表格分支和整页图片分支在这三步上完全一样贵。
- 图片解码这一步单独耗时 7298ms（两张约 360 万像素的 CMYK/YCCK 图片）——
  这是自己手写的纯 Kotlin JPEG 解码器（#32/#35 那套，正确性已经用 Pillow
  逐像素验证过，但从没验证过速度）的固有成本，没有硬件加速，对大图必然比
  原生解码器慢一个数量级，这次是第一次在"大图+要求限时打开体验"这个场景
  下真正测出速度数字。
- 三段相加（4478+19+3664+7298=15459ms）离总耗时 19286ms 还差约 3800ms——
  大概率是 `documentLock`（#36 才改成的公平互斥锁，`loadPage` 之间完全
  互斥）排队等锁的时间：`PdfPageAdapter` 的 3 线程池同时在解这份文档相邻
  几页，每页的图片解码都要好几秒，互斥之后这些耗时只能顺序累加、不能并发
  掉。这是 #36 明确记录过、当时就接受了的代价（"这份'3 路并发'从建
  `PdfPageAdapter` 那天起就没有验证过对 `PDDocument` 是安全的，这次实证
  直接推翻了这个假设，不能因为'并发看起来更快'就继续冒数据损坏的风险"），
  不是这次改动引入的新问题，只是这次改动让"图片显示对不对"这个问题被修好
  之后，"这份文档图片解码本身就很贵"这个早就存在但一直被表格误判/加锁两层
  东西挡住没看清楚的成本第一次完整暴露出来。

**这次没有动手**：CMYK/YCCK 解码器的运行速度、`documentLock` 全互斥要不要
改回更细粒度的方案，都是比这次"表格误判"大得多的题目，各自都有明确记录过
的、经过真机实证的理由（解码器速度还没人测过所以没有数据支撑怎么优化；
互斥锁是刚用真实数据损坏实证推翻"多读者安全"假设换来的，不该在没有新证据
的情况下退回去）。如实记录这次发现的完整链路，是否要投入去优化图片解码
速度或者重新设计加锁粒度，留给用户决定。

## 40. 已修：YCCK 反色判断从"固定不反色"改成"固定要反色"——两份旧 fixture 的参考答案原来就是错的，用户反馈"颜色还是不对"揪出来的

**背景**：#39 修完表格误判后用户反馈"图片显示不正常解决了一半，现在显示
的图片颜色不正确"，附了真机截图（人像脸部/西装发黑发绿）。

**排查过程**（完整技术推导见 [JpegDecoder] 类 KDoc"YCCK 支持"一节，这里只
记结论和方法论）：真机 `adb shell run-as` 把这份年报整份拉回本地分析，
`pdfimages` 逐张核对确认第一个反馈的是年报第 6 页整页背景设计图
（transform=2/YCCK，K 通道原始采样值均值 238.7），用当时"YCCK 不存在反色
歧义、固定公式"的实现解出来是纯黑负片，**Pillow 默认解码同一张图同样是
负片**——回头重新核对当初就是拿 Pillow 默认解码当参考、判定"已验证通过"
的两份旧 fixture（`cmyk-ycck-book.jpg`/`cmyk-ycck-subsampled.jpg`），发现
它们的参考答案从一开始就是错的，两份 fixture 分别是一张蓝紫色数字光效
设计图和一张真人颁奖照，默认解码都是看不出内容的深色污渍/负片，反色后
肉眼清晰。用 `pdftoppm`（poppler，独立于 Pillow/libjpeg-turbo 的另一套
实现）把年报整页渲染出来交叉核对，真机当天新查的 11+ 张 YCCK 图片（背景
设计图+真人照混合）没有一张需要"不反色"。

**修法**：YCCK 不再是固定公式也不投票，改成固定整体反色（4 个分量
C/M/Y/K 一起翻转）——比"给 YCCK 也加一套投票机制"这个中途想过的方案更
简单，也更符合目前掌握的全部真实证据（零反例）。两份旧 fixture 的参考
答案用反色后的 Pillow 输出重新生成，新增一份真机验证过的设计图 fixture
（`cmyk-ycck-inverted-design.jpg`，就是那张年报第 6 页背景图）。

**方法论教训**（如实记录，比这次具体的颜色 bug 更值得记住）：跟成熟第三方
库的输出逐像素比对，测的是"我们的实现是否等于参考实现"，不是"参考答案本身
对不对"——这次连续两次被同一类问题绊倒（这次 YCCK 反色、以及
[JpegDecoderCrossValidationTest] 类注释记录的 TwelveMonkeys 那次），核心
都是"这个库处理这类小众数据没问题"这个假设从来没有独立验证过。这次改用
poppler 整页渲染 + 肉眼判断画面是否合理（这批设计图美术风格高度一致）
两条独立信号交叉验证，才敢确定原来"已验证通过"的 fixture 其实是错的。

**没有修完**：装机复测确认 YCCK 修复本身有效（年报第 5 页那组 7 张颁奖照、
第 6 页背景图肉眼确认颜色正确），但同一份文档"Board of Directors"那页另外
两张带 SMask（软蒙版）的头像照片装机仍然发黑——单独把这两张图的原始字节
喂给 [JpegDecoder.decode] 直接验证过，解码器本身给出的是正确的肤色/西装
颜色，说明 bug 不在解码器这一层，在 App 更晚的某个环节（可能跟 SMask 合成
路径有关，同一页里第三张 RGB+SMask 的照片显示正常，走的应该是完全不同的
代码路径）。这次没有继续深挖——用户同一条反馈里明确把"加载速度"列为更高
优先级，这个新发现的 SMask 相关 bug 如实记录，留到下一轮。

## 41. 已修（四轮）：加载慢——合并重复解析、调整执行顺序避免 GC 干扰、图片边解码边预览、把图片解码挪出并发锁，真机四轮都实测有效；单页多图的极端情况仍然没有变快

**背景**：#39/#40 修完颜色问题后用户明确说"优先优化"加载速度。这轮没有
新增用户反馈的 bug，是主动按真机数据一路拆解出来的四个独立改动，每一个
都单独装机验证过。

### 改动 1：矢量扫描/表格检测和图片解码合并成一次遍历

`Session.loadPage` 原来对同一页的 content stream 用自己的
[PageContentStreamEngine] 解析两遍——先 `decodeImages=false` 只扫结构（拿
矢量线段/hasImages/hasFullPageImage 做表格检测），再对有图片的页额外起
一个 `decodeImages=true` 的引擎重新解析一遍顺便解码图片。真机诊断这一遍
"只扫描不解码"的开销本身在复杂页（封面页 6508 条矢量段）要 4+ 秒——两遍
等于白付一次。改成固定 `decodeImages=true`，让"扫结构"这次遍历顺便把
图片也解出来，`if (hasImages)` 分支直接复用这次解出来的结果，不再重新
起一遍引擎。代价：表格分支（改用 `PDFRenderer` 整页栅格化，不用这批解出
来的图）会白做一次解码——真机年报封面页误判表格那次（#39）修好之后表格
分支已经变得少见，这份代价换"每个有图片的页省一次完整重新解析"，权衡
起来划算。真机复测：占满全页的设计图页（第 11 页，全局地球图）从 18.3 秒
降到 5.2 秒。

### 改动 2：文字抽取挪到图片扫描/解码前面

反直觉的一步：先抽文字、再扫描+解码图片，比反过来快——真机对比过两个
顺序，"先扫描解码图片、再抽文字"会让 `stripper.getText` 本身也跟着变慢
（大图页真机复现过 3.6s→5.1s，涨了 1.4 秒），文字抽取这段代码完全没有
改动过，唯一变了的是"这一步之前有没有刚分配过几张大 Bitmap"——推断是
内存压力（GC）在两段本来互相独立的工作之间传导。`stripper.getText` 不
依赖表格检测的结果（下游只是拿 `tableRegion` 去过滤已经抽出来的文字行，
谁先算出来不影响正确性），调整顺序不改变任何行为。

### 改动 3：图片边解码边预览

真机反馈"整页图片的页（尤其是不显示文字的 hasFullPageImage 页）要等这
一页全部图片解完才看到任何东西，转圈十几秒"。给 `Session.loadPage` 加了
一个可选的 `onImageReady` 回调，每解出一张图片就调一次，调用方
（`PdfPageAdapter`）先用它把已经解出来的图片摆到屏幕上（先清空转圈占位
符，之后陆续往下摆），等这次 `loadPage` 整体返回时再用权威结果整体重新
渲染一次（可能包含图片拼接等预览阶段还不知道的调整）。加了一条测试
（`loadPage 图片边解码边回调 每张图片触发一次且在整体返回前完成`）验证
回调次数和"回调必然先于整体返回完成"这个时序契约。

### 改动 4（当天来回调过一次）：把图片解码挪到并发锁外面，`LOAD_POOL_SIZE` 先 3→1 又 1→3

`PdfPageAdapter.loadExecutor` 从 #21 那次真机 131 秒事故起一直是固定 3
线程池，但 `documentLock`（#36 那次真机数据损坏实证换来的完全互斥锁）
早就把整页处理（含图片解码）全部锁住了——3 个线程唯一还剩的效果是"同时
抢同一把锁，谁先抢到由操作系统调度决定"，完成顺序因此跟提交顺序（≈阅读
顺序）对不上。用户反馈"翻页时好几个圈圈同时转，看着乱"，第一次尝试是把
`LOAD_POOL_SIZE` 改成 1（严格 FIFO，完成顺序变得可预测）——但用户紧接着
指出"排队本身还是太长，多页等待时还是好几个圈圈在转"，根子没有解决：
`documentLock` 把真正耗时的图片解码也锁住了，改成 1 只是让"排很长的队"
变得有序，没有让队变短。

真正的解法：`JpegDecoder.decode` 是纯函数（只读一份已经从 `PDImage` 复制
出来的 `ByteArray`，不碰任何 `PDDocument`/`COSStream`），#36 证伪的是
"并发碰同一个 PDDocument"，不是"并发跑纯计算"，两者是完全不同的安全
性质，之前一直没意识到可以把这一步单独拎出来。把 `PageContentStreamEngine`
的 CMYK/YCCK 处理拆成两半（`deferCmykDecode=true` 时只在锁内做"读字节+
基本信息"，真正的 `JpegDecoder.decode` 推迟到 `Session.loadPage` 拿到
结果、锁已经释放之后再做），`LOAD_POOL_SIZE` 改回 3，图片解码这一步
重新有了真正的并行。真机复测：多个大图页的 Phase A（拿锁部分）都降到
几百毫秒量级，不同页面的完成时间开始出现真实重叠（比如两个各要 10+ 秒
的页面前后完成时间只差 600 毫秒，明显是同时在解码，不是排队）。

**这次没有解决、如实记录**：单页本身有多张大图（封面那种，2 张约 360 万
像素的 YCCK 图）的总耗时几乎没变（16.3 秒左右，改动前后几乎一样）——
并发只能让"不同页面互相不用等对方"，救不了"这一页自己要解两张大图"这
件事本身，这是手写纯 Kotlin JPEG 解码器（没有硬件加速）的固有成本，真正
要解决需要给解码器本身提速（比如换更快的 IDCT 算法），是另一个数量级的
工程投入，这次没有做，用户看到装机效果后明确把优先级切回颜色问题，留到
以后再评估要不要投入。

**全量单元测试 222 条零失败**，含新增的图片边解码边回调测试。

## 42. 已修：真机反馈"Board of Directors 头像仍然发黑"——两个独立 bug 叠在一起，先后揪出：ImageStripStitcher 把证件照当切片强行拼接，再是 TableGridDetector 第三次误判

**背景**：#40 修好 YCCK 反色之后，用户反馈同一份年报另一页（Board of Directors）
两张头像仍然发黑。装机排查发现这不是同一个 bug 的残留，是两个完全独立的
问题先后叠加、各自需要单独定位。

### 第一层：ImageStripStitcher 把 10 张证件照当"切片"强行拼接

年报第 5 页那组 7 张颁奖照片修好后，翻到 Board of Directors 页仍然发黑。
真机诊断日志确认字节层面是对的（`JpegComponentCount.of` 返回 4，交给了
`JpegDecoder`），直接用提取出来的原始字节单独调 `JpegDecoder.decode` 也是
正常的证件照，把解码结果导出成 PNG 拉回本地核对过 10 张全部正常——问题
出在更晚的环节。追出根因：`ImageStripStitcher.stitchIfTiled`（NOTES #20
加的，用来把"一页扫描图被切成 6-7 条等宽竖条"拼回一整张）的判断标准只看
"≥3 张、等高、等宽"，没有限制形状——这次 10 张董事头像刚好也是等尺寸
（318×353，高宽比约 1.1，接近正方形），巧合满足了同样的触发条件，被
错误当成"竖条切片"强行横向拼接压扁，效果是好几张脸糊在一起、整体发暗。

修法：加一道高宽比门槛（[ImageStripStitcher.MIN_STRIP_ASPECT_RATIO] = 3
倍）——真实切片样本高宽比约 8.6（125×1078），这次的证件照反例约 1.1，
两者之间空档很宽，3 倍这个阈值不贴着任何一边的边界值。补了两条测试
（真机尺寸的证件照反例、阈值边界值测试）。

### 第二层：TableGridDetector 第三次误判——这次是简介卡片的装饰元素

修完拼接问题后用户反馈"不对"，装机诊断打印表格分支真值，确认这次是
`TableGridDetector.tableRegionOrNull` 把整页误判成表格：3 位董事的简介
卡片，每张有照片边框+姓名标签（竖向排列，X 跨度窄，约 150pt）、职位
标题栏+右侧斜线装饰纹理（横向排列，X 跨度宽，约 511pt），这些线段各自
都是装饰元素，但坐标巧合分布满足"≥3 条独立横线+≥3 条独立竖线，且
包围盒重叠"——跟 #17（页边距装饰边框）、#39（封面设计页）是同一个
检测器的第三种独立触发场景，这次连"整页图片"这种干净信号都没有（照片
不是占满整页，#39 的修法不适用）。

**这次没有回避，直接改了 TableGridDetector 核心算法**（前两次都是绕开
核心算法在调用点做区分，这次真的动了核心逻辑，是这次会话里唯一一次）：
加了"横竖跨度比例检查"——真表格的横线整体跨度和竖线整体跨度应该大致
相等（两者围的是同一个矩形），这次的误判恰恰是横线（511pt）比竖线
（150pt）宽 3.4 倍。用现有全部历史测试反过来验证过这条新检查不会伤到
任何已有场景：真实 3 列 4 行网格比例 1.0，表头略高的正常表格比例 1.0，
连 NOTES #17 那条"用户明确接受的回归"测试（页边距装饰边框）比例也只有
1.1-1.4 倍，仍在阈值（2 倍）内，不受影响——这次瞄准的是"两批线段视觉上
根本不是一回事，只是巧合同区域"这个更极端的模式，跟 #17 撤回的"行列
间距均匀性检查"针对的"同一批真线段+一条不相关的线"是不同性质的问题，
恰好能用不同的几何信号（线段长度/跨度，不是位置分布）分开，这也是
TableGridDetector 类 KDoc 早就提到的"要分清楚需要引入新信息，比如线段
长度"这句话第一次真正被用上。补了真实坐标简化版的回归测试+阈值边界
测试。已知局限：如实记录了"真实表格标题栏明显宽于表格本身"这种反例
理论上可能被误伤，真机还没有撞见过，先如实记录。

**装机验证**：两处都修完后，Board of Directors 三张头像颜色、清晰度都
恢复正常，用户确认"已解决"。全量单元测试 226 条零失败。

**方法论**：这次教训是"同一个症状（发黑）背后可能是完全独立的两个
bug"——第一次以为 YCCK 反色的锅还没背完，装机验证发现最终 PNG 导出
结果实际是对的（证件照本身颜色正确），才意识到发黑不是颜色算错了，
是排版阶段把多张正确的图错误合成/裁剪了。逐层拆开验证（先信字节，
再信单张解码结果，再信最终显示效果）比一开始就假设"还是那个 bug"
更快找到真正的两个独立根因。

**勘误**：#41 提交时代码注释里多处错写成"NOTES #43"（当时 NOTES.md 还
没有 #43 这个条目），这次写 #43 之前顺手改成了正确的 #41——纯文档准确性
修正，不影响任何行为，代码本身没有改动。

## 43. 已修：另一份文件"打开了但没有内容"——JPX（JPEG2000）图片解码失败+"图片占满全页时隐藏文字"这条规则叠加，变成整页空白

**背景**：用户反馈另一份文件打不开，追问后确认是"打开了，但没有内容"——
跟 Board of Directors 那次表面症状（发黑）不同，这次是彻底空白。

### 定位：字节层面完全正常，PDFBox 自己的 stripper 也能抽出真实文字

真机拉回文件分析：`pdfinfo` 显示这是 Internet Archive 扫描的老书
（《Quality Function Deployment》，`Producer: Recoded by LuraDocument
PDF v2.68`），`pdftotext` 能抽出 50 万字符的真实正文，不是没有文字层。
写了一个临时 JVM 测试直接调用 `Session.loadPage`，确认 `blocks=0`——
逐层加诊断日志网格排查：`LineCollectingStripper.writeString` 确认真的
收集到 29 行真实文字（"Originally published as..."这类版权页正文），
`linesToParagraphs` 也确认正常产出 19 个段落，`RunningFooterFilter`
也没有过滤掉任何东西——问题出在更下游：`scanHasFullPageImage=true`，
命中了 NOTES #21 那条"图片占满全页时不显示旁边文字"的规则，把这 19 个
真实段落全部清空了。

### 真正的根因：这台设备解不了 JPX（JPEG2000），但错误规则把仅剩的真内容也清空了

`pdfimages -list` 确认这一页嵌了 JPX（JPEG2000）编码的扫描背景图。
装机诊断确认 `pdImage.image` 抛出 `com.tom_roush.pdfbox.filter
.MissingImageReaderException: Cannot read JPX image: JP2Android is
not installed`——PDFBox-Android 官方就有一个专门配套的可选组件
（`com.gemalto.jp2:jp2-android`）支持 JPX，不是需要从零手写解码器
（跟 CMYK/JBIG2 完全不是一个量级）。核实这个组件的可用性（`WebSearch`+
`WebFetch` 查官方 issue/Maven 仓库，`curl` 直接探测 Maven Central）：
原发布仓库 JCenter 早已关停（2021-2022），`repo1.maven.org` 上确认
没有这个坐标（404），JitPack 对 `ThalesGroup/JP2ForAndroid` 全部
版本/commit 的构建请求都返回 `Error`——添加 JPX 支持这件事本身目前
找不到可靠的引入渠道，单独作为一个投入决策评估，这次没有做。

真正的 bug 是两个"各自合理"的处理叠加产生的空白：
1. JPX 解码失败，`pdImage.image` 抛异常，原来的代码在这里直接
   `return`，图片静默消失（不展示占位图，也不留任何痕迹）。
2. `hasFullPageImage` 判断"要不要隐藏文字"只看**几何**（这张图有没有
   摆成占满全页的样子），不看**这张图有没有解码成功**——对 NOTES #21
   原本那次真机反馈（图片能正常解码，文字是解码失败的 OCR 乱码）这个
   判断是对的，但对这次的新场景（图片解码失败，文字反而是唯一的真实
   可读内容）这个判断是错的，把整页仅剩的真实内容也一起清空了。

### 修法：区分"这张图有没有摆成全页"和"这张图有没有真的解码成功"

`PageContentStreamEngine` 新增 `fullPageImageDecoded` 字段，跟原有的
`hasFullPageImage` 分开：`hasFullPageImage` 保持纯几何语义不变（继续
给 NOTES #39"整页图片优先于表格检测"那条判断用，改动它的语义没必要，
也会让表格分支承担额外的复杂度）；`fullPageImageDecoded` 只在真的成功
解码出像素内容（不是占位图）时才置真，新增的 `addRealImage` 辅助函数
统一处理"解码成功、走两层朝向修正、顺带判断是否计入 fullPageImageDecoded"
这几件事。"要不要隐藏文字"这个决定改成看 `fullPageImageDecoded`（不是
`hasFullPageImage`）——图片解码成功时维持原有行为（隐藏文字，展示图片），
解码失败时改成展示文字，好歹不是空白页。

顺带把"图片解码失败就静默消失"这条路径也改成跟 JBIG2/CMYK 解码失败
一致的"展示诚实占位图"（`图片格式不支持（JPEG2000）`，对 JPX 之外的
其它生成路径失败也统一给通用占位图文案）——这个改动让
`PdfTextExtractorImageTest` 里一条断言"损坏图片被静默跳过"的旧测试
需要更新（`content.images.size` 从 1 改成 2，多出来的是占位图），这是
刻意的行为变化，不是回归，旧断言本来就是在验证一个不符合本类一贯方针
（"不静默消失"）的旧行为。

补了两条真机复现场景的回归测试：`占满全页的图片解码失败时展示文字而
不是空白页`（用 `PdfTextExtractorImageTest` 里验证过的"空 COSStream 触发
`IOException`"手法模拟 JPX 解码失败）、`PdfTextExtractorImageTest` 更新
后的断言。

**装机验证**：这份 Internet Archive 扫描书重新打开，之前空白的页面
现在正常展示真实正文文字（"Using and Promoting Quality Charts..."）+
一张"图片格式不支持（JPEG2000）"的诚实占位图，不再是空白页。全量单元
测试 227 条零失败。

**方法论**：这次教训是排查方法本身——遇到"某个变量的值不对"，不能只
停在"这个变量确实是 true/false"就下结论，要往前一步问"这个变量的值
本身是不是被正确定义的"。`hasFullPageImage=true` 这个诊断结果一开始
看起来像是在确认"规则生效了"，实际上是在暴露"规则的判断依据本身不够
精确"——几何摆放和解码成功是两件独立的事，这次是第一次在真机数据上
撞见两者分歧的场景。

## 44. 已改：加载体验——文字抽完立刻回调展示、当前可见页优先于预加载页

**背景**：用户问了两个问题——"加载方式是一页一页还是几页同时加载？"
以及"为什么会出现几个圆圈同时在转？"。追问目的，用户澄清真实诉求跟
字面问题不一样："目的是减少加载时间，最起码可以先看到一个页面立马
出现"，后面又补一条"优先加载当前页面"。

### 问题一：一页的文字和图片绑在一起回调，图片没解完文字也看不到

`PdfTextExtractor.Session.loadPage` 原来的 `onImageReady` 回调（NOTES
#41 引入，图片边解码边展示）只覆盖了图片，文字块要等**这一页所有图片
都解码完**才会一起通过返回值交给调用方。一页字多图少还好，字少图多
（尤其是那种整页扫描图+少量真实文字混排的文档）时，用户盯着转圈的
时间大半花在等图解码，而其实文字早就抽完了。

**修法**：`loadPage` 加一个新的可选回调参数 `onTextReady`，在
`loadPageLockedPhaseA` 跑完（拿到全部 `DisplayBlock.Text`）就立刻调用，
不等后续图片解码——`PdfPageAdapter` 收到这个回调就先把文字渲染上屏，
图片解码完再用已有的 `onImageReady` 追加。补了一条测试专门验证时序：
`onTextReady` 只调一次，且严格早于 `onImageReady`。

### 问题二：几个圆圈同时转，是三个并发解码任务各自的 loading 占位符

`LOAD_POOL_SIZE=3`（NOTES #41 定的并发度）意味着最多同时有 3 个页面
在解码，每个页面加载完成前都会显示自己的转圈占位符——这不是 bug，是
预加载机制的正常表现（提前解码后面几页，翻页时能秒出），但用户观感
上"好几个圈同时转"容易被误读成故障。解释清楚之后，用户真正的诉求是
"当前正在看的这页应该比预加载的后面几页更快出结果"，这不是解释能
解决的，得改调度顺序。

**修法**：`ThreadPoolExecutor` 原来接的是普通 `LinkedBlockingQueue`
（先进先出，跟可见性无关），换成 `PriorityBlockingQueue` + 自定义
`PrioritizedLoadTask`（实现 `Comparable`）——优先级 = 这一页跟当前
第一/最后一个可见 item 的距离（`RecyclerView.LayoutManager` 实时查，
主线程读，因为 `onBindViewHolder` 本身就在主线程），距离越小优先级
越高；距离相同再按提交顺序（`AtomicLong` 递增）保证同等优先级下先来
先服务，不是随机乱序。这样即使 3 个线程都在忙着解后面几页，只要当前
页的任务一提交，会插到队列前面尽快被下一个空出来的线程捡走，不用等
前面几个预加载任务按提交顺序排队排到它。

**未装机做严格计时验证**：改动本身编译通过、单元测试覆盖了文字/图片
回调时序，真机上装过、能正常翻页，但"当前页确实更快出结果"这个体感
结论没有拿 logcat 时间戳逐条对比验证过，只是代码逻辑上应该成立——
如果日后翻页体感异常，`PrioritizedLoadTask.priority` 的计算或者
`visibleDistance` 的调用时机是最先该复查的地方。

## 45. 已改：UI 美化——按钮/滑杆换色换形，右侧翻页手柄改成点击中央才出现，加文件名

**背景**：一连串用户反馈，同一个方向上分好几次加码：先是"把右侧滑杆
放到最右侧"+"美化顶部的三个菜单"，确认满意后追加"4 个设置滑杆也
优化一下"，然后"右侧滑杆也优化颜色，使其显眼"，然后"右侧滑杆改为
隐藏式，点击中央才显示"，最后参照一张游戏音量滑杆的截图要求"滑杆
圆点改成旋钮质感（外圈描边+中间抓握纹理）"，同一次追加"三个菜单上面
增加文件名"。

### 按钮/滑杆换色

新建 `colors.xml`：`button_primary_bg`（藏青蓝 `#3D5A80`，只用在"打开
PDF"这一个主操作按钮，次要按钮"目录"/"设置"继续用中性灰，拉开视觉
层级）+ 后来加的 `button_thumb_ring`/`button_thumb_grip`（旋钮描边/
抓握纹理用的半透明白）。三个顶部按钮去掉 AppCompat 默认系统阴影
（`android:stateListAnimator="@null"`），换成 `bg_button_primary`/
`bg_button_secondary.xml` 自定义圆角背景。

### 右侧翻页手柄：位置、显隐、颜色三次迭代

手柄贴到内容列最右侧（`layout_marginEnd` 6dp→0dp，注意这贴的是
"内容列"右边缘，不是屏幕物理边缘——外面还套着 `rootLayout` 的
`paddingHorizontal=16dp`，真要贴屏幕物理边缘需要额外挪出这层
padding，这次没做）。颜色从半透明中性灰改成不透明藏青蓝（用户原话
"太低调容易被漏看/摸不准位置"）。显隐机制从"多页文档就常驻显示"改成
默认隐藏、点击屏幕中央区域才切换（`MainActivity.setupCenterTapToggleScrubber`，
用 `RecyclerView.OnItemTouchListener.onInterceptTouchEvent` 里跑一个
`GestureDetector` 单击识别，全程不拦截事件，不影响正常滚动/双指缩放）。

### 滑杆/手柄旋钮化：VectorDrawable 而不是 layer-list

用户参照的参考图（游戏音量滑杆截图）是"实心圆+白色描边+中间几条竖线
抓握纹理"，layer-list 叠 `<shape>` 没法精确摆出"三条等间距竖线居中
排列"（`<item>` 的 `left/right/top/bottom` 是到边界的内边距语义，凑
不出这个效果），改用 `VectorDrawable`（`seekbar_thumb_knob.xml` 给
4 个设置滑杆，`page_scrubber_thumb_knob.xml` 给右侧手柄）直接写
`pathData` 坐标。两者纹理方向不同：设置滑杆是横向拖动配竖线纹理，
右侧手柄是纵向拖动配横线纹理——纹理方向垂直于拖动方向这个视觉惯例
保持一致，只是转了 90 度。右侧手柄顺带把尺寸从 6dp×36dp 的窄条改成
32dp×32dp 的圆（`moveScrubberThumbTo` 用 `view.height` 实时读取，
换尺寸不用改任何拖拽定位计算）。旧的 `bg_page_scrubber_thumb.xml`/
`seekbar_thumb_large.xml` 确认没有别处引用后直接删除，不留死文件。

### 加文件名

`MainActivity.loadPdf` 一拿到 `Uri` 就调用新写的 `queryDisplayName`
（`content://` scheme 查 `ContentResolver`+`OpenableColumns
.DISPLAY_NAME`，查不到退回 `Uri.lastPathSegment`）设置到顶部新增的
`fileNameLabel`（`android:ellipsize="start"`，文件名前面截断保留后半
更有辨识度的部分）并显示——不等文档解析成功才显示，哪怕最后打开失败
也让用户知道刚才点的是哪个文件。

**误会插曲**：中途用户报过一次"看似的 bug"——"滑杆圆点变成方形中间
带线，像真正的按钮"，一度以为是真的视觉回归，装机反复截图复现不出来
（idle 态、模拟拖拽态都是干净的纯色圆点）。后来用户发来参考图才明白：
这不是 bug 描述，是在用文字描述"想要达到的效果"（旋钮质感），只是
表达方式让人误以为在报告一个已经发生的故障。教训：文字描述的视觉
缺陷如果怎么复现都对不上代码改动范围，优先怀疑"这其实是在描述期望
而不是描述现状"，直接要一张参考图/截图，比反复盲猜复现更快。

**装机验证**：文件名正常显示、设置滑杆和右侧手柄的旋钮样式跟参考图
一致、点击屏幕中央能正常切换手柄显隐。全量单元测试 228 条零失败。

## 46. 已改：#45 UI 美化的延续——跑马灯文件名、崩溃修复、滑杆标签配色、沉浸模式扩大到顶栏和设置面板

**背景**：#45 装机验证通过后，用户在同一方向上继续加码，一连串反馈：
"文件名字不够醒目，优化一下；文件名字可以向上移动一些，上面空白太多"
+"4 个设置滑杆加粗，名字不够醒目，名字和滑杆可以使用不同颜色，参照
游戏图"→"文件名字过长是需要滚动显示，避免只看到后半部分"→"滑杆的
横线加粗，参考我上次提供的游戏图"→"拉杆横线颜色不同"→"文件名、三个
菜单设置成同右侧滑杆一样是隐藏的"→（真机反馈）"点击屏幕中央，四个
设置滑杆没有隐藏"。

### 崩溃：`queryDisplayName` 在冷启动自动恢复文档时炸了整个 App

加完文件名当天验证时，冷启动自动恢复"上次打开的文件"直接闪退，
`adb logcat` 抓到 `SecurityException: Permission Denial: reading
com.android.providers.media.MediaDocumentsProvider ... requires that
you obtain access using ACTION_OPEN_DOCUMENT`——这份"上次文件"是从
系统相册选的图，`rememberLastOpenedUriPermission` 早就持久化过读
权限、`copyToCacheFile` 的 `openInputStream` 后来验证也能正常读到
内容，但单独对同一个 Uri 调 `ContentResolver.query()` 仍然被拒。这是
部分 Provider（尤其 MediaProvider 包了一层 DocumentsProvider）对
"持久授权覆盖 openFile 但不覆盖 query"的已知不一致行为，不是这份代码
哪里没做对，但代价是这行代码毫无防御地跑在 `onCreate` 的主线程调用链
上，一炸就是整个 App 闪退。修法：`query()` 调用包一层 `runCatching`，
查不到（无论是列名找不到还是异常）都走同一条退回
`Uri.lastPathSegment` 的兜底路径。顺带验证了这一发现——修复后这份
坏掉的"上次文件"记录会在下一次 `loadPdf` 的 Error 分支被现有的自愈
逻辑（`LastOpenedFileStore.clear`）自动清掉，不需要额外处理。

### 文件名：加粗提亮、上移、跑马灯滚动

`label_text_emphasis`（87% 不透明黑，加粗，15sp）替换原来的
半透明浅灰 13sp；`rootLayout` 的 `paddingTop` 16dp→8dp 减少顶部
留白（跟状态栏之间已经有 `applySystemBarInsetsAsPadding` 动态加的
安全间距，这 16dp 是额外叠加的固定值，缩小它不影响避让效果）。长
文件名从"省略号放最前面只保留后半截"改成安卓标准跑马灯
（`ellipsize="marquee"` + `marqueeRepeatLimit="marquee_forever"` +
`focusable`/`focusableInTouchMode`），另外在 Kotlin 里手动设
`isSelected = true`——marquee 是"选中态"才滚动的效果，纯 XML 属性
不会自己动，这是安卓 TextView 跑马灯最常踩的坑。

### 4 个设置滑杆：标签加粗跟滑杆本身拉开颜色层次，轨道加粗+四色区分

标签统一加粗 15sp + `label_text_emphasis`，跟滑杆本身的强调色形成
"黑色加粗标签+鲜艳颜色滑杆"的层次（参照参考图）。轨道从系统默认的
细线改成 8dp 粗、两端圆角（`seekbar_track_thick.xml`，
`android:progressDrawable`）。用户进一步要求"拉杆横线颜色不同"——
参考图里"游戏音量"紫、"话筒音量"红，每条滑杆有自己独立的强调色，不
是全部统一藏青蓝。`VectorDrawable` 不支持运行时传参染色
（`android:tint` 对多色 vector 会把白色描边/抓握纹理一起染掉），所以
4 种颜色对应 4 份单独的 track/thumb drawable 文件（`_purple`/
`_coral`/`_teal` 后缀 + 原来不带后缀的藏青蓝给"字号"），不是应该
避免的重复，是这个具体约束下最简单能用的办法。

### 沉浸模式从"只管右侧手柄"扩大成"统一管顶栏+文件名+设置面板+手柄"

用户要求"文件名、三个菜单也设置成同右侧滑杆一样是隐藏的"——`
scrubberRevealedByTap` 改名 `chromeRevealedByTap`，`updateScrubberVisibility`
改名 `updateChromeVisibility`，新增 `topButtonRow`（给顶部按钮行的
`LinearLayout` 补一个 id）纳入统一控制。**`topButtonRow` 有一条例外**：
`currentSession == null`（冷启动/没有文档在看）时无条件常驻显示，
不受点击状态支配——不然用户连"打开 PDF"这个入口都点不到。

装完之后用户真机反馈"点击屏幕中央，四个设置滑杆没有隐藏"——这暴露了
一个我漏掉的死角：设置面板（`settingsPanel`）本来是靠专门的"设置"
按钮独立展开/收起的，`toggleSettingsPanel`/`collapseSettingsPanel`/
`expandSettingsPanel` 原来直接读写 `settingsPanel.visibility` 当
唯一的状态来源，完全没有接入这次的沉浸模式机制。修法：新增独立字段
`settingsPanelExpanded` 记"用户展没展开过"这个意图，`settingsPanel`
最终显不显示是"用户展开过"和"沉浸模式没被收起"两个条件的交集，
`collapseSettingsPanel`/`expandSettingsPanel` 只改状态+按钮文案，
不再直接摸 `visibility`，统一交给 `updateChromeVisibility` 算——这样
用户点屏幕中央收起时设置面板跟着一起隐藏，再点一次呼出时如果之前
展开过也会跟着一起恢复，不需要重新点一次"设置"按钮。`settingsPanel`
在 XML 里的初始 `android:visibility` 也补成 `gone`，跟 Kotlin 里
`settingsPanelExpanded` 默认 `false` 保持一致，不再依赖
`collapseSettingsPanel()` 在 `onCreate` 里跑一次才收起（那一行现在
命中早退分支，是个空操作）。

**装机验证**：崩溃修复后冷启动自动恢复文档不再闪退（真机确认 Toast
优雅报错，下次冷启动自愈）；文件名跑马灯、4 色滑杆轨道、点击屏幕中央
统一隐藏/呼出顶栏+文件名+设置面板+手柄，均装机确认符合预期。全量
单元测试 228 条零失败。

## 47. 已改：系统深色模式适配——两次绕开尝试后，正经建了一份 values-night

**背景**：#46 的文件名/滑杆标签改成加粗深色文字后，用户反馈"文件名和
拉杆标签在夜间模式自动变为反色"。

### 第一次尝试：关掉 force dark——解决了错位，但暴露了更底层的问题

真机截图确认反色确实存在，第一反应是安卓系统的"智能深色"（force
dark，API 29+，目标 SDK ≥29 默认开启）：系统看到这个 App 的主题走
Day 分支，会对渲染结果做启发式反色让它"看起来像深色"，但只对它认为
是"文字"的部分动手、不动写死颜色的背景 Drawable，容易造成文字被
反色而背景没变的错位。加了 `android:forceDarkAllowed="false"`（API
29+ 属性，`minSdk=26` 需要 `tools:targetApi="q"` 才能过 lint）关掉
这个猜测。装机验证错位确实消失了，但用户又反馈"文件名和标签颜色是
黑色，不易看清"——这说明关掉 force dark 只是掩盖了一层，底下还有
问题没解决。

### 真正的根因：`rootLayout` 没写背景色，露出了 AppCompat 自己会切换的窗口背景

`rootLayout` 本身没有 `android:background`，夜间模式下会露出
`Theme.AppCompat.DayNight` 自己的窗口背景（`android:windowBackground`，
AppCompat 的 DayNight 委托机制会把它切成深色/黑色，这是主题解析层面
真的换了颜色，不是渲染猜测），黑色窗口背景配写死的近黑色文字
（`label_text_emphasis` `#DE000000`），就是黑底黑字看不清。

### 中途绕路：临时锁死 Light 主题，被用户否决

当时判断"这个 App 从设计上就没打算支持系统深色模式，所有颜色都写死"，
把父主题从 `Theme.AppCompat.DayNight.NoActionBar` 换成不跟随系统的
`Theme.AppCompat.Light.NoActionBar`，图省事绕开问题而不是解决问题。
装机验证文字确实清楚了，但那是因为窗口整体维持白天配色，用户看出来
"清晰，但不是夜间模式，而是白日模式"，明确要求"设计系统深色模式，
干就完了"——回绝了绕开方案，要求正面解决。

### 正解：只覆盖真正需要变的那一个颜色，不是整份复制 colors.xml

主题改回 `Theme.AppCompat.DayNight.NoActionBar`（`forceDarkAllowed
=false` 继续保留，我们自己做对了颜色就不需要系统再叠加一层猜测）。
新建 `values-night/colors.xml`，逐条盘点这个 App 用到的所有颜色，
确认真正需要深色变体的只有 `label_text_emphasis` 一个：
- 正文段落文字没有写死颜色（`createParagraphTextView` 不设
  `setTextColor`），继承主题的 `?android:textColorPrimary`，
  `DayNight` 本身就会切换，不用管。
- 按钮/滑杆的强调色（`button_primary_bg`/`accent_*`）本来就跟页面
  背景无关——按钮自己是纯色块，滑杆旋钮画在自己的圆面上，深浅色背景
  下对比度都不受影响。
- 设置面板/次要按钮的半透明中性灰背景（`#33888888` 这类）本来就是
  半透明叠加色，会随底色自动变深/变浅，这是它最初的设计意图（见
  `bg_settings_panel.xml` KDoc "深色/浅色主题通用"），不需要重复
  定义。

只有 `label_text_emphasis` 是唯一一处"直接把深色文字写死在可能变成
深色的背景上"的地方，`values-night/colors.xml` 里翻成 87% 不透明白，
跟浅色模式的 87% 不透明黑对称。

**方法论**：遇到"这个 App 没做过深色适配，出问题了"，第一反应不该是
"那就让它别参与深色模式"（治标，用户也确实当场否决了）；应该反过来
盘点"这个 App 已经用了多少个颜色、其中哪些是真正依赖背景明暗的深色
文字/浅色背景假设、哪些本来就是自适应的（主题属性、半透明叠加色、
自带对比度的强调色块）"——多数情况下需要显式覆盖的颜色集合比想象
中小得多，这次 8 个自定义颜色里只有 1 个真的需要 `values-night`。

**装机验证**：夜间模式下文件名和滑杆标签清晰可读，白天模式不受影响。
全量单元测试 228 条零失败。

## 48. 已修：JPX/JPEG2000 图片真正解码——#43 那条"不可引入"的结论过时了，用户三轮核实纠正后接入 `jp2-android`

**背景**：用户明确要求"投入完整实现" JPX/JPEG2000 支持，一开始按 #43 记录
的结论理解为"只能从 ISO/IEC 15444-1 规范手写 DWT+EBCOT 解码器"——规划了
一份完整的分阶段手写路线图（复用 JBIG2 现成的 MQ 算术解码器、marker 解析→
Tier-1→Tier-2→逆 DWT→色彩变换，逐阶段 TDD），提交计划审批时被用户当场
否决：**#43 记录的"`com.gemalto.jp2:jp2-android` 不可引入"这条结论已经
过时**。

### 用户三轮核实，一次比一次深

第一轮：用户查到新坐标 `io.github.michaldvorak-gemalto:jp2-android:1.0.4`
已经登陆 Maven Central，建议先做接入 POC，手写方案降级为后备。核实后确认
属实——上游项目（`ThalesGroup/JP2ForAndroid`）解决了发布问题，换了个人
维护者坐标重新发布，最新版本 1.1.0（2026-08-16）。

第二轮：用户指出我把"1.1.0 卡在 `minCompileSdk=37`"直接等同于"整条路线
失败"是错的——静态检查 1.0.5（同一坐标的上一个版本）发现它 `minCompileSdk
=1`，没有这个约束，只是少了 `setDecodingArea()`（区域解码，PDF 图片解码
用不上）。核实后确认属实，POC 矩阵改成优先测 1.0.5。

第三轮：用户指出"临时单元测试或 main 函数"验证 native 解码这个思路本身
有问题——`JP2Decoder.decode()` 依赖 Android ABI 的 native `.so`，Robolectric
纯 JVM 单测环境加载不了，必须用 instrumentation test 或真机；还指出
native crash 风险（`runCatching` 拦不住 `SIGSEGV`）要前置到 POC 阶段验证，
不能拖到正式接入后的补测试阶段；还要求 POC 至少测三类输入（正常/截断/
伪造头部数据）。全部采纳，重写了 POC 矩阵和验证方式。

三轮都是用户先给出具体、可核实的技术依据（Maven Central 元数据链接、AAR
`aar-metadata.properties` 静态检查、native 库运行环境的技术事实），不是
凭印象反对，每次核实后确认用户是对的。

### POC 阶段（真机装机验证，非纸面推测）

1. `implementation("io.github.michaldvorak-gemalto:jp2-android:1.0.5")` +
   `compileSdk=36`：`gradle assembleDebug` 直接成功，没有任何 SDK 约束，
   第一条路径就通过，不需要测 1.1.0 或升级 `compileSdk`。
2. 网络可达：这个新 groupId 通过项目现有的 Maven Central 仓库配置直接
   解析下载，没有额外镜像配置需求。
3. native 稳定性（真机装机验证，加临时诊断代码到 `MainActivity.onCreate`，
   验证完整删除干净）：
   - 正常 JP2 数据（`opj_compress` 生成的 16×16 灰阶无损小图）：解码成功，
     像素值跟 macOS `sips`（Core Graphics/ImageIO，跟 OpenJPEG 完全独立的
     另一套实现）解码结果逐像素一致。
   - 截断的数据流（正常文件砍掉后 40%）：稳定返回 `null`，无崩溃。
   - 伪造 magic number+随机字节冒充 JPX：稳定返回 `null`，无崩溃。
   - 全程没有出现不可控的 native crash（进程没有被杀掉）。
4. 真实样本：从设备上重新找到并拉取 NOTES #43 那份 Internet Archive
   扫描文档（`content://.../document%3A1000033972` 反查 MediaStore 拿到
   真实文件路径 `/storage/emulated/0/Books/Quality function deployment...
   .pdf`，`adb exec-out cat` 二进制安全读取），从 PDF 里手动定位并提取
   `/Filter/JPXDecode` 那个 image XObject 的原始字节（3590 字节，931×1250，
   RGB）。真机解码成功，尺寸正确；导出真机解码结果为 PNG 拉回电脑，跟
   macOS `sips` 独立解码结果做全图逐像素比对——116 万像素里约 35% 存在
   ±1 的最小误差，最大单通道差值为 1，符合"两套独立实现在色彩空间转换
   舍入策略上的正常差异"，不是内容错误（这份数据是有损压缩，跟前面
   无损小 fixture 的"完全一致"不是同一个基准）。
5. APK 体积增量约 4MB（4 个 ABI 的 `libopenjpeg.so`），真机是小米
   mondrian（纯 ARM64），需要时可以用 `ndk.abiFilters` 只保留
   `arm64-v8a` 瘦身，这次没做（体积增量可接受）。

POC 四项判定标准全部通过，且是矩阵里风险最低的第一条路径就通过，不需要
启动手写 DWT+EBCOT 方案。

### 正式接入

新增 `Jpeg2000Decoder`（`app/src/main/java/app/pdfreader/extract/
Jpeg2000Decoder.kt`），对齐 `JpegDecoder` 的既有契约（`decode(bytes):
Bitmap?`，`runCatching` 兜底，范围外/失败一律 `null`）。`PageContentStream
Engine.drawImage` 新增 `pdImage.suffix == "jpx"` 专属分支（放在原来的
`decodeJpegWithNativeSubsampling`/`pdImage.image` 通用兜底之前）：
`createInputStream(listOf("JPXDecode"))` 取原始字节（读取时机跟 JBIG2 用
`createInputStream(listOf("JBIG2Decode"))` 是同一个模式——在真正的解码
过滤器之前截断，拿到还没被 PDFBox 尝试解码的原始压缩数据），交给
`Jpeg2000Decoder.decode`，成功走 `addRealImage`（联动 `fullPageImageDecoded`，
图片解码成功时正确隐藏 OCR 乱码文字），失败退占位图（不调用
`orientImage`，理由跟 JBIG2/CMYK 占位图分支一致）。

### 测试：两层覆盖，Robolectric 测 wiring，instrumentation test 测 native 解码本身

`Jpeg2000Decoder` 依赖 native `.so`，Robolectric（纯桌面 JVM）加载不了
Android ABI 的 ELF 二进制——这是 POC 阶段就确认过的硬约束，两类测试分开：

- **Robolectric 单测**（`PdfTextExtractorImageTest`新增一条）：验证
  wiring 对不对（suffix 判断触发、字节正确提取、解码失败时占位图长宽比
  正确）。这条测试里 `Jpeg2000Decoder.decode()` 会因为 native 库加载
  不了而必然返回 `null`（`UnsatisfiedLinkError`/`NoClassDefFoundError`
  都是 `Error` 的子类，`runCatching` 能兜住），测的是失败路径下的降级
  行为，不是真正的解码正确性。
- **instrumentation test**（新增 `app/src/androidTest/` 整套基础设施——
  `testInstrumentationRunner`、`androidx.test.ext:junit`/`androidx.test
  :runner` 依赖、`Jpeg2000DecoderInstrumentedTest`）：跑在真机真实
  Android 运行时上，覆盖 POC 阶段验证过的四类场景（正常数据像素正确、
  截断流/伪造头部返回 `null`、NOTES #43 真实样本尺寸正确），`gradle
  connectedDebugAndroidTest` 或 `adb shell am instrument` 触发。真机验证
  4 条全过。

**小插曲**：`gradle connectedDebugAndroidTest` 这条路径被这个项目一贯的
USB 连接不稳定问题坑了两次（跑到一半设备掉线，UTP 测试编排对连接中断的
容忍度比单条 `adb` 命令低很多，直接判定"设备找不到"整个失败，不会自动
重试）。改用更抗断线的路径——`adb install` 手动装两个 APK（主 APK+
androidTest APK），再 `adb shell am instrument -w app.pdfreader.test/
androidx.test.runner.AndroidJUnitRunner` 直接触发，一次成功。

### 文档措辞：不过度背书、不过度承诺

`CLAUDE.md` 已知局限一节里"JPX/JPEG2000 完全不支持"的描述整条移除，
改成如实反映来源和范围的措辞——不写"完整支持 JPX"（`jp2-android` 本身
也有自己的边界，比如 1.0.5 没有区域解码、HTJ2K 支持程度没有针对性验证
过），不写"官方库"（这是个人维护者重新发布的坐标，不是 Thales 官方
持续担保的渠道）。

**方法论**：跟 [[feedback_stale_unavailability_claims]] 记录的教训一致——
"某组件不可引入"这类结论是有时效性的，尤其是小众/个人维护的开源库，
换发布者、换坐标、修复完打包问题重新发布，几周之内就可能让旧结论过时。
这次是这条教训从"记下来"到"真的被验证复现一次"——不是自己主动重新
核实发现的，是被用户的独立核实连续纠正三轮才走到正确路线上，且每一轮
用户给出的都是具体可复核的技术依据，不是印象或直觉。

**装机验证**：NOTES #43 那份文档重新打开，`logcat` 确认 `loadPdf
render(Success)` 无崩溃；instrumentation test 4 条全过（含对这份文档
真实提取字节的解码验证）。全量 Robolectric 单元测试 229 条零失败（新增
1 条 JPX wiring 测试）。

## 49. 已修：JPX 接入当天连环三轮真机回归——图片"隐藏文字"太粗暴、跳过图片"占地方"、封面缺内容，最终换掉渲染引擎才收尾

**背景**：#48 接入 `jp2-android` 装机验证通过（真机截图看到灰色扫描
背景图）当天，用户带着更严格的标准继续复测，连续揪出三层新问题，一层
比一层更深，最终发现根因不在我们自己的解码器，而在 PdfBox-Android 这个
底层库本身的能力边界，换了一套完全不同的渲染引擎才彻底解决。

### 第一轮：图片"解码成功"不等于"值得为它隐藏文字"

真机反馈"大部分是文字，少部分还是灰色页面"——追出这份 LuraDocument
双层扫描文档每页其实有两张 JPX 图：一张 931×1250 的低分辨率背景（接近
纯色，标准差实测约 3.1），一张 2792×3748 带 JBIG2 蒙版的"前景层"。原来
"图片占满全页且解码成功就隐藏文字"这条规则（NOTES #43 定的）对这两张图
一视同仁——只要解码成功就隐藏文字，展示接近纯色的背景图，比之前"占位图
+文字"的旧行为还差。

修法：`PageContentStreamEngine.hasVisibleContent`（20×20 网格采样算亮度
标准差，阈值 12，见该函数 KDoc）——占满全页的图片只有"真的有内容"才隐藏
文字，接近纯色的背景图不再让文字消失。

### 第二轮：不隐藏文字还不够，图片本身也不该占地方

第一轮修完，装机复测发现新问题：接近纯色的背景图虽然不再隐藏文字，但
仍然作为一个展示块加进页面，按原始像素尺寸等比缩放显示，占的屏幕空间
远大于旁边可能只有两三行的真实文字——翻到这页第一眼看到的还是一大片
灰色，文字要往下滚很久才看得到，观感上还是"这页是灰的"。

修法：`addRealImage` 改成"占满全页+没有实际内容"时这张图整个跳过，不
调用 `addImage`，不只是不隐藏文字。新增 `hasSkippedFullPageImage` 字段
记录"这一页有没有出现过一张被跳过的全页图片"，为第三轮的判断做铺垫。

### 第三轮：封面这类"设计全靠图片承载"的页面，被跳过的图片就是唯一内容

第二轮的策略——"跳过接近纯色/带不支持蒙版的全页图片"——对绝大多数内页
是对的（真实内容早就在独立抽取的文字里），但真机截图对比 poppler 独立
渲染发现对封面是错的：封面的副标题"Integrating Customer Requirements
into Product Design"、编者署名"Yoji Akao, Editor"、网格线，全靠"JPX
背景+JBIG2 蒙版裁出锐利图形"这套机制画出来，没有任何独立可提取的 PDF
文字对象兜底，被第二轮的规则跳过之后这些元素凭空消失，只剩一坨深色
背景+红色"QFD"字样。

**为什么带蒙版的图片会被跳过、又为什么不能简单地"跳过时也栅格化"**：
JBIG2 蒙版是 PDF 的 stencil masking 机制——蒙版抠掉的区域露出的是这张
图下方另外画的内容（内页是黑色文字填充，封面是红色/其它设计元素），不
知道下方画了什么就没法正确合成；"真正通用"的合成本质上等于要完整
重现页面的绘制顺序，等同于整页栅格化，不是两张位图简单叠加能做到的。

**第一次尝试栅格化方案不成立**：想复用表格分支已经在用的 PdfBox
`PDFRenderer.renderImageWithDPI` 整页栅格化封面——真机导出栅格化结果
逐像素比对，发现**跟我们自己手写的 drawImage 路径缺的是同一批内容**：
`PDFRenderer` 内部渲染管线一样依赖 PdfBox-Android 自己的图片解码能力，
而 PdfBox-Android 从来没有实现过 JBIG2 解码（这正是这个项目当初要
手写 `Jbig2GenericRegionDecoder`/`Jbig2SymbolTextDecoder` 的原因，见
NOTES #24-27）——JPX 本身能解码了（`Jpeg2000Decoder`），但 PdfBox 自己
的整页渲染器一样卡在 JBIG2 蒙版这一步，这条路从根上不通。

**判断"该不该栅格化"的信号也踩了一次坑**：最初用 `filtered.isEmpty()`
（段落数是否为零）判断"这一页有没有真实文字"，真机数据打脸——封面页
真实抽出了一个孤立的噪音字符"o"（扫描/字体渲染噪音，不是真内容），
`filtered.size` 因此是 1 不是 0，`isEmpty()` 判不出来，误判成"有文字"
不触发栅格化。改成按去除首尾空白后的真实字符数判断（阈值 10，`o` 只有
1 个字符，远低于内页动辄几百字符的真实段落，见
`MIN_REAL_TEXT_CHARS_FOR_FALLBACK_SKIP` KDoc）。

**真正的解法：换成 Android 系统自带的 `android.graphics.pdf.PdfRenderer`
（pdfium 引擎）**——用户明确要求"继续投入，查有没有其它渲染引擎可用"
之后，没有靠搜索下结论，直接在真机上用这份真实文件跑了一次 PoC（临时
诊断代码，测完即删）：pdfium 完整正确渲染出了封面（标题/QFD 图标/副
标题/网格线/编者署名全部正确，跟 poppler 独立渲染逐像素级别一致）。
Chrome 用的就是 pdfium，对 JBIG2/蒙版这类复杂特性的支持是成熟工业级
实现，不是这个项目要自己解决的问题。

`Session` 新增 `file: File` 字段（`android.graphics.pdf.PdfRenderer`
要吃 `ParcelFileDescriptor`，不能直接用已经解析好的 `PDDocument` 对象，
这也是要多存一份文件引用的原因）。`renderPageWithAndroidPdfRenderer`
每次调用新开一个 `PdfRenderer`/`ParcelFileDescriptor`，用完即关，不
跨 `loadPage` 调用缓存复用——这条兜底路径命中频率很低，没必要为了省
这点开销处理"pdfium 是否线程安全"这类复杂度。

**装机复测确认（用户主动提出"还有灰色页面，比如第二页和第三页"）**：
分别查过这两页的 `pdftotext`/`pdfimages` 结构，跟封面页结构一样（小
背景图+带蒙版大图，零真实文字），拿 poppler 独立渲染这两页——**poppler
自己渲染出来也是同样的纯灰色**，确认这两页在原书里就是真正的空白衬页
（扫描书封面和正文之间常见的空白页），不是新的 bug，App 显示灰色是
完全正确的结果。

**测试**：Robolectric 环境下 `android.graphics.pdf.PdfRenderer` 依赖的
系统 PDF 渲染服务不可用（`renderPageWithAndroidPdfRenderer` 内部
`runCatching` 吞掉失败返回 `null`，不会崩溃），两条栅格化兜底测试
断言改成测"没有走回旧的展示逻辑"（不该出现文字块/噪音字符），不测
"真的拿到栅格化图片"——这条结论只能在真机验证，已经用真机截图逐一
核对过（封面、内页文字、两张真正的空白衬页）。

**方法论**：连续三轮回归都是"用户装机复测揪出、不是自己主动发现"，
每一轮都通过真机截图/独立渲染工具（poppler）交叉验证，而不是靠代码
逻辑推理"应该没问题了"。第三轮换渲染引擎这一步尤其关键——遇到"自己的
实现和这个平台的官方实现都卡在同一处"时，第一反应不该是继续在同一套
底层库上打补丁，而是该问"这个平台还有没有别的、更成熟的实现"，Android
系统自带、每天被数十亿次调用的 pdfium 显然比这个项目自己练手写的
JBIG2/JPX 解码器更值得信任来处理这类复杂 PDF 特性。

**装机验证**：封面（NOTES #43 那份文档第 1 页）真机截图确认标题、QFD
图标、副标题、网格线、编者署名全部正确显示；第 2/3 页确认是真正的空白
衬页，poppler 独立渲染交叉核对过。全量单元测试 232 条零失败。

## 50. 已修：跟 #48 完全不同的另一本真实文档——设计页被误判成表格，CMYK 缩略图颜色全错

用户反馈一份完全不同的真实文档（"Ansys Optics光学与光子学设计仿真软件
集合v3.pdf"，不是 #43/#48 的 QFD 测试书，中途一度用旧的 content URI
误测了 QFD 书，靠 `run-as ... cat last_opened_file.xml` 读出真实
content URI 才纠正过来——**排查前先确认用户说的是哪份文件，不能想当然
沿用手头正在测的那份**）："第一页无法显示，第二页颜色不对"。

**第一页**：确认是既有的、文档化过的 `MAX_CMYK_JPEG_PIXELS`（600 万像素）
安全阀在正常工作——这张图真实体积 14,110,524 像素，超限后按设计展示
"图片过大，暂不支持解码（CMYK JPEG）"占位图，不是 bug。

**第二页**：poppler 独立渲染确认应该是白底、橙黑标题、橙色分类标签、
正常配色的缩略图；真机上却是黑底蓝字。三层临时诊断日志（`drawImage`
的宽高/CTM、`extractCmykJpegOrNull` 的声明分量数 vs 实际探测分量数、
`JpegDecoder.decodeInternal` 的帧解析结果）逐层排查，关键线索是
**第 2 页的 10 张 CMYK 缩略图全部成功走到 `extractCmykJpegOrNull`
（实际探测分量数=4），但没有一张后续出现 `JpegDecoder` 的解码日志**——
对照第 3 页（同一份文档，CMYK 图片正常出现 `JpegDecoder` 日志、
`isYcck=true invert=true` 正确解码），说明第 2 页的这批图片压根没有走到
真正解码那一步，中途被别的分支截胡了。

追到 `loadPageLockedPhaseA` 里的表格分支：加一行诊断日志打印
`tableRegion`，第 2 页命中的 `TableRegion` 精确等于整个页面 MediaBox
（`minX=minY=0`、`maxX=595.276 maxY=807.874`，正好是页面宽高）——
`TableGridDetector` 把这个设计页（多个缩略图配细边框/装饰线条/背景色块，
12872 条矢量段）里凑巧出现的"≥3 横+≥3 竖、边界框重叠"再次误判成表格
（跟 NOTES #39 是同一类"矢量线段凑巧像网格"误判，但 #39 的修复只覆盖
"整页刚好是一张图片"这一种情况，这次是完全没有占满全页图片的设计页，
没被那条修复挡住）。命中表格分支后，代码把整页交给 **PdfBox-Android
自己的 `PDFRenderer`** 栅格化再裁剪——而这条渲染管线根本不认识
DeviceCMYK（真机日志反复出现"Unsupported color space kind:
DeviceCMYK. Will try DeviceRGB instead"），把 CMYK 四个分量的原始
采样值直接当 RGB 读，颜色因此全错。

**第一版修复**（未采用，走过一次真机验证才发现问题）：判定"表格区域
四条边都跟页面边界零边距贴死"时直接把 `tableRegion` 置为 `null`，
让代码落回下面"逐张小图 + 文字段落"的正常 reflow 分支——单元测试通过，
但真机复测发现观感反而更差：这一页的背景色块、装饰线条本来就是矢量
图形，reflow 抽取模型从设计上就只认"文字段落"和"内嵌图片"两种内容，
完全没有"矢量填充"这个概念，10 张互不相关的缩略图纵向罗列、丢光所有
版式关系，整页看起来是一整块灰色——比错误的颜色更没用。这是本次排查
里第二次靠真机截图纠正一个"单元测试通过就以为对了"的判断，再次印证
"验证要打在真实调用路径上"（见 `feedback_verify_on_real_path` 记忆）。

**最终修复**：判定表格区域精确覆盖整页时，不落回 reflow 分支，也不用
PdfBox 自己的 `PDFRenderer` 裁剪，改用 NOTES #48 已经验证过的
`renderPageWithAndroidPdfRenderer`（Android 系统自带 pdfium）直接整页
栅格化——pdfium 同样不受 DeviceCMYK 这个坑影响，这本质上是 #48"遇到
这个平台自己的实现和第三方库卡在同一处时，该问平台还有没有更成熟实现"
这条方法论的又一次应用，只是这次触发条件从"JBIG2 蒙版"换成了"矢量线段
误判表格"。判定阈值（`TABLE_REGION_FULL_PAGE_REJECT_RATIO=0.95`）刻意
比 `FULL_PAGE_IMAGE_COVERAGE_RATIO`（0.7）更高——只拦"精确等于整个
MediaBox"这种极端形状，不误伤"表格确实很大、占了页面七八成"的正常
情况。没有改 `TableGridDetector` 本身的判定条件（NOTES #17 记过那是
来回调过好几次的敏感区域），只是在判定结果之上加一层"这真的是一张
只占页面一部分的表格吗"的合理性检查。

**测试**：跟 #48 一样，Robolectric 环境下 `android.graphics.pdf.
PdfRenderer` 不可用，新增测试只能断言"没有走回旧的两条错误分支"
（不是表格裁剪分支产出的 ~611px 裁剪图，也不是 reflow 分支产出的
120×80 小图+文字段落，Robolectric 下这条分支直接是空列表），真机
截图是实际正确性的证据——已经跟 poppler 独立渲染逐项核对过（Ansys
logo、标题、三个橙色分类标签、缩略图配色全部一致）。

**装机验证**：第 2 页真机截图确认白底、Ansys logo、橙黑标题"Ansys
Optics 从微观到宏观全尺度光学仿真平台"、三个橙色分类标签
（Lumerical/Zemax/Speos）、缩略图配色全部正确，与 poppler 独立渲染
逐像素观感一致。全量单元测试 233 条零失败。

## 51. 已修：一页多个独立图文小节时，图片统一堆在页面最后，跟各自的说明文字脱节

用户反馈"这本书有页面图片和文字分开了"，真机确认是另一本真实文档
（"Ansys电子设计解决方案.pdf"）——跟 #50 是完全不同的文档、不同的
bug。真机截图看到的现象：一页正文（"无线电系统/RF 电路"，2 列×6 个
独立小节，每个小节自己的标题+说明文字+1~3 张示意图）里，所有小节的
文字先连续显示完，图片全部堆在这一页最后，跟各自本该配的说明文字
完全脱节，读起来图文对不上。

**根因**：`Session.loadPage` 里 `PageContentStreamEngine` 扫描图片时
只记了"这张图属于哪一页"，[ImagePlacement] 只有"按页归类"这一种插入
逻辑（`afterParagraphIndex`，把一页所有图片统一插在这一页最后一个段落
之后）——这个简化对"每页只有一张主图"的文档看不出问题（NOTES.md 早期
决策记过"不追求图片精确嵌入原位置"），但对一页塞了多张独立配图的文档
（这次真机反馈 6 张）就会把图文关系整个打散。

**修复**：给 `PageImageResult`（`Ready`/`Pending` 两个分支）都加上
`topY`（图片在页面坐标系里距页顶多少 pt，从 CTM 的 `translateY`/`scaleY`
换算——`pdfTopY = translateY + max(0, scaleY)`，再套用本类反复用到的
`pageHeight - pdfY` 换算成"距页顶多少 pt"）；`ImagePlacement` 新增
`afterParagraphIndexByTopY`（单页版的 `afterParagraphIndexForRegion`，
不用比较页码，纯按纵坐标算插入点）；`Session.loadPage` 里原来"文字全部
追加 + 图片全部追加"两段简单拼接，改成新写的 `interleaveTextAndImages`：
按每张图片自己的 `topY` 分别算出插入点，插回紧跟它自己那段文字后面。
`ImageStripStitcher.stitchIfTiled` 是"要么原样返回、要么全部拼成一张"
的全有全无契约（不存在部分拼接），拼接后用参与拼接那批图片里最靠上的
`topY` 代表整张拼接图的位置，不需要额外处理"拼接后数量变了、topY 怎么
对应"这个问题。

**测试**：`ImagePlacementTest` 补了 `afterParagraphIndexByTopY` 的纯逻辑
测试；`PdfTextExtractorSessionTest` 新增一页两个独立图文小节的场景
——两个小节各写两行文字（不是一行）是必要的构造，不是随意加料：
[linesToParagraphs] 用"本页所有相邻行间距的中位数×1.5"当分段阈值，只有
两行文字时中位数就是这一个间距本身，"间距≤间距×1.5"恒成立，两行必然
被并成一段，测不出"按小节分段"这个前提；两个小节内部保持 12pt 单倍行距、
两个小节之间隔 118pt，三个间距 `[12,118,12]` 中位数 12、阈值 18，段落
切分先验证过符合预期，图片插入位置的断言才有意义。TDD 红绿都跑过（禁用
`interleaveTextAndImages` 时确认新测试真的失败，语义是"文字全对、但图片
仍然全堆最后"，不是别的原因导致的假失败）。全量单元测试 239 条零失败。

## 52. 已修：表格检测器第四次被装饰线误判——2×3 独立方框排版的杂志式排版被当成表格，整页裁成一张大图

修 #51 之后真机复测同一份文档，发现"无线电系统/RF 电路"这一页压根没
走到 #51 那条新逻辑——诊断加日志确认这一页命中的是 `TableGridDetector`
的表格检测分支（`tableRegion != null` 但没有精确覆盖整页，覆盖比例约
92%~94%，够不到 #50 那条 0.95 的"整页误判"红线），整页被 PdfBox 自己
的 `PDFRenderer` 裁剪渲染成一张大图——图文从最初的扫描阶段就没有分开
过，#51 的新逻辑根本没有机会介入。

**根因**：这页是 2 列×3 行共 6 个独立小节的杂志式排版，每个小节自己带
一圈装饰性边框（黑色标题条+细线勾边）。6 个方框整齐排成网格，横线竖线
数量、整体跨度比例（`TableGridDetector` 已有的 `MAX_EXTENT_RATIO` 检查）
在这种"整整齐齐的网格"上完全测不出异常——**这次误判的形状本身就是一个
正经的 2×3 网格**，是这个检测器历史上（NOTES #17/#26/#39/#42 记录过三次
不同的装饰线误判）最像真表格的一次。

**第一版修法（个体线段长度占比，已废弃）**：最初以为"每条线自己贯穿
多长"能当信号——真表格一条网格线贯穿整个表格宽度，方框网格每个方框
的边框只贯穿自己那一格。反编译本类自己的真机 fixture
（`sample-with-table.pdf`，Chromium 打印 `<table border>` 生成）的原始
content stream 才发现这个假设本身是错的：**Chromium 逐格画边框**，同一
条表格网格线在真实数据里被拆成好几段矩形（三列表格一条横线是三段独立
矩形，不是一条通栏矩形），单段长度只有列宽，比值反而比这次要拦的方框
网格更低——用这个信号会把真表格自己的 fixture 也误杀（`PdfTextExtractor
TableTest` 四条断言当场跑挂），不是阈值选错，是这个信号本身在"真表格
允许逐格画线"的前提下不成立，两种情况的比例区间是反过来的。

**最终修复**：真正的区别是"分段之间连不连得上"，不是"每一段多长"——
Chromium 逐格画的短线段，相邻两段首尾相接（前一列右边界正好是后一列
左边界，同一条逻辑线被切开画，中间没有缝）；方框网格里，相邻方框之间
隔着真实的设计间距（这次真机样本约 20pt 的栏间距/行间距）。把同一方向
的所有线段坐标区间（横线用 X 区间、竖线用 Y 区间）排序后依次合并，真
表格严丝合缝零缺口，方框网格的间距会在合并区间里露出实打实的空隙——
`TableGridDetector` 新增 `hasCoverageGap`，容差 3pt（吸收取整/反锯齿
误差，远小于真实间距也远大于真表格的零间隙），横竖任一方向出现缺口
就判定不是表格。

**测试**：`TableGridDetectorTest` 新增 2×3 独立方框网格的复现场景（真机
版式简化：两列每列宽 250pt、三行每行高 180pt，方框之间留真实间距）；
`PdfTextExtractorTableTest` 的四条既有断言（依赖真机 `sample-with-table.pdf`
fixture）验证这次修复没有误伤真表格。TDD 红绿都跑过。全量单元测试
240 条零失败。

**方法论**：这是本类第四次被装饰线误判，也是第一次"先设计的信号被自己
的真机 fixture 反手打脸"——提醒即使是"看起来显然对"的几何直觉（线段
应该贯穿整个表格），也要拿项目自己已有的真实数据验证一遍再定案，不能
只用新构造的合成测试自证。

## 53. 已修：多栏排版文字抽取的两个基础正确性 bug——跨页拼版文字越界混入、两栏标题在同一 y 高度被错误粘连

修 #51/#52 之后用户想继续解决"图文错位"的根本问题（两栏排版重排后阅读
顺序打乱），排查过程中发现比"重排顺序"更基础的两个正确性 bug——顺序
问题的前提是"抽出来的文字本身是对的"，这个前提在两栏文档上并不成立。

**bug 1：InDesign 跨页拼版导致邻页文字越界混入**。用独立工具 poppler
分别抽取"RF 电路"页（第 7 页）和它前一页"天线设计"（第 6 页），发现
两页抽出来的是完全相同的 79 行内容，只是每行 X 坐标整体相差了一个页面
宽度（595.276pt）——第 7 页的抽取结果里混进了第 6 页的全部文字，只是
X 坐标是负数（比如"天线设计"四个字被记在 X=-515，页面宽度只有 0~595），
肉眼看不见。根因是 InDesign 导出跨页拼版文档时，一页的 content stream
会包含相邻页面的绘图指令（`MediaBox` 只是显示窗口，不是内容边界）——
这正是 [PdfTextExtractor.isSegmentOnPage] KDoc 记录过的同一类根因（网页
转 PDF 长文的矢量线段越界），但当时只修了矢量线段这一种内容，文字行
从来没做过同样的边界过滤。新增 `isLineOnPage`，跟 `isSegmentOnPage`
同样的做法：只检查文字行的 X 坐标（Y 已经是这一页自己坐标系里"距页顶
多少 pt"，不存在跨页污染），落在页面宽度之外的行直接丢弃，不参与任何
后续判断。

**bug 2：两栏标题恰好落在同一 y 高度时被错误粘连成一行**。排查"多栏
检测"该怎么实现时，先验证了 `mergeSameLineRuns`（负责把同一视觉行因
字体切换被拆开的碎片重新粘回一整行）的判断依据——发现它只看 y 坐标
相同，完全不检查 X 坐标离多远。真机数据验证：左栏标题"1/2 分频器"
（endX=98.82）和右栏标题"CMOS PLL 合成器"（startX=301.86）y 坐标恰好
相等（142.48…，两栏排版常见），会被直接拼成一条"1/2 分频器CMOS PLL
合成器"——不是顺序乱，是两栏内容的文字本身被错误粘连。补一条 X 坐标
距离检查（`LINE_MERGE_MAX_X_GAP_PT=20`）：同一份真机数据里，同一视觉
行内因字体切换（中英文混排、数字用不同字体）产生的正常片段间隙实测在
1.86~3.72pt 这个窄区间，两栏标题间距实测约 203pt——两者差了近 60 倍，
阈值选在两者之间偏正常间隙一侧，中间留了极宽的安全区间。

**方法论**：这次没有先跳到"设计分栏重排算法"，而是先验证"打算喂给
分栏算法的原始数据本身对不对"——两个 bug 都是这一步验证时发现的，
如果直接开始写分栏逻辑，会在两个已经被污染的数据源之上盖楼，"多栏
检测"本身也会被 bug 2 污染（两栏标题被粘连后，合并行的 X 范围会横跨
整个页面，掩盖掉本该存在的"两栏之间的空白带"这个信号）。

**测试**：`PdfTextExtractorTableRegionTest` 新增 `isLineOnPage` 的纯
逻辑测试（正常范围内、越界、贴边容差、`pageWidth=0` 时不过滤四种情况）；
`PdfTextExtractorSessionTest` 新增跨页拼版的端到端复现（一页正文+一段
X 坐标在页面外的"邻页污染文字"，验证 `Session.loadPage` 最终结果只有
真正属于这一页的文字）；`PdfTextExtractorTest` 新增 `mergeSameLineRuns`
的两栏反例（验证不合并）和中英文混排正例（验证正常场景不受影响）。
两个 bug 的 TDD 红绿都跑过。全量单元测试 248 条零失败。

**后续**：这两个 bug 修好之后，"两栏正文按行交替重排"这个原计划的
功能还没有开始写——下一步是在这两个 bug 已经修好、抽取结果干净的基础
上，做多栏检测（复用 #52 刚验证过的"坐标区间合并起来看有没有缺口"这个
技术，这次用在文字/图片的左右边界上）+ 命中时整页栅格化的兜底路径。

## 54. 已改：不追求两栏正文重排出正确阅读顺序，改成识别到两栏排版就整页栅格化

#53 修完两个基础 bug 后，继续按原计划设计"两栏检测+重排"，验证阶段发现
真实文档比预想复杂：同一页会在"两栏"和"通栏"之间切换（真机"天线设计"
页上半部是两栏、下半部又变回通栏，页面顶部还有一句跨越 78% 内容宽度的
介绍段落"骑"在两栏分界线上），简单的"整页有没有一条穿不断的空白带"
判断在这种混合版式上会漏判——要可靠识别，需要改成按高度分段检测（把
页面切成好几个横条分别判断），工作量和验证难度都远超原计划。

用户拍板改变方向：不追求"重排出正确的两栏阅读顺序"，改成"识别到两栏
排版就整页栅格化，不重排"。这个转向让风险模型完全不同——漏检的代价
只是"这一页维持 #53 两个 bug 修好之后的样子"（不会比现在更差），误判
的代价只是"这一页少了调字号能力"（pdfium 渲染的内容本身完全准确，
不会显示错误内容）。两种代价都能接受，检测器不需要做到完美，之前担心
的"按高度分段"这层复杂度不再需要。

**实现**：新增 `PdfTextExtractor.hasColumnGap(lines)`——跟 #52
`TableGridDetector.hasCoverageGap` 同一个技术，这次用在文字行的左右
边界上：把这一页所有文字行按 X 排序，依次合并成连续区间，只要中间出现
一次超过 `MIN_COLUMN_GAP_PT`（20pt）的缺口，且缺口两边各自至少有
`MIN_COLUMN_LINES_PER_SIDE`（5 行）文字，就判定为两栏。两个阈值都是
真机数据校准：真机两栏版式的栏间距实测约 33.47pt，两边行数各 17/18；
真机数据里同一页散落图注凑巧留出的孤立小缺口约 13.6pt、只有 1 行文字
（不满足行数门槛，正确排除）。命中时交给已经验证过的
`renderPageWithAndroidPdfRenderer`（pdfium）整页栅格化，不进入任何
段落切分/表格检测/图片抽取分支。

**已知局限（真机数据验证过、用户明确接受，不是没想到）**：这个方法
要求"全页没有任何一行文字跨过这条缝"，"天线设计"那种页面顶部有跨栏
介绍段落的混合版式会漏检——不修，风险模型见上面"方法论"一节，漏检
不会让页面变得比现在更差。

**测试**：`PdfTextExtractorTest` 新增 `hasColumnGap` 的五条纯逻辑测试
（两栏正例、跨栏介绍段落已知局限反例、孤立小缺口反例、无缺口单栏反例、
`pageWidth=0` 旧数据反例）；`PdfTextExtractorSessionTest` 新增端到端
复现（左右各 6 段独立文字、中间干净空白带，验证 `Session.loadPage`
走整页栅格化分支、不逐段重排文字）。两层 TDD 红绿都跑过。全量单元
测试 254 条零失败。

**装机验证**：真机日志确认"无线电系统/RF 电路"页（PDF 页码 7）正确
命中 `hasColumnGap=true`，`renderPageWithAndroidPdfRenderer` 栅格化
成功（`raster=true`，无异常无崩溃）；"天线设计"页（PDF 页码 6，已知
局限命中的那一页）`hasColumnGap=false`，继续走 #53 已经修好的正常
reflow 路径，不受影响，验证了预期的"漏检不掉队"这条设计目标。肉眼
在真机上完整滚动到这一页截图核对像素级效果这一步因为是连续滚动阅读器
（没有跳页功能）、手动滑动距离不好精确控制，最终没能截到图，以日志
证据为准——这条待补的视觉验证记在这里，供以后需要时复查。

## 55. 已查：小白 PDF"CV 模型识别版面+Yoga 重排"路线可行性评估——自己的 POC 结果偏负面，但对方 App 在同一难页上处理正确，结论不下定论

用户提出改用小白 PDF 阅读器（`com.jujie.xbreader`，设备上装着，APK
反编译过）那套架构：CV 模型识别版面区块，再用 Yoga（Flexbox）重排。
先用 `_loki` 精炼过一版可行性评估 prompt，纸面评估的"决定性约束"是
"黑箱模型对单人维护者不可验证"——没有停在纸面，本机用 Docker
（`python:3.10-slim`，容器名 `cv-validate`，PaddlePaddle 因
[GitHub#77340] 已知 oneDNN/PIR 回归 bug 从 3.3.0 手动降级到 3.2.2）
实跑了 PaddleX 3.7.2 + PP-DocLayout-M（Apache-2.0），挑四张真实问题页
人眼核对：

- **RF 电路页**（这条调查的起点，2 栏 6 小节密集图表）：**差**——6 个
  独立小节的图表被合并成一个巨框，漏检 4/6 小节标题；降低置信度阈值
  不是补全而是炸出几十个互相矛盾标签的重叠框
- **天线设计页**（`hasColumnGap` 已知会漏检的那页）：**差**——下半页
  约 40% 内容完全未检出
- **Ansys Optics 三栏页**（#50 我们几何规则曾经误判的表格页）：**好**，
  比我们当时打的补丁更干净
- **QFD 封面**：持平（两边都是整页当一张图）

4 张里 2 张更差（含起点那张）、1 张更好、1 张打平，当时结论是"不建议
投入 Yoga/Android 移植的后续工程量"。

**后续补测（本次会话完成）**：直接在小白 PDF 阅读器自己的"OCR 排版"
模式（应用内"页面设置→排版"三档之一）里打开同一份文档、翻到同一页
（RF 电路，PDF 页码 7），用户真机肉眼核对三项：阅读顺序、图表清晰度、
图文对应关系——**三项全部正确**（顺序对、图清晰、图文关系没有像我们
的 bug 那样打散）。

**这条结果不能简单读成"该投入 Yoga 路线"，也不能读成"我们的负面结论
错了"**——两者其实不矛盾：

1. 它推翻的是"CV 版面识别这条技术路线在这张难页上注定做不好"这个具体
   猜测（用真实反例证伪了）。
2. 它没有推翻可行性评估里那条决定性约束——对方具体怎么做到的仍然是
   黑箱：可能是更强的模型（PP-DocLayout-M 只是 PaddleX 里体量较小的
   一档，没试过 PP-StructureV3 等更重的模型）、可能有专门调过的置信度
   /后处理规则、也可能压根不是纯 CV 方案（比如混合了 PDF 自身的文字层
   坐标信息做校正）。不知道"为什么对方成功"，就没法判断这件事对一个
   业余时间维护、没有对方团队规模去调参/训练的个人项目是否可复现。
3. 更谨慎的读法是："我们选的模型/默认配置不够好"这个可能性没有排除，
   不能把"PP-DocLayout-M 默认参数在 4 张图上 2 好 2 差"直接等同于"CV
   这条路线对我们不可行"。

**结论：搁置，不是否决，也不是立项**。真要往下走，下一步该是"换更强
模型再测同一批真实问题页"或者"部分反编译小白 PDF 找它到底调了什么"，
两条都需要新的一轮投入才能得出更确定的结论，本次会话没有做。

**环境残留**：Docker 里 `cv-validate` 容器还留着（pip 装了
paddlepaddle 3.2.2 + paddlex 3.7.2 + PP-DocLayout-M 权重缓存在
`/root/.paddlex`）——如果决定不再往这条线投入，`docker rm -f
cv-validate` 释放空间；如果还要继续实验，环境是现成的不用重配。

## 56. 已查（终审）：反编译小白 PDF 找到真实架构——四件套模型+许可证问题，正式否决 CV/Yoga 路线；顺带推翻 #55 那份评估报告自己的推理方式

延续 #55 留的两条路，选了"部分反编译小白 PDF"。**先纠正一条过时的项目
记录**：本机 `adb`（`/Users/mac/Library/Android/sdk/platform-tools/adb`）
其实可用、设备已连接——CLAUDE.md/NOTES 里反复写的"本机没有 adb，真机
交互全靠用户手动装机+回传"这条环境限制已经不成立，下次需要跟真机交互
先试 `adb devices`，别再默认要求用户手动传文件。

**#55 那份 `_loki` 精炼过的可行性评估报告本身有个方法论漏洞，值得单独
记一笔**：报告把"决定性约束"定成"黑箱模型对单人维护者不可验证"——但
这条前提没有先试过基础逆向手段就下了结论。实际上 `adb pull` 拉 APK
+ `strings` 扫模型文件元数据，十几分钟就把"黑箱"打开了：模型架构、
版本、许可证全部拿到实锤（见下文）。"查不清楚"不是真的查不清楚，是
评估时没去查。这次终审结论虽然和上次报告的方向一致（都是不建议投入），
但**理由必须换成下面这些查实的事实，不能继续沿用"黑箱不可验证"这个
站不住的前提**——下次遇到"某方案是黑箱、没法评估"这类结论，先用最
基础的手段（`adb pull`/`unzip -l`/`strings` 这个量级）实际试一下再
下结论，不能把"没试"包装成"不可验证"。

**方法**：`adb pull` 直接拉 base.apk（299MB），`unzip -l` 看包内文件
结构 + 对模型文件跑 `strings` 找嵌入的元数据字符串——不需要反汇编
Smali/Java 代码，看 `assets/` 目录和原生库列表就已经拿到决定性信息，
没有进一步反编译 `libxbreader.so`/`libtt_ugen_layout.so` 这两个他们
自己的原生库去扒具体算法实现（技术上可行，但这已经从"看他们用了什么
公开组件"越界到"提取对方专有实现细节"，没必要也不应该做到这一步——
下面这些信息已经足够做出决策）。

**发现——四个 ONNX 模型 + 一个自研原生库，走 ONNX Runtime 推理**：

| 文件 | 大小 | 识别出的真实身份 |
|---|---|---|
| `doc_layout_x_260625.onnx` | 236MB | **YOLOv12x**（Ultralytics，AGPL-3.0）—— 版面区块检测，YOLO 系列里最大最准的"x"档 |
| `pc_260629.onnx` | 24MB | **YOLO12s-cls**（Ultralytics，AGPL-3.0，strings 里直接带
"Ultralytics YOLO12s-cls model trained on..."+ 许可证声明）—— 轻量页面分类器，猜测是"这页是不是扫描版/整页图/正常排版"这类预判 |
| `ocr_det_mobile_v4.onnx` | 4.8MB | PaddleOCR 系文字检测（strings 命中"PaddlePaddle Graph in PIR mode"，命名对应 PP-OCRv4 mobile 检测档） |
| `ocr_v6_rec_small.onnx` | 21MB | PaddleOCR 系文字识别（同样 PIR mode 签名），配 `assets/labels/ocr_dict_v6.txt`（18708 字符字典，标准 PaddleOCR 字典格式） |

`lib/arm64-v8a/libonnxruntime.so` 承载全部四个模型的推理（ONNX Runtime
是引擎无关的，Paddle 模型经 `paddle2onnx` 转过来一起跑）；
`libtt_ugen_layout.so`（440KB，ByteDance 内部命名风格的 `tt_` 前缀）
大概率是他们自己写的原生重排/版面合成引擎，把检测框+识别文字组装成
最终排版——这部分没有再深挖，判断到此为止。

**这直接回答了 #55 留的问题"对方到底是不是用了更强的模型"——是，而且
差距不是一星半点**：我们 POC 用的 PP-DocLayout-M 是 PaddleOCR/PaddleX
生态里偏轻量的一档；对方用的是完全不同的模型家族（YOLOv12，2025 年
的新架构，以密集小目标检测见长，跟"一页塞 6 个独立小节图表"这种场景
天然对口），选的还是同档里最大的"x"变体，外加一个专门的页面分类器
做前置判断、一条完整的 PaddleOCR 文字识别管线兜底扫描内容、一个自研
原生库做最终合成——四层管线，不是我们那种"单模型一次推理出结果"的
简化设计。

**新增决定性因素：AGPL-3.0 许可证**。`pc_260629.onnx` 的字符串里
直接嵌了"AGPL-3.0 License (https://ultralytics.com/license)"——
Ultralytics 的 YOLO 系列模型走 AGPL-3.0，商用要么公开使用方 App 的
完整源码，要么找 Ultralytics 买 Enterprise 商业授权。这台设备上这个
App 怎么处理这条义务不是我们能查的，但如果我们自己想复刻同一条技术
路线（用 YOLOv12 训练/微调一个版面检测模型），这条法律义务会直接落到
我们自己头上——对一个业余维护、不打算开源全部代码也不打算买商业授权
的个人项目，这条路本身就先天不通，跟"模型选轻了"完全是两个层级的
问题。

**终审结论：正式否决，不再是"搁置"**。三个理由叠加，任何一条单独都
够放弃，三条一起没有讨论空间：① 模型体量差距（236MB 的 YOLOv12x 主
检测模型，训练/维护这个量级的模型不是个人项目能对等投入的）；② 四层
管线架构复杂度（检测+分类+OCR+自研合成，不是换个模型就能对齐的架构
差距）；③ AGPL-3.0 许可证义务与个人闭源 App 天然冲突。维持现状——
`hasColumnGap` 识别到两栏排版就整页栅格化（#54），不再评估 CV 版面
识别这条路线。

**收尾**：`docker rm -f cv-validate` 释放掉 POC 用的容器；反编译过程
中 `adb pull` 出来的 300MB APK 和解包出的模型文件都是对方版权资产，
只用于本地临时技术判断，看完立刻删除，没有留存也没有提交到仓库
（`.gitignore` 补了 `.research/` 这条规则防止以后误存同类临时产物）。
